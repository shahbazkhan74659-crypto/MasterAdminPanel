# Technical Decisions

These decisions were made during prior planning discussions, before any production code was written, originally recorded in `docs/DESIGN.md` and `docs/tech.md`. Both files were migrated into this 6-file documentation system (2026-09-08) and then deleted — their content now lives here (and in `PROJECT.md`/`ARCHITECTURE.md`) rather than in `docs/`. Where original reasoning was not captured at the time, this is marked explicitly rather than guessed.

## Decision: A shared "Content Admin API" protocol, not a per-CMS scraper

- Status: Accepted
- Date: Prior to 2026-09-07 (exact date not captured; locked as of the 2026-09-07 commit that introduced the design/tech docs)
- Context: The owner repeatedly needs an admin/CMS UI for websites they build, and was building one bespoke UI per project.
- Decision: Build one admin panel that speaks a single protocol — the Content Admin API (auth, list collections, get a collection's schema, CRUD on records, media upload/listing, search) — that any backend can implement. The panel does not know about specific CMS platforms.
- Reasoning: Avoids rebuilding a one-off admin UI per project. Two tiers of implementer are anticipated: sites the owner builds (primary v1 target, implement the contract natively) and third-party platforms like WordPress/Shopify (would need a per-platform adapter, not required for v1).
- Consequences: The API's own tech stack is a per-site decision, out of scope for this repository. No spec draft has been written yet — see `PHASES.md`/`TASKS.md`.

## Decision: Site-first, not spec-first — build the Content Admin API before formally documenting its spec

- Status: Accepted (resolved by roadmap ordering)
- Date: 2026-09-08
- Context: The original design record left open whether to lock down the full Content Admin API spec before building anything ("spec-first") or start from one real implementation and generalize the contract from what it actually needs ("site-first").
- Decision: The roadmap (`PHASES.md`) orders Phase 6 (Data Management System engine) and Phase 7 (Content Admin API implementation) before Phase 9 (writing the formal spec document) — resolving the question in favor of **site-first**.
- Reasoning: Owner's phase ordering when defining the roadmap; not separately argued, but the practical effect is that the spec gets written from a real, working implementation rather than designed speculatively up front.
- Consequences: Phase 9's spec document must describe what Phase 6/7 actually built, not aspirational behavior — see `CLAUDE.md` rule 9. If Phase 6/7's implementation changes significantly after Phase 9, the spec document will need a follow-up update to stay accurate.

## Decision: Schema-driven forms are the load-bearing design piece

- Status: Accepted
- Date: Same planning period as above
- Context: For one UI to render editors for arbitrary content types across arbitrary sites, the panel needs to know each field's shape without per-site custom frontend code.
- Decision: The Content Admin API must describe its own schema (field name, type, validation, relations) so the panel can generate the appropriate input (text, rich text, image picker, relation dropdown, date, boolean, array, etc.) generically.
- Reasoning: The same trick LSP uses to let one editor support many languages, scoped here to content administration. Get this right first — everything else (tabs, tree, palette) is UI built on top of it.
- Consequences: This is the piece implementation should prioritize getting right before other UI work — see `TASKS.md`.

## Decision: Image and Video as real schema field types (backend), with dedicated frontend UI

- Status: Accepted
- Date: 2026-09-08
- Context: A review of the original design surfaced a gap — "media upload/listing" was named as a Content Admin API capability, and "image picker" was mentioned narratively as a schema-driven form field type, but neither had any real backend field-type support or dedicated frontend UI scheduled anywhere in the roadmap. No video support was mentioned at all.
- Decision: Image and Video are real field types in the backend content-editing logic ([Phase 6] "Data Management System Backend," alongside the existing text/rich-text/relation/date/boolean/array types) — storing/retrieving image and video field values as part of a record's schema and CRUD flow. The corresponding frontend picker/upload/preview UI is separately scoped to [Phase 26] "Image and Video Field UI (Frontend)."
- Reasoning: Owner's explicit direction, closing a real gap between what the design narratively implied (media handling, an "image picker") and what was actually scheduled to be built (nothing).
- Consequences: A dedicated media library/browser UI (a gallery/grid of uploaded media, search/filter, delete) is still **not** scheduled — Phase 6/26 cover per-record Image/Video *fields*, not a standalone media management surface. How much real editing logic Phase 26 includes (upload/preview/replace only, vs. actual crop/trim-style editing) is not yet decided — see `PHASES.md`'s Phase 26 entry.

## Decision: VS Code interaction metaphor for the panel's UI

- Status: Accepted
- Date: Same planning period as above
- Context: The panel needed a UI/interaction model distinct from a typical boxy CRUD admin UI.
- Decision: Model the panel visually and interactively on VS Code — activity bar (switch sites/workspaces or modules), explorer sidebar (current site's collections/records tree), editor tabs (open records, unsaved-changes dot), editor pane (schema-driven form, or Monaco for raw JSON/HTML/code fields), command palette (quick actions like "New Post"/"Publish"/"Switch Site"), status bar (connected site, environment, save/sync state), split panes (compare two records or environments).
- Reasoning: Not explicitly captured beyond the stated preference; likely follows from the owner's own familiarity with VS Code as a power-user interaction model well-suited to a tree-of-content-types + open-record-tabs structure.
- Consequences: Reusing Monaco (VS Code's real editor component, open source) for code/JSON fields and the SQL console follows directly from this metaphor — see the stack decision below.

## Decision: SQL console connects directly to the database (exception to the API protocol)

- Status: Accepted
- Date: Same planning period as above
- Context: In addition to the schema-driven CRUD path, the owner wants a dedicated SQL query console (its own tab type, VS Code database-extension-styled: table chips, query input, result grid) for personal database work.
- Decision: The SQL console bypasses the Content Admin API entirely and connects directly to the target database. Kept visibly separate (its own tab, its own icon) rather than blended into the record editor, since raw SQL bypasses schema validation and any safety the Content Admin API would otherwise enforce.
- Reasoning: Acceptable specifically because this is a personal-use tool with one trusted operator and no multi-tenant exposure — a materially different trust boundary than the generic content forms, which is why it must stay visibly distinct rather than quietly reusing the same code path.
- Consequences: This directly drove the "not a pure web app" stack constraint below (a browser cannot open a raw DB connection). ~~**Still open, not yet decided**: whether the real implementation defaults to read-only with an explicit confirm step before non-`SELECT` statements, or stays unrestricted.~~ **Resolved 2026-09-09** — see the "SQL console write-safety default" decision below. The *granular*, per-connection version of this question remains open for [Phase 8].
- **Refinement (2026-09-08, Phase 7 planning — see `PHASES.md`):** "connects directly to the target database" covers two cases, not one. For a locally-reachable database (e.g. Phase 1's local Postgres/MySQL/SQLite), the console's backend driver opens a real direct connection, no intermediary. For a real remote website's database that isn't directly network-reachable by the panel, Phase 7's **Content Admin API** implementation is the bridging mechanism — the console's queries get routed through that site's own API server to reach its database. This is still the same trust boundary as "direct" access (unrestricted raw SQL, no schema validation) — it is *not* the schema-driven CRUD path — just carried over a different transport when a raw TCP path isn't available. Do not confuse this bridging role with the Content Admin API's original schema-driven CRUD purpose; a single site's Content Admin API implementation may end up serving both purposes (CRUD forms and raw SQL bridging) but they remain conceptually distinct capabilities.

## Decision: Production stack — Electron + React/TypeScript + Vite + Monaco + Zustand + native DB drivers

- Status: **Superseded** (2026-09-08) — see "Browser-based web app, not an Electron desktop shell" below.
- Date: Same planning period as above (locked alongside the design)
- Context: The SQL console requirement (direct DB connection) rules out a pure web app — a browser cannot open a raw TCP connection to Postgres/MySQL/SQLite. The panel needs a real backend process with native DB drivers.
- Decision:
  - Shell: **Electron** — same foundation as real VS Code; Node.js main process gets direct DB-driver access with no sidecar process needed.
  - UI: **React + TypeScript**, built with **Vite** — matches the component/state model already proven in the prototype; fast dev loop.
  - Code/SQL editing: **Monaco** — real syntax highlighting for the SQL console and any raw JSON/code fields, instead of a plain textarea.
  - State management: **Zustand** — lightweight global store for open tabs, active site, connections; no Redux ceremony needed for a solo project.
  - DB access (SQL console): **Node drivers per engine** (`pg`, `mysql2`, `better-sqlite3`) running in the Electron main process, exposed to the renderer via `contextBridge`/IPC.
  - Content Admin API client: plain `fetch` from the renderer — outbound HTTPS only, to sites the user controls.
  - Secrets (API keys, DB credentials): Electron's built-in **`safeStorage`** — OS-backed encryption, no extra native dependency (skips `keytar`). DB credentials and per-site API keys are two separate secret categories — do not conflate them.
  - Local app state (layout, recent sites, open tabs): **electron-store** — simple JSON persistence for non-secret settings only.
  - Packaging: **electron-builder** — produces a Windows installer for the primary dev machine.
- Reasoning: Everything follows from the direct-DB-connection constraint; see per-row rationale above.
- Consequences: This resolved the "Runtime: local desktop app vs. web app styled to look like VS Code" question that the original design record had listed as open — in favor of Electron. **Reversed 2026-09-08** — see "Browser-based web app, not an Electron desktop shell" below. No implementation was ever built against this stack, so nothing needs unwinding in code.

## Decision: Reject Tauri as the shell

- Status: **Superseded** (2026-09-08) — moot now that the shell itself (Electron) is superseded; kept here as a historical record of the reasoning, since a Rust/Tauri shell could resurface as an alternative if the browser-based direction is ever reversed again.
- Date: Same planning period as above
- Context: Tauri (Rust backend + OS webview) was considered for its smaller binary size and lower memory use, as an alternative to Electron.
- Decision: Rejected.
- Reasoning: The SQL console's DB layer would either need a Rust rewrite (`sqlx`) or a Node sidecar process anyway under Tauri, adding complexity without a clear payoff for a personal, single-user tool where dev speed matters more than binary size.
- Consequences: Revisit only if resource usage becomes a real practical problem — not expected to be reopened otherwise.

## Decision: Browser-based web app, not an Electron desktop shell

- Status: Accepted
- Date: 2026-09-08
- Context: The owner decided to run the panel as a browser-based site rather than an installed Electron desktop app, superseding the earlier Electron/Tauri-shell decision above. This does not remove the original constraint that drove that decision — a browser still cannot open a raw TCP connection to Postgres/MySQL/SQLite for the SQL console — it moves where that constraint is satisfied: a real backend server process now holds the native DB drivers (still `pg`/`mysql2`/`better-sqlite3`, still Node.js) and the browser talks to it over HTTP/WebSocket instead of an Electron main process talked to over IPC.
- Decision: Build the panel as a client (React + TypeScript, Vite) + backend server (Node.js) web app, deployed as a hosted site rather than packaged as a desktop installer.
- Reasoning: Owner's explicit direction ("just use it as a browser based site"), prioritizing browser accessibility over an installed desktop app.
- Consequences:
  - Everything Electron-specific from the superseded stack decision no longer applies: `safeStorage` (secrets), `electron-store` (local settings), `electron-builder` (packaging), and `contextBridge`/IPC (renderer↔main communication).
  - What still carries over unchanged: React + TypeScript + Vite (frontend), Monaco (Monaco is web-native — it works in a plain browser at least as well as in Electron), Zustand (frontend state), and the `pg`/`mysql2`/`better-sqlite3` drivers (now living in a backend server instead of an Electron main process).
  - A backend server framework/runtime choice is now needed (not yet decided — see Open decisions below).
  - Secrets storage (API keys, DB credentials) needs a new server-side mechanism to replace `safeStorage` — not yet decided.
  - A new requirement that didn't exist for the desktop app: **the site needs its own authentication in front of it**. A desktop app installed only on the owner's machine was implicitly trusted because only the owner could open it; a browser-based site is reachable by anyone who has the URL unless it's gated. See the "Single-user login" decision below.
  - Hosting is decided — see the "Render free tier + UptimeRobot" decision below.

## Decision: Single-user login for now; multi-agent/admin-policy access deferred to Version 2

- Status: Accepted (partial) — single login only; the admin/policy layer is a stated Version 2 future direction, not designed
- Date: 2026-09-08 (retitled/tagged "Version 2" 2026-09-09, alongside the "Generate Connection API deferred to Version 2" entry below — same treatment, not a change in substance)
- Context: Going browser-based (see above) means the site needs real authentication, unlike the desktop app's implicit single-operator trust model. The owner was asked what auth model to build.
- Decision: For now, build only a single-user login flow (the owner's own account). The owner has also stated a future direction — additional accounts ("agents") should only be creatable by an Admin, under some policy system — but explicitly scoped that out of current work: "for now only Login flow." This is now grouped with other explicitly-deferred-to-**Version 2** items (see the "Generate Connection API deferred to Version 2" entry below).
- Reasoning: Owner's explicit direction — keep the current build scoped to what's needed now (a working login gate) rather than building out a multi-account/policy system that isn't needed yet.
- Consequences: Do not build multi-account creation, roles, or a policy engine until the owner asks for it specifically — treat "Admin-created agents under a policy" as a noted Version 2 future direction in `PROJECT.md`/`TASKS.md`, not a current requirement, and not assigned to any phase in the locked Phase 0–30 roadmap. Specific login mechanics (session vs. token, password storage/hashing, where the single account's credentials live) were later decided — see the "Login mechanics" decision below.

## Decision: Hosting — Render free tier + UptimeRobot

- Status: Accepted
- Date: 2026-09-08
- Context: The browser-based site (see above) needs a hosting target. The owner's other project, Portfolio, already uses this exact pattern successfully (see Portfolio's own `Project Docs/ARCHITECTURE.md`/`DECISIONS.md`).
- Decision: Host on Render's free tier, with a free UptimeRobot monitor pinging the live URL to prevent Render's free-tier idle spin-down/cold-start — the same arrangement as the Portfolio project.
- Reasoning: Owner's explicit direction, reusing a pattern already proven to work on another of the owner's projects.
- Consequences: Render's free tier has no persistent disk (a real constraint the Portfolio project also hit for media storage) — if this panel ever needs to persist files (e.g. uploaded media through the Content Admin API path) rather than just proxying to remote sites, the same kind of external-storage workaround Portfolio needed (Cloudinary) may apply here too. Not yet relevant to the SQL-console/login-only current scope. Database hosting for the panel's *own* data (e.g. the single login account) is resolved in direction by [Phase 28] "Neon Free-Tier Postgres Setup" (see `PHASES.md`) — a Neon Postgres database, same pattern as Portfolio's production database — though the actual setup hasn't happened yet.

## Decision: Build a visual design canvas prototype before production implementation

- Status: Accepted (observed via existing artifact)
- Date: 2026-09-07 (commit `90a90b4`)
- Context: The owner wanted to validate the VS Code-metaphor UI design interactively before committing to a build.
- Decision: Publish a Claude Design canvas (`design/master-admin-panel.html`, source `design/Main.dc.html`/`design/canvas.json`) as a mock frontend — no real API calls, no real DB connection, no backend.
- Reasoning: Validates the UI/interaction design against the locked product concept before real implementation effort begins.
- Consequences: This prototype is a design/validation reference only, the same way Portfolio's `prototype/index.html` was a styling reference only for that project — it is not expected to be ported into or reused as production code. No decision has been recorded either way on reuse, since production implementation hasn't started; treat "prototype is reference only, not to be ported" as the working assumption unless the owner says otherwise when implementation begins.

## Decision: Split Phase 12 into 12a (background) and 12b (login page); design the login page fresh, no prototype to match

- Status: Accepted
- Date: 2026-09-09
- Context: The original Phase 12 covered only the global background. The owner wanted a login page built as its own static frontend phase, separate from Phase 13's base structure. But the design canvas prototype (`design/master-admin-panel.html`) was built before the browser-based pivot introduced the login requirement (see "Browser-based web app" and "Single-user login" decisions above) — it has no login screen at all, so there's nothing for a login-page phase to pixel-match, unlike every other static UI phase (12a, 13–22).
- Decision: Split [Phase 12] into [Phase 12a] "Global Background Matching the Prototype" (unchanged scope) and [Phase 12b] "Login Page (Static, Theme-Matched)" (new). Phase 12b's visual design is created fresh as part of that phase's own scope — using the prototype's VS Code-metaphor visual theme (colors/typography/style tokens) for consistency — rather than matching a pre-existing mockup that doesn't exist. The login page stays a standalone static page, explicitly not folded into Phase 13's base global structure the way Phase 12a's background is.
- Reasoning: Owner's direct choice, made when asked how to handle the missing prototype reference — design fresh within the phase rather than requiring a separate design step beforehand.
- Consequences: `PHASES.md`'s Phase 13 (title, objective, scope, completion criteria) and Phases 14–22's "match the prototype exactly" standard-setting language were updated to reference Phase 12a specifically (not the now-split Phase 12). Phase 23's wiring-up scope now explicitly includes wiring Phase 12b's login page to Phase 4's `/auth-api/login` endpoint, alongside the other static-to-real wiring it already covered.

## Decision: SQL console write-safety default — read-only, confirm:true required for writes

- Status: Accepted
- Date: 2026-09-09
- Context: The "SQL console connects directly to the database" decision above left open whether the real implementation defaults to read-only with a confirm step, or stays unrestricted. [Phase 5] "SQL Console" was scoped to resolve this basic global posture as part of its own completion criteria (see `PHASES.md`) — distinct from [Phase 8] "SQL Console Access/Modification Policy," which covers a more granular, configurable, per-database/per-connection policy layer built on top of whatever basic default Phase 5 established. `DECISIONS.md`'s own "Still open" note above, and `CLAUDE.md`'s project notes, had both drifted toward attributing the whole question to Phase 8 alone — this entry and the corrections alongside it fix that.
- Decision: The console defaults to **read-only**. Any statement classified as a write requires the request to explicitly pass `confirm: true`, or it's rejected with `409`. Classification is a pragmatic, honest-effort scan (not a full SQL parser): a statement is read-only if its first keyword is in an allowlist (`SELECT`, `SHOW`, `EXPLAIN`, `DESCRIBE`/`DESC`, `PRAGMA`, `WITH`), and additionally, the whole statement is scanned for write keywords (`INSERT`/`UPDATE`/`DELETE`/`MERGE`/`DROP`/`ALTER`/`TRUNCATE`/`CREATE`/`GRANT`/`REVOKE`) as a safety net for CTEs (`WITH ... AS (...) INSERT ...`) — a design that can rarely over-block (e.g. a CTE literally named `update_log` used only in a `SELECT`) but never under-blocks, which is the correct direction to err. Requests containing more than one SQL statement (detected via a naive semicolon check, not real statement parsing) are rejected outright at `400`, closing the `SELECT 1; DROP TABLE users;` bypass rather than trying to classify each statement independently.
- Reasoning: Owner's direct choice. A safe-by-default posture costs nothing for the console's only current use (Phase 5's own verification and future ad-hoc personal queries) while a real accidental-write mistake is exactly the failure mode a personal admin tool with direct DB access should guard against by default.
- Consequences: Implemented in `backend/src/sqlConsole/classify.ts` (`isWriteStatement`, `hasMultipleStatements`), enforced in `backend/src/sqlConsole/sqlConsoleRoutes.ts` before any connection is touched. Known, accepted gaps — not full SQL-parser rigor, single-statement-only, `PRAGMA` treated as always read-only even for the rare pragma that mutates connection state — are deliberately left for [Phase 8] to tighten if the owner wants a more rigorous or configurable policy later.

## Decision: Data management schema via auto-introspection + column-naming convention for special field types

- Status: Accepted
- Date: 2026-09-09
- Context: [Phase 6] "Data Management System Backend" needed a way to determine a collection's schema (field names/types) without a separate config file to author and maintain, while still supporting the field types `PROJECT.md`/`DECISIONS.md` describe (rich text, image, video, relation) that don't map to any native SQL column type.
- Decision: Schema is **auto-introspected** from the database's real columns (native type → base type: text/integer/float/boolean/date/other). A **naming convention on the column name** tags the special types raw introspection can't express: `_richtext`, `_image`, `_video` (each requires the underlying column be `text`-based), and `_id_relation` (requires `integer` or `text`, and resolves its target collection by trying `<stem>`, `<stem>s`, `<stem>es` against the engine's real table list). If a suffix matches but the base type is incompatible, or a relation target can't be resolved, the suffix is ignored — the field falls back to its plain base type with `specialType: null`, and a server-side warning is logged, rather than erroring. Scoped to the same Phase 1b local databases Phase 5 already targets — not remote sites (that's Phase 7's job).
- Reasoning: Owner's direct choice. Avoids a second, hand-maintained source of truth about a collection's shape that could drift from the real table; the naming convention is a pragmatic, zero-config way to express the handful of extra field types this project actually needs.
- Consequences: Implemented in `backend/src/dataManagement/fieldTypes.ts` (`mapNativeType`, `applySpecialType`) and `introspection.ts` (`getTableSchema`, primary-key detection per engine). A known, accepted gap: SQLite has no native boolean/date type, so a declared column type that doesn't literally contain a recognizable type-name substring (e.g. `DATETIME`) falls to `baseType: "other"` rather than being inferred — not solved further this phase. A table with no primary key, or a composite one, still returns a schema but rejects `:id`-based record operations with a 400 (`supportsRecordOperations: false`) rather than attempting to support it.

## Decision: Real, single-step file-upload handling for Image/Video fields

- Status: Accepted
- Date: 2026-09-09
- Context: The original schema-driven forms decision named an "image picker" narratively but never specified real upload mechanics. [Phase 6]'s own scope note left this "to be defined... as the owner directs," and the owner was asked directly what they wanted.
- Decision: Uploading a file to an Image/Video field is a **single backend request** — no separate "upload, get a path back, then PATCH the record" flow. `POST /data-api/:engine/:collection/records/:id/upload/:field` (using `multer`, a new dependency) validates the target field is actually tagged `image`/`video` in that collection's introspected schema, confirms the record exists, saves the file to a new gitignored `backend/media/<engine>/<collection>/<field>/` with a generated filename (never the client's original filename) and a mimetype check against the field's declared type, then writes the resulting served path (via a new `/media` static mount) directly into the record's column as part of the same request/response.
- Reasoning: Owner's explicit direction — an admin/agent should be able to upload a file directly without knowing or constructing a file path themselves; the backend handles storage and record-linking together. This is real backend work (an API endpoint), distinct from [Phase 26]'s separate job of building the actual frontend picker/preview UI component.
- Consequences: Re-uploading to a field that already holds a path leaves the old file on disk untouched — no cleanup/orphan-deletion logic this phase (an accepted, deliberate limitation; full lifecycle management would be more than a personal tool needs right now). File-size/mimetype limits (10MB images, 200MB video, filtered by MIME prefix) are basic sanity bounds, not a security-hardened upload pipeline. `backend/media/` is gitignored, same treatment as `local-data/`.

## Decision: Login mechanics — server-side session cookie, bcrypt hashing, Postgres-backed storage

- Status: Accepted
- Date: 2026-09-09
- Context: The "Single-user login" decision above committed to building a login flow but explicitly left its mechanics open: session vs. token auth, password hashing, and where the owner account's credentials live. [Phase 4] "Login Auth Engine/Flow" was scoped to resolve all three.
- Decision:
  - **Auth mechanism**: server-side session via an httpOnly cookie (`express-session`), not a JWT/token.
  - **Password hashing**: bcrypt-format hashing via the `bcryptjs` package (pure JS, produces standard `$2a$`/`$2b$` hashes — same format as native `bcrypt`, chosen to avoid Windows native-build friction; no perf concern at single-user, occasional-login scale).
  - **Credential storage**: a new `users` table in Phase 1a's `adminpanel_app` Postgres database — the local database already provisioned specifically for the panel's own data. Schema created via a plain `CREATE TABLE IF NOT EXISTS` run at server startup (`backend/src/db/schema.ts`); no migration framework introduced for one table.
  - **Session storage**: sessions are also persisted in `adminpanel_app` (via `connect-pg-simple`, its own auto-created `session` table), not in-memory — chosen so logins survive `tsx watch`'s dev-time restarts and Render's eventual free-tier cold starts (Phase 29).
  - **Account creation/reset**: a one-off CLI script (`backend/src/scripts/seedOwner.ts`, run as `npm run seed:owner -- <username> <password>`) is the only way the account gets created or its password changed. No signup flow exists or is planned — consistent with the project's single-owner, no-multi-account scope.
- Reasoning: Owner's direct choices, aligned with the project's stated non-goals (single operator, no multi-account system) and with reusing infrastructure already committed to (Phase 1a's Postgres) rather than introducing new storage just for auth.
- Consequences: `backend/src/index.ts` gained `express.json()` and `express-session` middleware; a shared `pg.Pool` module (`backend/src/db/pool.ts`) now exists, scoped only to `adminpanel_app` — a new pattern distinct from `testRoutes.ts`'s per-request connections to the Phase 1b target databases, which remain unchanged. No existing route (e.g. `/test-api/*`) was gated behind auth by this decision — per the roadmap, real route-gating is Phase 23's job, once a real UI exists to protect. Production HTTPS deployment (Phase 29) will need `cookie.secure: true` and `app.set("trust proxy", 1)` (Render terminates TLS at a proxy) — flagged in code as a comment, not solved yet. `SESSION_SECRET` was added to `.env.local` (already gitignored) as this app's own runtime config — distinct from [Phase 10]'s "secrets storage mechanism," which covers secrets for *managed sites*, not this app's own session-signing key.

## Decision: Phase 7 implemented as a direct remote-DB connection, not an API bridge

- Status: Accepted
- Date: 2026-09-09
- Context: [Phase 7]'s objective/scope (see `PHASES.md`) originally framed the Content Admin API as an HTTP protocol a remote site's own backend would implement, which this panel's backend calls as a client — bridging the SQL Console (Phase 5) to a site whose database isn't directly network-reachable, with queries "technically routed through the site's own API server" (see the "SQL console connects directly to the database" decision's Phase 7 refinement note above). Mid-implementation, the owner gave a hard, direct instruction: no code in the target site's own repository may be touched at all, to test this or for any other reason. The site chosen to exercise this phase was Portfolio (`C:\Portfolio`), a real site the owner already runs, using its **local dev Postgres** database (not its production Neon database) — its connection details read directly from Portfolio's own untouched, gitignored `.env` file.
- Decision: Since nothing can be added to Portfolio's (or any connected site's) own codebase, Phase 7 does not implement an HTTP API that a site exposes. Instead, this panel's own backend is given the target site's real database credentials directly and connects to it with its own native driver — exactly the same direct-connection model [Phase 5] already uses for the three local Phase 1b databases, just pointed at a second, real site's actual database instead of one this panel provisioned for itself. Scope was also narrowed to only the SQL-bridging capability Phase 7 explicitly names — the full originally-designed Content Admin API surface (auth, collections, schema, CRUD, media, search, exposed over HTTP to arbitrary sites) was not built this phase and remains open for whenever it's actually needed.
- Reasoning: Owner's explicit, direct constraint against touching a connected site's own code, given mid-session. A real remote database that accepts direct network connections (Portfolio's Postgres, like the local Phase 1b databases and unlike, say, a database that's only reachable from inside a site's own private network) doesn't actually need a site-implemented bridge to be queried this way — the bridge mechanism described in the original Phase 7 framing remains the right approach specifically for a site whose database genuinely isn't directly reachable, which was not the case here.
- Consequences: `backend/src/sqlConsole/connections.ts` gained a `remoteSites` registry (Postgres-only, id-keyed, built from `REMOTE_SITE_<ID>_*` env vars in `.env.local` — interim placement, same caveat as every other credential in that file; real secrets storage is still Phase 10's job). `adapters.ts`'s `runPostgresQuery`/`listPostgresTables` were generalized to take a `Pool` parameter instead of closing over the fixed local `pgPool`, so the same logic serves both the local `postgres` engine and any registered remote site (Phase 6's `dataManagement/introspection.ts`, the other caller of `listPostgresTables`, was updated to keep passing the local `pgPool` explicitly — no behavior change there). New routes `GET/POST /sql-console-api/sites/:siteId/{tables,query}` reuse Phase 5's `isWriteStatement`/`hasMultipleStatements` safety gate verbatim. Verification against Portfolio's real dev database was deliberately read-only only (table listing, real `SELECT`s, and the negative-case matrix) — no write was ever sent with `confirm:true` against that real data, unlike Phase 5's own verification, which ran a full write round-trip against its own dedicated test databases. If a future site's database genuinely isn't directly network-reachable, the original bridge-through-the-site's-own-API framing is still the right model to reach for then — this decision only records why it wasn't the right fit for Portfolio's directly-reachable Postgres under the "don't touch the site's code" constraint.

## Decision: Generate Connection API deferred to Version 2

- Status: Accepted
- Date: 2026-09-09
- Context: [Phase 7]'s implementation (see the "Phase 7 implemented as a direct remote-DB connection, not an API bridge" entry above) connects one specific site (Portfolio) to the SQL Console via a hardcoded id in `backend/src/sqlConsole/connections.ts`'s `remoteSites` registry, with that site's credentials read from fixed `REMOTE_SITE_<ID>_*` env vars in `.env.local`. Connecting a *new* site today requires manually editing code (adding its id to the registry) and `.env.local` (adding its credentials), then restarting the backend — there is no API endpoint or UI to register a new site's database connection at runtime.
- Decision: A general "Generate Connection" capability — submit a new site's connection details (host/port/credentials) through an API/UI and get back a usable connection without a code change or restart — is explicitly deferred to **Version 2**, a future direction beyond the current locked Phase 0–30 roadmap. It is not assigned to any phase in `PHASES.md` and is not current scope.
- Reasoning: Owner's explicit direction, given directly after Phase 7 shipped. Phase 7's own scope was already narrowed to proving the direct-connection bridge works against one real site; building a general connection-management system on top of that is a materially larger, separate capability that wasn't part of what Phase 7 committed to.
- Consequences: `PROJECT.md`'s Core Features section now carries this as a named Version 2 direction (same "noted as intent only, not current scope" treatment as the multi-agent/admin-policy direction already recorded there). Do not build this until the owner asks for it specifically, and do not assume it slots into any existing Phase 0–30 phase (including [Phase 8]'s access/policy work or [Phase 10]'s secrets storage) without the owner saying so directly — `PHASES.md` remains locked against unprompted edits.

## Decision: Current scope is local-disk databases only — remote/production database connections (e.g. Neon) are Version 2

- Status: Accepted
- Date: 2026-09-09
- Context: Discussing whether [Phase 7]'s mechanism could also reach Portfolio's **production** Neon Postgres database (rather than only its local dev Postgres, which Phase 7 actually connects to) surfaced two real gaps: (1) the current `Pool` construction in `backend/src/sqlConsole/connections.ts` sets no `ssl` option, and Neon requires TLS (`sslmode=require`) — a connection attempt would likely fail outright as-is; (2) more importantly, a genuinely remote/production database carries a materially different risk profile than a database reachable on the same machine/disk (AdminPanel's own Phase 1b test databases, or another local site's local dev database like Portfolio's) — it holds real, live, user-facing data behind a real network hop, not a local dev sandbox.
- Decision: The current locked roadmap (Phase 0–30, i.e. "Version 1") restricts **both** the SQL Console's local target engines (Phase 5) **and** its Phase 7 remote-site extension to databases reachable **locally, on the same machine/disk as AdminPanel itself** — never a genuinely remote/production database reached over the network. Connecting to a site's actual production/hosted database (e.g. Portfolio's Neon instance, or any future site's equivalent), including the SSL/TLS support the connection layer would need to do that, is explicitly deferred to **Version 2**, grouped with the "Generate Connection API" decision above.
- Reasoning: Owner's explicit, direct instruction. Keeps the current build's blast radius bounded to local/dev-stage data only, consistent with how Phase 7 was already verified read-only-only against Portfolio's local dev database rather than its production one.
- Consequences: `PROJECT.md`'s SQL console bullet and its Version 2 note now state this local-only boundary explicitly. Do not add a production/remote database (Neon or otherwise) to `remoteSites`, and do not add SSL/TLS support to the connection layer, until the owner asks for it as part of actual Version 2 work — this is a scope boundary on the current roadmap, not a to-do for [Phase 8] or any other Phase 0–30 phase.

## Decision: SQL Console access/modification policy — protected-table-name write block

- Status: Accepted
- Date: 2026-09-09
- Context: [Phase 5] built a single, global write-safety rule (read-only by default, `confirm:true` unlocks any write) applied identically to every connection. [Phase 7] extended the same console to a real external site's database (Portfolio's local dev Postgres) with no additional protection — a confirmed write against *any* table on *any* connection, including Portfolio's real Django `auth_user` table (which holds real password hashes), would have succeeded. [Phase 8]'s own scope explicitly calls for this to be decided as something more than a single on/off switch, potentially scoped per-database/per-connection.
- Decision: Every table whose name contains "user", "credential", "session", "password", or "secret" (case-insensitive substring match) can be **read but never written to** — not even with `confirm:true` — enforced uniformly across every connection today (all three Phase 1b local engines and every Phase 7 remote site). The policy is modeled per-connection *and* per-table (a specific connection could override the global table rules later, though nothing does yet) and per **both** read and write (only write is actively restricted by this rule; read stays open everywhere, including on protected tables, so the same structure can carry a future read-restricting rule without a rework). Configured as a static code module (`backend/src/sqlConsole/policy.ts`), the same interim pattern as `connections.ts`'s registry — no new database table, no env vars; change it, restart the server.
- Reasoning: Owner's direct choices on all four points (the concrete rule, the per-connection+per-table granularity, modeling both read and write, and static-code-module config) — the pattern-based table-name rule needs no schema awareness or per-site configuration to already cover the real, sensitive tables the console can reach today (most notably Portfolio's `auth_user`/`auth_user_groups`/`auth_user_user_permissions` and `django_session`), and to require no extra setup, generalizing automatically to any future connection with a similarly-named table.
- Consequences: `extractWriteTargetTable` (in `policy.ts`) is a pragmatic, honest-effort regex extraction of the table a write statement targets — same "not a full SQL parser" philosophy `classify.ts` already established for read/write classification — recognizing `INSERT INTO`/`UPDATE`/`DELETE FROM`/`DROP TABLE [IF EXISTS]`/`ALTER TABLE`/`TRUNCATE [TABLE]`. An unrecognized statement shape (notably `CREATE TABLE`, `GRANT`, `REVOKE`) returns `null` and is **never** treated as protected — a known, accepted gap, in the safe direction (under-blocking an edge case, never over-blocking or misidentifying a normal statement). `sqlConsoleRoutes.ts`'s two `POST .../query` handlers were refactored into one shared `gatedQuery` helper so the policy check applies identically to local engines and remote sites from a single choke point, rather than being duplicated (and risking drift) across both routes. Verification deliberately proved the block using Portfolio's real `auth_user` table with **no `confirm` sent at all** (a `403` instead of the previous `409` proves the policy runs independently of the confirm gate) — the full confirmed-write round-trip proof ran only against a throwaway table on AdminPanel's own local test database, never against Portfolio's real data, consistent with [Phase 7]'s own verification discipline.

## Decision: Phase 9 spec documents AdminPanel's actual internal data-api contract, not a remote-site-implemented protocol

- Status: Accepted
- Date: 2026-09-09
- Context: [Phase 9]'s own objective/scope text (see `PHASES.md`) assumed Phase 6 and Phase 7 had already built "the contract" and "the protocol/interface layer exposing [Phase 6's] engine to remote sites" — implying a spec could simply formalize an existing client/server relationship where a remote site's own backend implements the Content Admin API and this panel calls it over HTTP. A review of the actual code before writing the spec (`backend/src/dataManagement/dataRoutes.ts`, `introspection.ts`, `backend/src/auth/authRoutes.ts`) showed that relationship doesn't exist: `/data-api/*` only ever reaches AdminPanel's own three local [Phase 1b] databases via direct SQL introspection, never a remote site's backend over HTTP; `/auth-api/*` is the panel's own single-operator login, not a per-site token/key mechanism; and [Phase 7]'s only real reach into a separate site (Portfolio) was for raw SQL via the SQL Console, not this schema-driven CRUD surface at all. Two named Content Admin API capabilities — search, media listing — were never built.
- Decision: The [Phase 9] spec (`Project Docs/CONTENT-ADMIN-API-SPEC.md`, a new file — see the "Six-file limit exception" note in `CLAUDE.md`) documents the real, working request/response contract exactly as implemented — schema introspection format, field types including the naming-convention special types, CRUD verbs, validation rules, error envelope, and the single-step media upload flow — explicitly scoped as "AdminPanel's own internal backend API for its own locally-reachable databases," with a per-capability Implementation Status marking what's built vs. not (remote-site auth, search, and media listing all marked ❌ not implemented). It does not claim any remote site has implemented, or could yet call, this contract as a two-party HTTP protocol.
- Reasoning: Owner's direct choice when this gap was raised — matches [Phase 9]'s own completion criteria ("accurate to what Phase 6/7 actually built... not aspirational") more literally than inventing a client/server narrative the working code doesn't back up, and follows the same corrective pattern already used for [Phase 7] ("implemented as a direct remote-DB connection, not an API bridge," above).
- Consequences: The original vision — a protocol arbitrary sites implement natively, with the panel as an HTTP client — remains substantially unbuilt beyond [Phase 7]'s narrow raw-SQL bridge to Portfolio. Building a real HTTP-based version of this contract that a separate site's backend actually serves (plus per-site auth/API keys and search) is not assigned to any phase in the locked Phase 0–30 roadmap and needs explicit owner direction to add. Read `PROJECT.md`'s Content Admin API description against this reality, not as already delivered.

## Decision: Secrets storage — encrypted server-side store in adminpanel_app Postgres

- Status: Accepted
- Date: 2026-09-09
- Context: [Phase 10] left the secrets-storage mechanism explicitly undecided ("env vars, an encrypted server-side store, a secrets manager, etc... not pre-committed"), with two hard constraints: reachable from the backend server, and API keys vs. DB credentials kept as genuinely separate categories. Before this, every managed-site secret (the three [Phase 1b] local target-DB credentials, [Phase 7]'s `REMOTE_SITE_PORTFOLIO_*` credentials) lived in plaintext in `.env.local`, read via scattered `process.env.*` calls in `backend/src/sqlConsole/connections.ts` at module-load time — repeatedly flagged throughout `PHASES.md`/`ARCHITECTURE.md` as "interim placement only."
- Decision: A new `secrets` table in the existing `adminpanel_app` Postgres database (the same DB already holding `users`/`session`), added to `ensureSchema()` in `backend/src/db/schema.ts`. Each row holds one logical credential set (e.g. `db-credential/target-postgres` holds the full `{host,port,database,user,password}` object as one JSON blob), encrypted at rest with AES-256-GCM via Node's built-in `crypto` module (`backend/src/secrets/crypto.ts`) — no new npm dependency. A `category` column (`'db-credential' | 'api-key'`, enforced by a DB-level `CHECK` constraint) keeps the two secret categories genuinely separate, not just a TS-level convention. A new `backend/src/secrets/store.ts` exposes `setSecret`/`getSecret`/`requireSecret` as the only way the rest of the backend touches secrets. One new bootstrap env var, `SECRETS_MASTER_KEY` (32 random bytes, hex-encoded), holds the encryption key — the one remaining plaintext secret, same bootstrapping pattern as `SESSION_SECRET`.
- Reasoning: Owner's direct choice between this and formalizing env vars as the mechanism. An encrypted DB-backed store makes secrets addable/rotatable via a direct DB write (no `.env.local` edit + redeploy), reuses the already-established `adminpanel_app` persistence pattern rather than introducing new infrastructure, and needs no new dependency (`node:crypto` natively supports AES-256-GCM) — consistent with this project's standing anti-overengineering pattern (e.g. the Tauri rejection, choosing `bcryptjs` over native `bcrypt`). This is a real storage subsystem, not a "did we get the DB details," which was the more literal reading of the phase's own name and completion criteria than continuing to read `process.env` directly.
- Consequences: `backend/src/sqlConsole/connections.ts` changed from synchronous, module-load-time pool construction to an async `initConnections()`, awaited in `index.ts`'s `bootstrap()` after `ensureSchema()` and before `app.listen()` — the same ordering pattern already used for schema setup. The owner also chose (a second, related decision) to actually migrate the real, working `TARGET_*`/`REMOTE_SITE_PORTFOLIO_*` credentials into the new store via a one-off `backend/src/scripts/migrateSecrets.ts` (`npm run migrate-secrets`, following `seedOwner.ts`'s exact precedent), rather than only proving the mechanism with a throwaway test secret — and then removed those retired vars from `.env.local` for real. Verified end-to-end post-migration: all three local engines and the Portfolio remote connection still return real query results; [Phase 8]'s write-safety/protected-table policy checks (`409`/`403`) still fire identically, since `policy.ts`/`gatedQuery` never depended on how the pool was constructed. One accepted, flagged side effect: `backend/src/testRoutes.ts` (Phase 3's throwaway diagnostic routes, untouched by this phase, deleted whole in [Phase 11]) now fails, since it reads `TARGET_*` directly and independently of `connections.ts`. `ADMINPANEL_DB_*` and `SESSION_SECRET` remain out of scope, per the existing "Login mechanics" decision's carve-out — they're the panel's own runtime config, not a managed-site secret. The "per-site API keys/tokens for the Content Admin API" category has zero real data today (no CAA token scheme exists — see the Phase 9 entry above) but is modeled and proven via the `category` column and a throwaway round-trip test, ready for when a real API-key scheme is built.

## Decision: Test framework choice — Vitest for unit/component tests, Playwright API-testing mode for e2e

- Status: Accepted
- Date: 2026-09-09
- Context: [Phase 11] needed the project's first real, permanent test tooling — every phase through Phase 10 was verified only via ad-hoc `curl`/`psql` commands recorded in `PHASES.md`'s status notes, never an automated, re-runnable suite. The owner asked for both Playwright component tests and e2e tests; I flagged a real mismatch first — Phase 11 is backend-only (no UI/React exists until Phase 12+), and Playwright's Component Testing feature specifically mounts UI framework components in a browser, so there's nothing like that to test yet.
- Decision: **"Component tests" = Vitest**, co-located `*.test.ts` files next to the modules they test, covering pure-function backend pieces in isolation: `secrets/crypto.ts` (AES-256-GCM round-trip, tamper detection), `sqlConsole/classify.ts` (write/multi-statement detection), `sqlConsole/policy.ts` (protected-table matching, write-target extraction), `dataManagement/fieldTypes.ts` (native-type mapping, special-type tagging), `dataManagement/validation.ts` (coercion/validation rules). **"E2E" = Playwright Test's API-testing mode** (the `request` fixture, no browser launched), in a new `backend/e2e/` directory — real HTTP flows against the actually-running server (auth lifecycle, per-engine SQL console + data-management CRUD via a create/drop fixture-table helper, write-safety/policy gates, and read-only checks against the real Portfolio remote connection), replacing the ad-hoc curl-based verification used in every prior phase. `playwright.config.ts`'s `webServer` block builds and starts the real backend (`npm run build && npm start`) and waits on `GET /health` before tests run; `workers: 1`/`fullyParallel: false` since specs share the same real local databases and the real Portfolio connection.
- Reasoning: Owner's direct choice. Vitest needs effectively no configuration for this NodeNext/ESM/strict-TS backend (native `.js`-import-resolves-to-`.ts` support). Playwright's `request` fixture is a first-class, purpose-built replacement for the existing curl-based verification discipline — same idea (real HTTP calls against a real running server), now permanent and re-runnable, without needing a browser or any UI to exist.
- Consequences: New devDependencies `vitest@5.0.0`, `@playwright/test@1.63.0`; new scripts `test:unit`/`test:e2e`/`test`/`typecheck` (a new `tsconfig.test.json`, `rootDir: "."`, covers `e2e/` and the two config files, which sit outside `src/` and so aren't covered by the production `tsc` build). `backend/src/scripts/seedOwner.ts`'s upsert logic was extracted into a small exported `upsertUser()` (`backend/src/scripts/upsertUser.ts`) so Playwright's `globalSetup` could seed a **separate, dedicated e2e test account** by calling it directly, rather than shelling out or touching the real owner account — no behavior change to the `seed:owner` CLI itself. **One real, pre-existing gap in [Phase 8]'s policy layer was surfaced while writing these tests, not fixed by them** (this phase is tests-only): `sqlConsole/policy.ts`'s `extractWriteTargetTable` has no pattern for `CREATE TABLE`, so a protected-pattern name in a `CREATE TABLE` statement is *not* policy-blocked (`isWriteBlockedByPolicy` returns `false`) — it still requires `confirm:true` like any other write, it's just invisible to the policy layer. Locked in as an explicit test case (`policy.test.ts`) documenting the gap rather than hiding it, left for a future Phase 8 tightening pass if the owner wants one. All 125 unit tests and all 21 e2e tests pass; `tsc --noEmit` (production build) and the new `typecheck` script are both clean.

## Open decisions (not yet made)

Carried forward from the original design record — do not treat any of these as settled:

- **Backend server framework/runtime**: Node.js is implied (to reuse `pg`/`mysql2`/`better-sqlite3`), but no specific framework (Express, Fastify, etc.) has been chosen for the browser-based backend — see "Browser-based web app" decision above. To be decided in [Phase 2] "Backend Server (Node.js)," see `PHASES.md`.
- ~~**Secrets storage (browser-based)**: `safeStorage` no longer applies now that the panel isn't Electron — how API keys/tokens and DB credentials get stored server-side.~~ **Resolved 2026-09-09** — see the "Secrets storage — encrypted server-side store in adminpanel_app Postgres" decision above ([Phase 10]).
- ~~**Login mechanics**: session vs. token auth, password storage/hashing, and where the single owner account's credentials live.~~ **Resolved 2026-09-09** — see the "Login mechanics" decision above.
- **Versioning/history**: whether records get git-like diff/version history in the editor pane. **Not assigned to any phase** in the locked Phase 0–30 roadmap (see `PHASES.md`) — remains a genuinely open idea with no scheduled home; do not assume it will happen unless the owner adds a phase for it.
- ~~**SQL console write safety**: read-only-by-default vs. unrestricted.~~ **Resolved 2026-09-09** — see the "SQL console write-safety default" decision above ([Phase 5]). ~~The granular, per-connection/per-table policy on top of that basic default.~~ **Resolved 2026-09-09** — see the "SQL Console access/modification policy — protected-table-name write block" decision above ([Phase 8]). The real UI for either is still separately [Phase 18] (static) and [Phase 23] (wired up).
