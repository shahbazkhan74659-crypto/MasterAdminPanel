# Development Phases

**🔒 LOCKED (2026-09-08).** The full roadmap (Phase 0 through Phase 31) is complete and set by the project owner (Admin). This file is done — every phase from design/stack lock through live-deployment verification is defined, with objectives, scope, and completion criteria.

**Amended 2026-09-11 (Admin's direct command):** [Phase 23] "Staging Layer" was inserted between the then-Phase 22 (Split Panes) and the then-Phase 23 (Wiring), cascading every phase from the old Phase 23 onward up by one (old 23–30 → new [Phase 24]–[Phase 31]). The roadmap is now Phase 0 through Phase 31. See `DECISIONS.md`'s "Staging layer for data-management edits, deployed to live on demand" entry.

**Standing rule: Claude must not add, remove, reorder, renumber, or otherwise edit any phase in this file without an explicit, direct command from Admin in that session** — not inferred from other work, not "this seems related," not because another doc changed. If a change elsewhere in the project seems to imply a phase should be added or updated, say so and ask — do not edit this file on that inference alone. This is stricter than `CLAUDE.md` rule 3's general "don't invent phases" guidance: even a clearly-in-scope, well-reasoned addition still requires Admin to say it directly.

Admin can still update this file anytime, as many times as wanted — "locked" means Claude doesn't touch it unprompted, not that the roadmap itself is frozen forever.

**Standing convention, Phase 4 onward (owner's direction, 2026-09-08):** every phase after Phase 3 (the temporary blank test page) builds backend functionality only. Frontend work is limited to whatever minimal additions Phase 3's test page needs to exercise that phase's backend piece — not real UI work. Do not build any part of the real React/Vite/VS Code-metaphor frontend (`ARCHITECTURE.md`) under a numbered phase unless the owner explicitly says a phase is about the real frontend.

## Phase 0 — Pre-Development (Design & Stack Decision)

### Objective
Define the product concept, UI/interaction model, core protocol architecture, and technology stack, and validate the design against a working visual prototype — before writing any production code.

### Scope

**0a. Product & Design Definition**
- Core concept defined: a personal, reusable admin panel driven by a shared "Content Admin API" protocol, rather than a one-off UI per site (see `PROJECT.md`).
- VS Code-metaphor UI mapped out (activity bar, explorer tree, editor tabs, command palette, status bar, split panes) (see `PROJECT.md`, `ARCHITECTURE.md`).
- Schema-driven forms identified as the load-bearing design decision — the API describes its own schema so one UI can render editors for arbitrary content types (see `DECISIONS.md`).
- SQL console scoped as a deliberate, explicit exception to the schema-driven CRUD path — connects directly to the database rather than through the Content Admin API (see `DECISIONS.md`).
- Several product decisions explicitly left open rather than guessed at: spec-first vs. site-first build order, secrets-storage scoping detail, versioning/history in the editor, SQL console write-safety default (see `DECISIONS.md`, `TASKS.md`).

**Status: Complete**, as evidenced by the locked design record in `DECISIONS.md` (originally `docs/DESIGN.md`, migrated).

**0b. Technology Stack Decision**
- Stack originally chosen: Electron + React/TypeScript (Vite) + Monaco + Zustand + native per-engine DB drivers (`pg`/`mysql2`/`better-sqlite3`) + Electron `safeStorage` + `electron-store` + `electron-builder` (see `DECISIONS.md`). Tauri evaluated and explicitly rejected.
- **Revised 2026-09-08**: owner decided to run the panel as a browser-based web app instead of an Electron desktop app — Electron/Tauri, `safeStorage`, `electron-store`, and `electron-builder` are all superseded. React/TypeScript/Vite/Monaco/Zustand carry over unchanged; DB drivers move into a new backend server component; hosting is Render's free tier + UptimeRobot; a single-user login gate is now required (a desktop app didn't need one). See `DECISIONS.md` for the full decision history and what's still open (backend framework, secrets storage, login mechanics).

**Status: Complete**, as a locked direction — see `DECISIONS.md` (originally `docs/tech.md`, migrated, then revised 2026-09-08). Several implementation-level specifics under the new browser-based direction remain open — see `TASKS.md`.

**0c. Visual Prototyping**
- A design canvas prototype (`design/master-admin-panel.html`, plus its source `design/Main.dc.html`/`design/canvas.json`) built and published, demonstrating the VS Code-metaphor UI as a mock frontend only — no real API calls, no real DB connection.

**Status: Complete.** The existing design canvas is sufficient as a design/validation reference — no further prototyping work is scheduled before the owner picks a next real-implementation step (see `TASKS.md`'s Next section for the two candidate directions under consideration).

### Completion Criteria
- 0a: Complete — product/UI/protocol design locked (see `DECISIONS.md`).
- 0b: Complete — stack locked (see `DECISIONS.md`).
- 0c: Complete — `design/master-admin-panel.html` exists and serves as the interactive design reference.

**Phase 0 overall status: Complete.**

## Phase 1 — Local Database Setup

### Objective
Set up every local database needed before any backend server code is written against them: the panel's own storage (Phase 1a) and the local target-database engines it will manage/query (Phase 1b). **Split into 1a/1b on 2026-09-08 (Admin's direct command)** — the original single Phase 1 only covered the target-engine setup now scoped to 1b; 1a is new and addresses a real gap Phase 4 had already flagged as open ("where does the panel's own account data get persisted locally").

**1a. Local Postgres for the App Itself**

#### Objective
Set up a local PostgreSQL instance dedicated to AdminPanel's own data during local development — distinct from any of Phase 1b's target/managed databases. This is the local-dev counterpart to Phase 29's production Neon Postgres, and gives Phase 4 (login) and Phase 10 (secrets storage) a concrete local place to persist the panel's own account/credential data instead of leaving it unresolved.

#### Scope
To be defined in further detail as the owner directs. Implied by the phase name: install/provision a local PostgreSQL instance for the app's own data only, create whatever database/role/credentials the backend will use for it during development, and confirm it's reachable — kept separate from Phase 1b's Postgres instance (which represents a *managed target site*, not the panel itself).

#### Completion Criteria
A local Postgres instance dedicated to the app's own data is running and reachable (e.g. via `psql`), with a dedicated database/credentials for this project's development use — no application code needs to exist yet for this phase to be complete.

**Status: Complete (2026-09-08).** Uses the already-installed local PostgreSQL 18 server (native Windows service, shared across the owner's other local projects) — no new install needed. Provisioned a dedicated, isolated role and database: role `adminpanel_app` (LOGIN), database `adminpanel_app` owned by that role. Verified reachable via `psql` connecting *as* `adminpanel_app` (not the `postgres` superuser) — `SELECT 1;` and `\conninfo` both succeeded against `localhost:5432`. Connection details recorded in a local, gitignored `.env.local` at the repo root (interim placement only — real secrets storage mechanism is still Phase 10's job).

**1b. All Database Engine Types Setup (Local Target/Managed Databases)**

#### Objective
Get all three of the panel's planned target-database engines installed, configured, and ready to use for development locally — **Postgres, MySQL, and SQLite** (per `DECISIONS.md`'s stack decision) — not just Postgres. Reasoning: the panel is meant to manage/query whichever engine a given target site actually runs on (e.g. Portfolio → Postgres, TS Library → MySQL), so all three need to be available to develop and test against from the start, not added one at a time later. These represent the sites/databases the panel *manages* (via the SQL Console and Content Admin API) — not the panel's own storage, which is Phase 1a's job.

#### Scope
To be defined in further detail as the owner directs. Implied by the phase name: install/provision a local PostgreSQL instance, a local MySQL instance, and a local SQLite database file, create whatever database/role/credentials the panel's backend will use for each during development, and confirm all three are reachable — before any backend server code is written against them (backend framework choice is still open, see `TASKS.md`).

#### Completion Criteria
All three local target-database engines (Postgres, MySQL, SQLite) are running/available and reachable, each with a dedicated database/credentials for this project's development use, confirmed via a real connection to each (e.g. `psql`, a MySQL client, and opening the SQLite file) — no application code needs to exist yet for this phase to be complete.

**Status: Complete (2026-09-08).** Postgres 18 and MySQL 8.0 were already installed and running as native Windows services (`postgresql-x64-18`, `MySQL80`) — no new installs needed. Provisioned isolated, dedicated dev credentials for each engine, distinct from Phase 1a's `adminpanel_app`: Postgres role/database `adminpanel_target_pg`; MySQL user/database `adminpanel_target_mysql`; a real SQLite file at `local-data/adminpanel_target.sqlite` (new gitignored `local-data/` folder). Each engine verified with a full read/write round-trip (`CREATE TABLE` → `INSERT` → `SELECT` → `DROP TABLE`), not just a bare connection check. Connection details recorded in the same local, gitignored `.env.local` as Phase 1a, under a `TARGET_*` prefix to keep them distinct (interim placement only — real secrets storage is still Phase 10's job).

### Completion Criteria
Both Phase 1a and Phase 1b are complete — see each sub-phase's own completion criteria above.

**Phase 1 overall status: Complete (2026-09-08).**

## Phase 2 — Backend Server (Node.js)

### Objective
Stand up the actual backend server that every later phase depends on — the Node.js process that will hold the native per-engine DB drivers (`pg`/`mysql2`/`better-sqlite3`) and, later, the SQL console and Content Admin API logic, replacing the Electron main process from the superseded desktop-app plan (see `DECISIONS.md`).

### Scope
To be defined in further detail as the owner directs, including the specific framework — **Express, Fastify, or otherwise — to be decided as part of this phase**, not pre-committed. Implied by the phase name: a minimal, running Node.js server (chosen framework, project scaffold, a basic health-check/route) that Phase 3's test page can call, and that Phase 4 (login), Phase 5 (SQL console), Phase 6 (Data Management System), and Phase 7 (Content Admin API) will build their real logic into. This is the "backend server framework/runtime choice" item that was previously an open item in `TASKS.md`/`DECISIONS.md` — resolving it is this phase's job.

### Completion Criteria
A real Node.js backend server runs locally, using whichever framework gets chosen during this phase, and responds to at least one real HTTP request — confirming the server itself works before any database, SQL console, or Content Admin API logic is built into it.

**Status: Complete (2026-09-08).** Framework decided: **Express**, written in **TypeScript** (owner's direct choice, matching the frontend's language). Scaffolded a new `backend/` project (`package.json`, `tsconfig.json`, `src/index.ts`) with a single `GET /health` route returning `{ ok: true }`, listening on `process.env.PORT` (default `3001`) — deliberately nothing else yet, no DB/SQL-console/Content-Admin-API logic, per this phase's own scope. Verified with a real HTTP request (`curl http://localhost:3001/health` → `200 {"ok":true}`) and a clean `tsc --noEmit` type-check, then the dev server was stopped again.

## Phase 3 — Temporary Blank Test Page for Backend/Database Engine/Flow Testing

### Objective
Build a temporary, minimal ("white blank") `.html` test page purely to exercise and verify the backend — hitting real endpoints on the Phase 2 backend server, testing each of the three database engines' connection/query flow (Postgres, MySQL, and SQLite, all set up in Phase 1b), and validating the overall request/response flow end-to-end — before any real frontend (React/Vite, VS Code-metaphor UI) is built.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name: a disposable, unstyled static HTML page (no design system, no framework) with just enough script/markup to trigger backend calls (against the Phase 2 server) and display raw results, used as a throwaway diagnostic harness rather than a step toward the real UI. Explicitly not the real frontend — see `DECISIONS.md`/`ARCHITECTURE.md`'s planned React/TypeScript/Vite frontend, which this does not replace or evolve into.

### Completion Criteria
The blank test page can trigger and show the result of at least one real backend call per database engine (Postgres, MySQL, SQLite) against the Phase 2 server, confirming the backend/DB flow works end-to-end from a real HTTP client (the page itself) rather than only from server-side scripts/tests.

**Status: Complete (2026-09-08).** Added `pg`/`mysql2`/`better-sqlite3`/`dotenv` to `backend/`; three throwaway endpoints in `backend/src/testRoutes.ts` (mounted at `/test-api/*`, kept in its own file for a clean Phase 11 deletion) each open a real connection to the Phase 1b target databases and run a real query. A plain static page, `backend/public/test.html`, has one button per engine that fetches its endpoint and dumps the raw JSON. Verified by actually clicking all three buttons in a real browser — Postgres, MySQL, and SQLite each returned real data (server time/current DB, or SQLite's version string). **Bug caught and fixed along the way:** `.env.local`'s `TARGET_MYSQL_PASSWORD` (and `ADMINPANEL_DB_PASSWORD`/`TARGET_PG_PASSWORD` proactively too) contained an unquoted `#`, which `dotenv` treats as a comment marker — silently truncating the password and breaking MySQL auth. Fixed by quoting all three password values in `.env.local`; Postgres/SQLite happened not to contain `#` so weren't affected, but are now quoted defensively too.

## Phase 4 — Login Auth Engine/Flow

### Objective
Build the single-user login flow's backend (per `DECISIONS.md`'s "Single-user login for now" decision) — the authentication engine that will gate the panel once a real frontend exists, built into the Phase 2 backend server.

### Scope
To be defined in further detail as the owner directs, including the still-open login mechanics (see `DECISIONS.md`/`TASKS.md`): session vs. token auth, password storage/hashing, and where the single owner account's credentials live (this phase should resolve where that account is persisted — Phase 1a's local Postgres for the app itself, or another mechanism — not assume one). Implied by the phase name: real login/logout endpoints on the Phase 2 backend server, with actual credential verification — not a stub. **Per the standing convention above, this is backend work** — a real login *page/form* is real-frontend work and does not happen in this phase; only whatever minimal form Phase 3's blank test page needs to exercise the login endpoints (submit credentials, show success/failure, confirm a session/token is issued).

### Completion Criteria
A real login endpoint on the Phase 2 backend server accepts the single owner account's credentials, correctly accepts valid ones and rejects invalid ones, and issues whatever session/token mechanism gets decided during this phase — exercised via Phase 3's blank test page, not a real UI. Login mechanics (session vs. token, password hashing, credential storage location) are explicitly decided as part of this phase, not left implicit.

**Status: Complete (2026-09-09).** Login mechanics resolved: **server-side session via an httpOnly cookie** (not a token), **bcrypt-format password hashing** (via the `bcryptjs` package — same `$2a$`/`$2b$` hash format as native `bcrypt`, chosen to avoid Windows native-build friction), and the owner's credentials live in a new `users` table in **Phase 1a's `adminpanel_app` Postgres** database (auto-created at server startup via `CREATE TABLE IF NOT EXISTS`, no migration framework). Sessions are persisted server-side in the same database via `connect-pg-simple` (a `session` table, also auto-created) rather than in-memory, so logins survive both `tsx watch`'s dev-time restarts and Render's eventual free-tier cold starts. New backend pieces: `backend/src/db/pool.ts` (shared `pg.Pool` scoped to `adminpanel_app`), `backend/src/db/schema.ts` (`ensureSchema()`), `backend/src/auth/authRoutes.ts` (`POST /auth-api/login`, `POST /auth-api/logout`, `GET /auth-api/me`), and `backend/src/scripts/seedOwner.ts` (a one-off `npm run seed:owner -- <username> <password>` CLI script — the only way the single owner account gets created or its password reset; no signup flow exists or is planned). `backend/src/index.ts` gained `express.json()` and `express-session` middleware; no existing route (`/test-api/*`) was gated behind auth — that wiring is explicitly deferred to Phase 24, per the roadmap. Verified end-to-end: wrong password → 401 with no cookie; correct password → 200 + `HttpOnly; SameSite=Lax` cookie; `/auth-api/me` reflects session state; logout destroys the session server-side (confirmed via the `session` table row disappearing, not just the client cookie clearing); login survives a full dev-server process restart with no re-seeding; `/test-api/postgres` still works with no session at all; `tsc --noEmit` and `npm run build` both clean.

## Phase 5 — SQL Console

### Objective
Build the SQL query console — the dedicated, direct-to-database query tool that is a deliberate, explicit exception to the schema-driven Content Admin API path (see `DECISIONS.md`).

### Scope
To be defined in further detail as the owner directs. Implied by `PROJECT.md`/`DECISIONS.md`'s existing design record: the console's real logic (clickable table listing, running a query, returning a real result set with columns/rows/row count/elapsed time), built into the Phase 2 backend server using its native per-engine drivers (`pg`/`mysql2`/`better-sqlite3`, per `ARCHITECTURE.md`) rather than through the Content Admin API — across all three engines set up in Phase 1b, not just Postgres. Builds directly on Phase 1b (real Postgres/MySQL/SQLite instances to query), Phase 2 (the backend server it runs in), Phase 3 (the backend/DB-engine flow already proven end-to-end for all three), and Phase 4 (the login gate this console should sit behind once a real frontend exists). Still open per `DECISIONS.md`/`TASKS.md` and needing a decision during this phase: whether the console defaults to read-only with an explicit confirm step before non-`SELECT` statements, or stays unrestricted. **Per the standing convention above, this is backend work** — the actual VS Code-styled console UI (table chips, query input panel, result grid) is real-frontend work and does not happen in this phase; only whatever minimal additions Phase 3's blank test page needs to exercise the console's backend endpoints.

### Completion Criteria
A working SQL console **backend** can connect to each of the three local Phase 1b databases (Postgres, MySQL, SQLite), list tables, accept and run a query, and return a real result set (columns, rows, row count, elapsed time) — exercised via Phase 3's blank test page, not a real UI — with the write-safety posture (read-only-by-default vs. unrestricted) explicitly decided rather than left implicit.

**Status: Complete (2026-09-09).** Write-safety posture resolved: **read-only by default, `confirm: true` required for any non-`SELECT` statement** (owner's direct choice). This is the basic global default only — a more granular, configurable policy (per-database/per-connection rules) remains [Phase 8]'s separate, later job; `DECISIONS.md`'s open-decisions list and `CLAUDE.md`'s notes previously attributed the whole question to Phase 8 alone, which has been corrected to reflect this split. New backend module `backend/src/sqlConsole/` (four files): `connections.ts` (one persistent connection per engine — a `pg.Pool` and an `mysql2` pool against the Phase 1b `TARGET_*` databases, plus a single shared read-write `better-sqlite3` handle), `classify.ts` (a pragmatic, honest-effort — not a full SQL parser — write-statement classifier and a single-statement-only guard), `adapters.ts` (normalizes each driver's very different result shape into one `{ columns, rows, rowCount, elapsedMs, command? }` shape), and `sqlConsoleRoutes.ts` (`GET /sql-console-api/:engine/tables`, `POST /sql-console-api/:engine/query`, mounted in `index.ts`). Unlike `testRoutes.ts`'s "always 200" style, this router uses meaningful status codes (400 bad input/unknown engine/multiple statements, 409 unconfirmed write, 500 real driver error), matching `authRoutes.ts`'s precedent as the project's "real, permanent code" convention. Not gated behind the Phase 4 login session — that's Phase 24's job. Verified end-to-end via `curl` for all three engines: table listing; a `SELECT` returning real columns/rows/rowCount/elapsedMs; an unconfirmed write rejected with 409; a multi-statement request rejected with 400; and a full `CREATE`/`INSERT`/`SELECT`/`DROP` round trip with `confirm:true` that actually persisted and was verified before cleanup. `/test-api/*` and `/auth-api/*` reconfirmed undisturbed; `tsc --noEmit` and `npm run build` both clean.

## Phase 6 — Data Management System Backend (Direct Typed Edits)

### Objective
Build the backend for the "normal" (non-SQL) data management path: directly changing data by typing new values into fields, rather than writing raw SQL (the Phase 5 SQL Console's path). This is the schema-driven CRUD mechanism `DECISIONS.md` calls "the load-bearing design decision" — list a collection, read its schema, and update/create/delete records by field, typed input in, validated write out. **Renumbered 2026-09-08 (was Phase 8) to sit before Phase 7's Content Admin API, since that phase exposes this engine's CRUD logic to remote sites and needs it to exist first — see the note below.**

### Scope
To be defined in further detail as the owner directs. Per the standing convention, this is backend work — the schema-driven CRUD engine/endpoints (list collections, get a collection's schema, create/read/update/delete a record by field) built into the Phase 2 backend server, exercised via Phase 3's blank test page (typing a new value into a plain input and submitting it, not a real schema-driven form UI). **Confirmed by the owner (2026-09-08): this is the same engine as Phase 7's Content Admin API — this phase builds the actual list/read/update/delete-by-schema logic, which Phase 7's Content Admin API is the protocol/interface exposing to remote sites.** One engine, built across both phases, not two separate mechanisms — this is why this phase now comes first. **Includes Image and Video as real field types in the content-editing logic** (owner's direction, 2026-09-08) — not just the "image picker" mentioned narratively in `DECISIONS.md`'s schema-driven forms decision, but actual backend support: storing/retrieving image and video field values as part of a record's schema and CRUD flow, alongside the existing text/rich-text/relation/date/boolean/array field types. The corresponding frontend picker/upload/editing UI for these two field types is separately scoped to Phase 27 — this phase is backend/data-model only.

### Completion Criteria
A real backend endpoint can list a collection/table's records and schema — including Image and Video field types — and accept a typed field value (for text-like fields) or an image/video value (for those two field types) to update (or create/delete) a record, with the change actually persisted to a real database — exercised via Phase 3's blank test page, not a real schema-driven form UI.

**Status: Complete (2026-09-09).** Built the schema-driven CRUD engine against the same three Phase 1b local databases the SQL console (Phase 5) already targets. Schema is auto-introspected from the database's real columns (native SQL type → a base type: text/integer/float/boolean/date/other); a **naming convention on the column name** tags special types raw SQL can't express — `_richtext`, `_image`, `_video` (all require a `text`-based column), and `_id_relation` (resolves its target collection by trying `<stem>`, `<stem>s`, `<stem>es` against the engine's real table list). An incompatible or unresolvable suffix is ignored (plain field, warning logged), not an error. New module `backend/src/dataManagement/` (`introspection.ts`, `fieldTypes.ts`, `validation.ts`, `crud.ts`, `upload.ts`, `dataRoutes.ts`), mounted at `/data-api`: `GET /:engine/collections`, `GET /:engine/:collection/schema`, `GET/POST /:engine/:collection/records`, `GET/PATCH/DELETE /:engine/:collection/records/:id`. Table/column identifiers are only ever taken from introspection's own output (never raw client strings) before being used in a query — the injection guard for this path. A table with no primary key or a composite one still supports `schema`/`collections` but rejects `:id`-based operations with a clear 400 rather than trying to support it. **Includes real Image/Video file-upload handling** (owner's explicit direction): `POST /:engine/:collection/records/:id/upload/:field` (multer, new dependency) validates the field is actually tagged image/video first, saves the file to a new gitignored `backend/media/<engine>/<collection>/<field>/` (generated filename, restricted extension allowlist, mimetype-checked against the field's type), serves it via a new `/media` static mount, and writes the resulting path into the record in the same request — not a separate upload-then-PATCH step. No write-safety gate here (unlike Phase 5) — this is the validated, schema-driven path by construction. Not gated behind the Phase 4 login session — that's Phase 24's job. **Bug caught and fixed along the way:** `better-sqlite3` rejects binding a JS `boolean` directly ("SQLite3 can only bind numbers, strings, bigints, buffers, and null") — fixed by converting booleans to `0`/`1` for SQLite parameter binding only. Verified end-to-end across all three engines: schema introspection correctly tagged a real richtext/image/video column each; full create/read/update/delete round trips; a real file upload whose bytes matched byte-for-byte on retrieval via `/media`, correctly written into the record; the full negative-case matrix (unknown engine/collection/field, wrong type, missing required field, PK-in-body, non-image/video upload target, wrong mimetype, no-PK table); `/test-api/*`, `/auth-api/*`, `/sql-console-api/*` reconfirmed undisturbed; `tsc --noEmit` and `npm run build` both clean.

## Phase 7 — Content Admin API (a.k.a. "Custom Admin API"; connects a real website's database to the SQL Console)

### Objective
Build a first real implementation of the **Content Admin API** (`PROJECT.md`/`DECISIONS.md`'s originally-designed schema-driven protocol — "Custom Admin API" is the same thing, just different wording, confirmed by the owner) — the protocol/interface layer exposing Phase 6's Data Management System engine to remote sites. This phase's specific, stated capability: it lets a real website's database be connected to the SQL Console built in Phase 5 — i.e. wiring the console up to query a real, external site's database, not just the local Phase 1b databases. **Renumbered 2026-09-08 (was Phase 6) to sit after Phase 6's Data Management System, since this phase exposes that engine's CRUD logic and depends on it existing first.**

### Scope
To be defined in further detail as the owner directs. Note the refinement this phase introduces to the existing SQL console decision (`DECISIONS.md`): the console was designed to bypass the Content Admin API and connect **directly** to a target database. For a real remote website whose database isn't directly network-reachable by the panel, this phase's Content Admin API implementation is the bridging mechanism that still gets the console real SQL access to that site's database — technically routed through the site's own API server, but conceptually still "direct, unrestricted SQL access, bypassing schema validation," the same trust boundary the original SQL console decision described, not the schema-driven CRUD path. See `DECISIONS.md` for where this nuance should get folded into the existing decision record. **Per the standing convention above, this is backend work** — building and deploying the Content Admin API server-side (in the Phase 2 backend server, on top of Phase 6's engine), and the panel's backend logic to talk to it — not the real frontend; verify it through Phase 3's blank test page.

### Completion Criteria
To be defined, but per the standing convention: completion is a working backend integration (panel backend ↔ a real site's Content Admin API ↔ that site's database, exercised via Phase 3's blank test page), not a real UI for it.

**Status: Complete (2026-09-09), narrowed scope.** Built only the SQL-bridging capability Phase 7 explicitly names, not the full originally-designed Content Admin API surface (auth/collections/schema/CRUD/media/search) — that fuller surface stays undone until a later phase actually needs it. **Important correction to this phase's own framing, made mid-implementation on the owner's direct instruction**: the owner ruled out touching any code in the target site's own repository at all (`C:\Portfolio`, the real site used to exercise this phase — no file there was read for anything but its already-gitignored local `.env`, and none was written). That rules out the "site's own API server bridges to its DB" mechanism this phase's objective/scope text above describes — there is no API running inside Portfolio, and none was added. What was actually built instead: the SQL Console's (Phase 5) existing direct-connection model, extended to a second, real site's actual database using that site's own real DB credentials, rather than a database AdminPanel provisioned for itself (Phase 1b). See `DECISIONS.md`'s new "Phase 7 implemented as a direct remote-DB connection, not an API bridge" entry for the full reasoning — this is a real, deliberate correction to this file's own prior framing, not silently glossed over, per `CLAUDE.md` rule 7/9. New: `backend/src/sqlConsole/connections.ts` gained a `remoteSites` registry (Postgres-only, id-keyed, built from `REMOTE_SITE_<ID>_*` env vars — currently just `portfolio`, pointed at Portfolio's real **local dev** Postgres database, not its production Neon database); `adapters.ts`'s `runPostgresQuery`/`listPostgresTables` were generalized to take a `Pool` parameter instead of closing over the fixed local `pgPool` (both existing call sites, including Phase 6's `dataManagement/introspection.ts`, updated to pass `pgPool` explicitly — no behavior change for the existing three local engines); `sqlConsoleRoutes.ts` gained `GET/POST /sql-console-api/sites/:siteId/{tables,query}` alongside the existing `/:engine/...` routes, reusing Phase 5's `isWriteStatement`/`hasMultipleStatements` safety gate and response shape verbatim. Verified end-to-end against Portfolio's real dev database: table listing returned its real schema (`projects_project`, `core_hirerequest`, `core_feedback`, etc.); a real `SELECT` returned real project rows; an unconfirmed `UPDATE` correctly rejected with `409`; an unknown site id rejected with `400`; a multi-statement request rejected with `400`. **Deliberately read-only this phase** — unlike Phase 5's own verification (which ran a full confirmed write round-trip against its own dedicated test databases), no write was ever sent with `confirm:true` against Portfolio's real data. `/test-api/*`, `/auth-api/*`, `/data-api/*`, and the existing `/sql-console-api/:engine/*` routes reconfirmed undisturbed; `tsc --noEmit` and `npm run build` both clean; `git status` inside `C:\Portfolio` confirmed clean throughout — no file there was ever modified.

## Phase 8 — SQL Console Access/Modification Policy (Backend Only)

### Objective
Build the policy layer governing how much of a database the SQL Console is allowed to access and modify — backend/database logic only. **Narrowed 2026-09-08** (was "Fully Functional SQL Console," which originally also included the real UI) — the real UI is now Phase 18's job (static) plus a later wiring-up phase; this phase keeps only the pure backend/policy-enforcement half, per the owner's explicit direction, and drops back under the standard backend-only convention (no more override needed).

### Scope
To be defined in further detail as the owner directs. Implied by the phase name: this is where the long-open "SQL console write safety: read-only-by-default vs. unrestricted" item (`DECISIONS.md`, carried since Phase 0) finally gets decided and enforced at the backend/database layer, potentially scoped further (e.g. per-database or per-connection read/write permissions), not just a single global on/off switch. Do not assume the policy's shape (global toggle vs. per-database/per-operation scoping) until the owner specifies it. Per the standing convention, exercised via Phase 3's blank test page, not a real UI.

### Completion Criteria
A real, enforced policy exists at the backend/database layer restricting what the SQL Console backend (Phase 5) can access/modify — verified against at least one case where the policy actually blocks a disallowed access or modification, exercised via Phase 3's blank test page.

**Status: Complete (2026-09-09).** Concrete rule (owner's direct choice): every table whose name contains "user", "credential", "session", "password", or "secret" (case-insensitive) can be **read but never written to** — not even with `confirm:true` — enforced uniformly across every connection (Phase 1b's three local engines and Phase 7's remote sites alike). Structured per-connection *and* per-table (a specific connection could override these global table rules later; nothing does yet) and models both read and write per table (only write is actively restricted today — read stays open everywhere, including on protected tables, ready for a future read-restricting rule with no rework). Configured as a static code module, same pattern as `connections.ts`'s registry — no new database table, no env vars, matching this phase's own scope note. New `backend/src/sqlConsole/policy.ts`: `isProtectedTable` (substring match against the pattern list) and `extractWriteTargetTable` (a pragmatic, honest-effort regex extraction of the table a write statement targets — `INSERT INTO`/`UPDATE`/`DELETE FROM`/`DROP TABLE`/`ALTER TABLE`/`TRUNCATE`, same "not a full SQL parser" philosophy as Phase 5's `classify.ts`; returns `null`, never "protected," for anything it doesn't recognize, e.g. `CREATE TABLE`, `GRANT`, `REVOKE` — a known, accepted gap). `sqlConsoleRoutes.ts`'s two `POST .../query` handlers (local engines and Phase 7 remote sites) were refactored to share one `gatedQuery` helper — multiple-statement check, then the new policy check (403, regardless of confirm), then the existing confirm-required check (409) — removing real duplication between the two routes rather than adding a second copy of the new check. Verified end-to-end: a `DELETE FROM auth_user` against `sites/portfolio` with **no `confirm` ever sent** returned `403` (proving the policy check runs independently of, and before, the confirm gate — no query ever reached Portfolio's real database); confirmed `UPDATE`/`INSERT` naming a protected-pattern table on a local engine still returned `403` even with `confirm:true`; a normal write on a non-protected table still required and accepted `confirm:true` exactly as Phase 5 (regression-checked via a real `CREATE`/`DROP` round trip on a throwaway table); a real `SELECT` against Portfolio's actual `auth_user` table still succeeded (read intentionally untouched). `GET .../tables`, `/test-api/*`, `/auth-api/*`, `/data-api/*` all reconfirmed undisturbed; `tsc --noEmit` and `npm run build` both clean; `git status` inside `C:\Portfolio` confirmed clean throughout.

## Phase 9 — Content Admin API Spec Documentation

### Objective
Write the formal, standalone specification document for the **Content Admin API** — turning the working contract built in Phase 6 (the CRUD engine) and Phase 7 (the protocol/interface layer) into a written spec that any future site can implement from, without needing to read Phase 7's actual implementation code to reverse-engineer the contract. This is the "originally-designed schema-driven protocol" `PROJECT.md`/`DECISIONS.md` have described narratively since Phase 0 — this phase is the first time it gets written down as an actual spec document.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name: document every part of the contract — auth (get a session/token), list collections, get a collection's schema (field names, types, validation, relations), CRUD on records, media upload/listing, and search — in enough concrete detail (request/response shapes, schema format, error handling) that a new site could implement it correctly without access to Phase 7's own code. This also resolves the long-open "spec-first vs. site-first" question (`DECISIONS.md`, open since Phase 0): building Phase 6/7 first and documenting the spec afterward in Phase 9 means the roadmap ended up **site-first**, with the spec formalized retroactively from a working implementation rather than designed up front.

### Completion Criteria
A written Content Admin API spec document exists, covering every capability listed above, accurate to what Phase 6/7 actually built (not aspirational or not-yet-implemented behavior) — see `CLAUDE.md` rule 9 on documenting actual state.

**Status: Complete (2026-09-09), scope corrected.** Before writing, a review of the actual Phase 6/7 code (`backend/src/dataManagement/`, `backend/src/auth/authRoutes.ts`) surfaced a gap between this phase's own objective text — which assumed Phase 6/7 already built "the contract... any future site can implement from" — and reality: `/data-api/*` only ever reaches AdminPanel's own three local [Phase 1b] databases via direct SQL introspection, never a remote site's backend over HTTP; `/auth-api/*` is the panel's own single-operator login, not a per-site token mechanism; [Phase 7]'s only real reach into a separate site (Portfolio) was for raw SQL, not this CRUD surface; and search/media-listing (both named Content Admin API capabilities) were never built at all. Per `CLAUDE.md` rule 9 ("not aspirational"), the spec was written to match what's actually true rather than the phase's own assumed framing — see `DECISIONS.md`'s new "Phase 9 spec documents AdminPanel's actual internal data-api contract, not a remote-site-implemented protocol" entry for the full correction, the same treatment [Phase 7] got. Produced `Project Docs/CONTENT-ADMIN-API-SPEC.md` (a new file — a deliberate, justified exception to `CLAUDE.md` rule 8's six-file limit, since a protocol reference for future implementers is categorically different from the six internal project-management docs): documents `/auth-api/*` (login/logout/me) and `/data-api/*` (collections, auto-introspected schema incl. the naming-convention special types `_richtext`/`_image`/`_video`/`_id_relation`, full CRUD, single-step media upload) in concrete request/response detail — status codes, error envelope, validation coercion rules per base type, upload constraints — with an explicit per-capability Implementation Status marking remote-site auth, search, and media listing as ❌ not implemented rather than omitting them silently. `ARCHITECTURE.md`'s API Architecture section and `CLAUDE.md`'s Project-Specific Notes were both updated to cross-reference the new file and its scope correction.

## Phase 10 — Secrets Storage Mechanism

### Objective
Decide and build the server-side mechanism for storing secrets — per-site API keys/tokens (for the Content Admin API, Phase 7) and per-site DB credentials (for the SQL Console, Phase 5/8) — now that Electron's `safeStorage` no longer applies (superseded by the 2026-09-08 browser-based pivot, see `DECISIONS.md`).

### Scope
To be defined in further detail as the owner directs, including the specific mechanism — env vars, an encrypted server-side store, a secrets manager, etc. — to be decided as part of this phase, not pre-committed. Implied by the phase name: whatever gets chosen must be reachable from the Phase 2 backend server, and per `DECISIONS.md`'s standing principle, API keys and DB credentials should be kept as two distinct secret categories rather than conflated, regardless of mechanism. Per the standing convention, this is backend work — exercised via Phase 3's blank test page, not a real UI.

### Completion Criteria
A real secret (e.g. a test DB credential or API key) can be stored via the chosen mechanism and correctly retrieved by the backend server for actual use (e.g. opening a DB connection or making an authenticated Content Admin API call), with API keys and DB credentials kept as separate categories rather than a single undifferentiated store.

**Status: Complete (2026-09-09).** Mechanism resolved: an **encrypted server-side store** — a new `secrets` table in the existing `adminpanel_app` Postgres database, values encrypted at rest with AES-256-GCM via Node's built-in `crypto` (no new dependency), decrypted on read (owner's direct choice over formalizing env vars). One row per logical credential set (e.g. `db-credential/target-postgres` holds the full connection object as one encrypted blob), with a DB-level `CHECK` constraint (`category IN ('db-credential', 'api-key')`) genuinely separating the two secret categories. New module `backend/src/secrets/` (`crypto.ts` — `encrypt`/`decrypt`; `store.ts` — `setSecret`/`getSecret`/`requireSecret`), plus a new bootstrap env var `SECRETS_MASTER_KEY` (32 random bytes, hex-encoded) — the one remaining plaintext secret, same pattern as `SESSION_SECRET`. `backend/src/db/schema.ts`'s `ensureSchema()` gained the `secrets` table alongside `users`. `backend/src/sqlConsole/connections.ts` changed from synchronous, module-load-time pool construction to an async `initConnections()`, awaited in `index.ts`'s `bootstrap()` after `ensureSchema()` and before `app.listen()`. **Owner also chose to actually migrate the real, working credentials** (`TARGET_PG/MYSQL/SQLITE_*`, `REMOTE_SITE_PORTFOLIO_*`), not just prove the mechanism with a throwaway secret: a one-off `backend/src/scripts/migrateSecrets.ts` (`npm run migrate-secrets`, following `seedOwner.ts`'s precedent) read them from `.env.local` and wrote them encrypted into the new table; those entries were then removed from `.env.local` for real. Verified end-to-end post-migration and post-retirement (i.e. with the old env vars genuinely gone, not just written to the DB in addition to): all three local engines and the Portfolio remote connection ([Phase 7]) still return real query results; [Phase 8]'s write-safety (`409` unconfirmed write) and protected-table policy (`403`, fired with no `confirm` sent at all) checks still pass identically, since the policy layer never depended on how the pool was built; the `api-key` category was proven via a throwaway round-trip (written, read back, deleted) since no real API key exists yet; AES-GCM tamper detection was proven by flipping a byte of a dummy payload's auth tag and confirming `decrypt` throws. `ADMINPANEL_DB_*`/`SESSION_SECRET` stayed out of scope (the panel's own runtime config, per the existing Login-mechanics decision's carve-out). One accepted, flagged side effect: `testRoutes.ts` (Phase 3's throwaway diagnostic routes, untouched here, deleted whole in [Phase 11]) now fails, since it reads `TARGET_*` directly and independently of `connections.ts`. `tsc --noEmit` clean throughout.

## Phase 11 — End-to-End Backend/Local-DB Testing, and Retiring the Phase 3 Test Page

### Objective
Run a full end-to-end test of everything the backend has grown into so far — the Phase 2 server, Phase 4 login, Phase 5 SQL console, Phase 6 data management engine, Phase 7 Content Admin API, and Phase 10 secrets storage — against the Phase 1a/1b local databases (the app's own Postgres and the Postgres/MySQL/SQLite target engines), confirming it all actually works together as one system rather than only piecewise via individual phase checks. Once that's verified, **delete Phase 3's temporary blank test page** — its job (a throwaway harness to exercise backend pieces before a real frontend existed) is done.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name: exercise real flows spanning multiple backend pieces together (e.g. log in, then run a SQL console query, then a data-management CRUD operation, then a Content Admin API call, each against a real local database) rather than testing each phase's piece in isolation as before. Only after this passes does the Phase 3 test page get removed from the repo — per `CLAUDE.md` rule 7 (No Silent Destruction), removing it should be a deliberate, called-out step in this phase, not an incidental cleanup.

### Completion Criteria
A real end-to-end pass exercises every backend piece built in Phases 2–10 against the Phase 1a/1b local databases with no failures, and afterward the Phase 3 blank test page (and any of its supporting code) is removed from the repository, with `PHASES.md`/`ARCHITECTURE.md` updated to reflect it no longer exists.

**Status: Complete (2026-09-09).** Built the project's first real, permanent test suite (owner's direct choice on tooling — see `DECISIONS.md`'s "Test framework choice" entry): **Vitest** for backend module/"component" tests (125 tests across `secrets/crypto.test.ts`, `sqlConsole/classify.test.ts`, `sqlConsole/policy.test.ts`, `dataManagement/fieldTypes.test.ts`, `dataManagement/validation.test.ts`, all pure-function, no DB needed) and **Playwright Test's API-testing mode** (no browser — just real HTTP calls) for end-to-end flows against the real, running server, in a new `backend/e2e/` directory (21 tests across `auth.spec.ts`, `sql-console.spec.ts`, `data-management.spec.ts`, `policy.spec.ts`, `remote-site.spec.ts`). The e2e suite genuinely exercises Phase 2 (server bootstrap), Phase 4 (login lifecycle via a dedicated e2e test account, never the real owner's), Phase 5/8 (SQL console write-safety and protected-table policy gates), Phase 6 (a full create-schema-CRUD-drop cycle via a fixture-table helper, run against all three real Phase 1b local databases), Phase 7 (real, read-only-only queries against Portfolio's actual remote database), and Phase 10 (implicitly — the server can't even boot without the encrypted secrets store resolving correctly). **A real, pre-existing gap in Phase 8's policy layer was found and documented, not fixed** (this phase is tests-only): `policy.ts`'s `extractWriteTargetTable` has no `CREATE TABLE` pattern, so a protected-name `CREATE TABLE` isn't policy-blocked — locked in as an explicit, commented test case rather than silently left undiscovered; see `DECISIONS.md` for the full note. Once both suites passed clean against the real databases (with fixture tables/rows confirmed left behind nowhere afterward), **`backend/src/testRoutes.ts` and `backend/public/test.html` (plus the now-empty `backend/public/`) were deleted for real**, and `index.ts`'s `testRoutes` import, its `/test-api` mount, and its root `public/` static mount were removed (the `/media` mount, unrelated, was kept) — verified afterward with both suites re-run clean, plus a manual check that `GET /` and `GET /test-api/postgres` now correctly 404 while `/health` still works. `tsc --noEmit` (production build) and a new `typecheck` script (covering `e2e/`, which sits outside `src/`) both clean throughout.

## Phase 12 — Global Background & Login Page (Both Static, Before Any Real Structure Exists)

### Objective
Build the first two real-frontend pieces (React/TypeScript/Vite, per `ARCHITECTURE.md`) as standalone static pieces, before Phase 13's base structure exists: the global background (12a) and the login page (12b). These are the first real-frontend phases in the roadmap — everything from Phase 4 through Phase 11 was backend-only or backend-plus-test-page per the standing convention. **Split into 12a/12b on 2026-09-09 (Admin's direct command)** — the original single Phase 12 only covered the background; 12b is new, giving the login page its own scoped phase, kept explicitly separate from Phase 13's base structure rather than folded into it.

**12a. Global Background Matching the Prototype (Background Only)**

#### Objective
Implement the exact global background from the design canvas prototype (`design/master-admin-panel.html`) — and only the background, not the rest of the VS Code-metaphor UI (activity bar, explorer, tabs, editor pane, command palette, status bar all remain unbuilt).

#### Scope
To be defined in further detail as the owner directs. Implied by the phase name: a real React/Vite project (if it doesn't already exist from earlier phases) rendering just the app shell's global background — color/gradient/texture/theme tokens — matching the prototype pixel-for-pixel, with no other prototype UI element (sidebar, tabs, panels, etc.) built yet. Narrowly scoped on purpose — do not expand this phase into building other UI pieces just because the frontend project now exists.

#### Completion Criteria
The real frontend renders a background visually matching the design canvas prototype's global background exactly, with no other UI chrome present yet.

**Status: Complete (2026-09-09).** Scaffolded the project's first real frontend — `frontend/` (Vite + React + TypeScript, per `ARCHITECTURE.md`'s locked stack), sibling to `backend/` at the repo root. The prototype's actual global background lives in `design/Main.dc.html`'s outermost `.app` container (the `master-admin-panel.html` file at the repo root is the Claude Design Canvas *editor's* own chrome/tooling wrapping that content, not the admin panel's design itself — its `--om-*`/Tailwind tokens belong to the canvas tool, not this project). That background is a flat color, no gradient/texture/image: `background: #1e1e1e`, `color: #cccccc`, `font-family: "Segoe UI", system-ui, -apple-system, sans-serif`, `font-size: 13px`, full viewport height, `overflow: hidden`. Vite's template boilerplate (counter demo, React/Vite logos, default `App.css`) was stripped entirely; `App.tsx` renders a single empty `.app-shell` div carrying exactly those styles via a plain `App.css` (no Tailwind or other CSS framework introduced — matches the prototype's own plain-`<style>` approach, and no CSS-tooling choice was locked in `ARCHITECTURE.md` yet). No sidebar, tabs, panels, or any other prototype UI chrome built. Verified via `tsc --noEmit` (clean) and the running Vite dev server (`npm run dev`, port 5173) — fetched the served `index.html` and the compiled `App.css` module directly to confirm the exact background/text-color/font values reached the browser, then opened it in a real browser for a visual check.

**12b. Login Page (Static, Theme-Matched)**

#### Objective
Build the login page as a standalone static frontend piece — the page a user sees before authenticating, gating the rest of the panel (Phase 4 already built the real backend it will eventually call: `POST /auth-api/login`). The design canvas prototype predates the browser-based pivot that introduced the login requirement (see `DECISIONS.md`) and has no login screen in it — there is nothing to pixel-match here, unlike 12a and Phases 13–22. **Per the owner's direct decision (2026-09-09): the login page's visual design is created fresh as part of this phase's own scope**, using the same VS Code-metaphor visual theme (colors/typography/style tokens) as the rest of the prototype, not a pre-existing mockup.

#### Scope
To be defined in further detail as the owner directs. Implied by the phase name: a real, standalone static login page (username/password fields, a submit control) visually consistent with the prototype's theme — designed fresh within this phase since no prior mockup exists, per the objective above. No real backend wiring yet: Phase 4's `/auth-api/login` endpoint exists but is not called from this page in this phase, matching the "static first, wire later" pattern already used for Phases 15–22. **Explicitly not in scope**: real form submission, session handling, redirect-on-success/failure logic, or mounting inside Phase 13's base global structure — this page stays a standalone piece, kept deliberately separate from Phase 13 rather than integrated into it (unlike Phase 12a's background, which Phase 13 does fold in). **Per the owner's direct addition (2026-09-09): client-side form validation via Zod**, with password rules specifically called out — this is *format* validation only (a schema check on field shape before the still-absent real submission), not the excluded real submission/session logic itself. The same schema is intended to be reused server-side, plus rate limiting on the real login route, once Phase 24 wires this page to the backend — see `DECISIONS.md`.

#### Completion Criteria
A real, static login page exists in the frontend — designed fresh, visually consistent with the prototype's theme, with username/password fields and a submit control — kept as a standalone page separate from Phase 13's base structure, with no backend wiring of any kind.

**Status: Complete (2026-09-09).** Built `frontend/src/LoginPage.tsx`/`LoginPage.css`, kept deliberately unimported by `App.tsx` (Phase 12a's own deliverable, left untouched) — verified by temporarily pointing `main.tsx` at `LoginPage` for a real browser check, then reverting `main.tsx` back to `App` so the mounted app still reflects Phase 12a's committed background-only piece. No router exists yet ([ARCHITECTURE.md] still lists routing as undecided), so this was the chosen way to keep the page real and independently verifiable without inventing routing this phase doesn't call for. Design: a centered card reusing the prototype's exact existing tokens rather than invented ones — `.modal`'s `background:#252526`/`border:#454545`/`border-radius:6px`/`box-shadow` for the card, `.input`'s exact input styling (`#3c3c3c` fill, `#007fd4` focus border), `.btn-primary.enabled`'s exact button styling (`#0e639c` → `#1177bb` hover), and the existing error-red token (`#f48771`, from `.console-error`) for validation messages — on the same `#1e1e1e`/`#cccccc`/Segoe UI app-shell background as Phase 12a. Added `zod` (new dependency, `frontend/`) for client-side validation: username required, password requires 8+ characters with uppercase/lowercase/digit/special-character rules (owner's direct request, "password rules specially") — validated on submit via `safeParse`, inline errors shown per field, still no `fetch`/session/redirect of any kind. Also hardened against browser autofill/autocomplete noise during testing (owner's direct follow-up requests): `autoComplete="off"` on the form and both fields (avoiding `new-password`, which triggers Chrome's own strong-password suggestion UI), `data-lpignore`/`data-1p-ignore`/`data-bwignore`/`data-form-type="other"` to suppress password-manager icons, and the readonly-until-focus trick (fields start `readOnly`, cleared `onFocus`) — the most reliable client-side technique against Chrome's autofill-on-load behavior; noted for the owner that a credential already saved by Chrome for `localhost:5173` from manual testing can still override this and needs clearing at `chrome://settings/passwords` directly, since no HTML/JS can suppress an already-saved-credential suggestion. Placeholder text added to both fields (`e.g. admin` / masked dots), styled with the prototype's muted secondary-text token (`#7d7d7d`). Verified via a clean `tsc --noEmit` throughout and real browser checks. See `DECISIONS.md` for the Zod/rate-limiting forward note for Phase 24.

### Completion Criteria
Both Phase 12a and Phase 12b are complete — see each sub-phase's own completion criteria above.

**Phase 12 overall status: Complete (2026-09-09) — both 12a and 12b done.**

## Phase 13 — Base Global Structure (Integrating the Phase 12a Background)

### Objective
Build the base global structure of the real frontend — the root app-shell/layout scaffold that every later UI piece (activity bar, explorer, tabs, editor pane, command palette, status bar) will mount into — and **integrate Phase 12a's global background into it**. The background was deliberately built standalone/separately in Phase 12a (before any real structure existed); this phase is where it gets folded into the actual base layout rather than staying a lone, unstructured page. Phase 12b's login page is explicitly *not* folded in here — it stays a separate, standalone page (see Phase 12b's scope).

### Scope
To be defined in further detail as the owner directs. Implied by the phase name: a real root layout component (or equivalent) establishing the app's overall structural skeleton, with the Phase 12a background now living inside it as the base layer, not as a separate standalone piece. This phase does not yet build the activity bar/explorer/tabs/editor pane/command palette/status bar themselves — only the structural scaffold they'll eventually occupy — and does not include Phase 12b's login page, which stays standalone. **The base structure must match the design canvas prototype (`design/master-admin-panel.html`) exactly** — same standard this phase holds itself to as Phase 12a held the background to; this is not a loose approximation, but a pixel-accurate structural match to what's already locked in the prototype. **Per the owner's direct clarification (2026-09-09): the titlebar is built for real in this phase**, not left as another empty shell — the prototype's titlebar (app icon/name, breadcrumb slot, Ctrl K button) has no dedicated phase of its own anywhere in Phases 14–22 (those name only activity bar/explorer/tabs/SQL console panel/command palette/status bar/editor pane/split panes), so it belongs to this phase's base structure rather than staying deferred.

### Completion Criteria
A real base global structure exists in the frontend, with the Phase 12a background integrated into it (no longer standalone), providing a scaffold ready for later UI pieces to be added into — without yet building those pieces — and matching the design canvas prototype's structure exactly, not just approximately. Phase 12b's login page remains a separate, standalone page, not part of this structure.

**Status: Complete (2026-09-09).** Built `frontend/src/AppShell.tsx`/`AppShell.css` — the "base.html"-equivalent layout the owner asked for: one shared component holding the app's full structural skeleton, read verbatim off the prototype's real markup/CSS (`design/Main.dc.html`), for every later static UI piece to mount into. `App.tsx` (the page mounted at `/` via `main.tsx`'s existing pathname router) now renders `<AppShell />` instead of the old bare background div; `App.css`'s one rule moved into `AppShell.css` as `.app`, folding Phase 12a's background in as the base layer rather than a standalone page. Structure: `.app` (background/font, unchanged from 12a) → `.titlebar` (built for real — app icon, "Master Admin Panel" name, an empty breadcrumb slot since real path data is [Phase 24]'s job, and a static non-functional "Ctrl K" button since the command palette itself is [Phase 19]'s job) → `.main-row` containing three **empty shells** at their exact prototype dimensions/colors (`.activitybar` 48px/`#333333`, `.sidebar` 250px/`#252526`, `.editor-col` flex:1/`#1e1e1e`) → `.statusbar` (also an empty shell, 24px/`#007acc`) — each empty region carries a one-line comment naming the phase that fills it (15/16/17-18-21-22/20 respectively), per the roadmap's own phase assignments. `frontend/src/App.css` was deleted (superseded by `AppShell.css`); `LoginPage.tsx`/`main.tsx`'s router were untouched. Verified via a clean `tsc --noEmit` and a real browser check of `http://localhost:5173/` against `design/master-admin-panel.html`, plus confirming `/login` still renders unaffected. See `DECISIONS.md` for the titlebar-scope decision.

## Phase 14 — Favicon Icon (Matching the Prototype)

### Objective
Add the real frontend's favicon, matching the design canvas prototype (`design/master-admin-panel.html`) exactly — same "match the prototype precisely" standard Phase 12a (background) and Phase 13 (base structure) already held themselves to.

### Scope
To be defined in further detail as the owner directs, including confirming exactly what icon/asset the prototype actually uses or implies for this (verify against the prototype file itself rather than assuming) before implementing it as the real favicon.

### Completion Criteria
The real frontend serves a favicon that matches the design canvas prototype exactly.

**Status: Complete (2026-09-09).** Owner's direct clarification (referencing a screenshot of the running app): the favicon is the same icon shown in `AppShell.tsx`'s titlebar, top-left — `.app-icon`, two overlapping 16×16 rounded squares (one stroke-only, one filled), already present verbatim in both the real frontend and the prototype (`design/Main.dc.html`'s own `.app-icon` markup/CSS, `color: #3794ff`). Replaced `frontend/public/favicon.svg` (previously an unrelated, never-customized Vite template placeholder) with that exact icon — `currentColor` hardcoded to `#3794ff` since a standalone favicon file has no surrounding CSS to inherit from, transparent background, no tile added (owner's direct choice: a literal 1:1 match to the prototype's icon, nothing extra). `frontend/index.html`'s existing `<link rel="icon">` and `<title>` needed no changes. Verified in a real browser: `frontend/public/favicon.svg` served directly renders identically to the titlebar `.app-icon` seen in the running app.

## Phase 15 — Activity Bar (Static, Matching the Prototype)

### Objective
Build the activity bar UI element (per `PROJECT.md`'s VS Code element mapping — switches between connected sites/workspaces or modules), matching the design canvas prototype exactly, as a **static** piece: visual/structural only, not yet wired to the Phase 2 backend server, Phase 4 login, or any real database.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name and the established "match the prototype exactly" standard (Phase 12a/13/14): the activity bar's visual appearance and structure, built into the Phase 13 base global structure, with static/placeholder content (e.g. hardcoded icons/items) rather than real data. **Explicitly not in scope for this phase**: any backend or database connection — switching sites/modules for real, real site/workspace data, or any live state. That is deferred to a later phase.

### Completion Criteria
The real frontend renders an activity bar visually and structurally matching the design canvas prototype exactly, using static placeholder content — with no backend or database connection of any kind.

**Status: Complete (2026-09-09).** Filled in `frontend/src/AppShell.tsx`'s `.activitybar` shell with two static site-avatar items reused verbatim from the prototype's own default demo data (`design/Main.dc.html`'s `SITES_INITIAL`/`activeSiteId`) rather than inventing new placeholder content — "Northwind Blog" (`NB`, `#3fa7ff`, active, matching the prototype's default `activeSiteId: 'northwind'`) and "Acme Shop" (`AS`, `#c77dff`) — plus the flex spacer and a static, non-functional "Connect new site" `+` button (no click handlers, same precedent as Phase 13's static "Ctrl K" button). Added the missing item-level CSS to `AppShell.css` (`.activity-item`, `.activity-item:hover`/`.active`, the active-state left white indicator bar, `.avatar`, `.avatar-add`, `.activity-spacer`) verbatim from the prototype's stylesheet — the container rule `.activitybar` itself was already correct from Phase 13. No backend/database connection, click handling, or state of any kind. Verified via a clean `tsc --noEmit` and a real browser check of `http://localhost:5174/` against `design/master-admin-panel.html`.

## Phase 16 — Explorer Sidebar (Static, Matching the Prototype)

### Objective
Build the explorer sidebar UI element (per `PROJECT.md`'s VS Code element mapping — the current site's collections/records tree), matching the design canvas prototype exactly, as a **static** piece: visual/structural only, not yet wired to the Phase 2 backend server, Phase 6 data management engine, Phase 7 Content Admin API, or any real database.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name and the established "match the prototype exactly" standard (Phase 12a/13/14/15): the explorer sidebar's visual appearance and structure (tree layout, expand/collapse, icons, etc.), built into the Phase 13 base global structure alongside the Phase 15 activity bar, with static/placeholder tree content rather than real collections/records. **Explicitly not in scope for this phase**: any backend or database connection — real collections, real records, or any live state. That is deferred to a later phase.

### Completion Criteria
The real frontend renders an explorer sidebar visually and structurally matching the design canvas prototype exactly, using static placeholder tree content — with no backend or database connection of any kind.

**Status: Complete (2026-09-09).** Filled in `frontend/src/AppShell.tsx`'s `.sidebar` shell with a header (active site name "Northwind Blog" + a static "Open SQL Console" icon button), a monospace endpoint line, and a tree — all reused verbatim from the prototype's own default demo/render state (`design/Main.dc.html`'s `SITES_INITIAL`/default `expanded`/`activeTabKey`) rather than inventing new placeholder content, matching Phase 15's precedent: **Posts** (count 3, expanded) with its 3 record rows, the first ("Launching Our New Storefront") shown highlighted/active to match the prototype's default open tab; **Pages** (count 2) and **Authors** (count 2), both collapsed with no child rows rendered. Added the missing sidebar CSS to `AppShell.css` verbatim from the prototype's stylesheet (`.sidebar-header`, `.icon-btn`, `.sidebar-endpoint`, `.tree`, `.tree-row`/`.tree-collection`/`.tree-record`, `.chevron-wrap`, `.folder-icon`, `.doc-icon`, `.tree-label`, `.tree-count`, `.tree-children`) plus the global `.mono` utility class the endpoint line needs, which didn't exist yet in the real frontend. `.doc-icon-sm` (same prototype CSS block, but belongs to Phase 17's editor tabs) was deliberately left out to avoid scope creep. No backend/database connection, click handling, or expand/collapse state of any kind. Verified via a clean `tsc --noEmit` and a real browser check of `http://localhost:5174/` against `design/master-admin-panel.html`.

## Phase 17 — Editor Tabs (Static, Matching the Prototype)

### Objective
Build the editor tabs UI element (per `PROJECT.md`'s VS Code element mapping — open records, each with an unsaved-changes dot), matching the design canvas prototype exactly, as a **static** piece: visual/structural only, not yet wired to the Phase 2 backend server, Phase 6 data management engine, Phase 7 Content Admin API, or any real database.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name and the established "match the prototype exactly" standard (Phase 12a/13/14/15/16): the editor tabs' visual appearance and structure (tab strip, active/inactive state, close button, unsaved-changes dot, etc.), built into the Phase 13 base global structure alongside the Phase 15 activity bar and Phase 16 explorer sidebar, with static/placeholder tab content rather than real open records. **Explicitly not in scope for this phase**: any backend or database connection — real open records, real unsaved-state tracking, or any live state. That is deferred to a later phase.

### Completion Criteria
The real frontend renders editor tabs visually and structurally matching the design canvas prototype exactly, using static placeholder tab content — with no backend or database connection of any kind.

**Status: Complete (2026-09-09).** Filled in `frontend/src/AppShell.tsx`'s `.editor-col` shell with a `.tabbar` containing a single static tab, reused verbatim from the prototype's own default render state (`design/Main.dc.html`'s default `tabs` array — one record tab for `nw-posts`/record `p1`, `dirty: false`) rather than inventing new placeholder content, matching Phase 15/16's precedent: the doc icon, the label "Launching Our New Storefront" (the same record Phase 16 already shows as the active row in the explorer tree), no dirty dot (record isn't dirty in the default state), and a static, non-functional `.tab-close` (×) button — the tab itself rendered in its active state (`.tab.active`, the `#3794ff` bottom-border indicator). Added the missing tab-strip CSS to `AppShell.css` verbatim from the prototype's stylesheet (`.tabbar`, `.tab`, `.tab:hover`, `.tab.active`, `.tab-label`, `.dirty-dot`, `.tab.active .dirty-dot`, `.tab-close`, `.tab-close:hover`); `.doc-icon-sm` already existed from Phase 16 and is used here for the first time. No backend/database connection, click-to-switch, working close button, or state of any kind. Verified via a clean `tsc --noEmit` and a real browser check of `http://localhost:5174/` against `design/master-admin-panel.html`, plus confirming `/login` still renders unaffected.

## Phase 18 — SQL Console Panel (Static, Matching the Prototype)

### Objective
Build the SQL Console panel UI (per `PROJECT.md`'s design record — table chips, query input, result grid, styled after VS Code's database-extension UX), matching the design canvas prototype exactly, as a **static** piece: visual/structural only, not yet wired to the Phase 5 SQL console backend or any real database.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name and the established "match the prototype exactly" standard (Phase 12a/13/15/16/17): the SQL Console panel's visual appearance and structure (table chips, query input box, result grid layout), built into the Phase 13 base global structure alongside the other static UI pieces, with static/placeholder content rather than a real query/result flow. **Explicitly not in scope for this phase**: any backend or database connection — running real queries, real result data, or the Phase 8 access/modification policy. That remains deferred to wiring-up work.

### Completion Criteria
The real frontend renders an SQL Console panel visually and structurally matching the design canvas prototype exactly, using static placeholder content — with no backend or database connection of any kind.

**Status: Complete (2026-09-09).** Since the prototype only renders the console panel when a console-type tab is active — but Phase 17 had already committed the tabbar's only tab (the record "Launching Our New Storefront") as active — the owner resolved the resulting design fork directly: add a second tab, "SQL · Northwind Blog" (console icon), and make it the active one for this phase's demo, reusing the prototype's own real "just opened the console" state rather than inventing new placeholder content (same precedent as Phases 15–17). Filled in `frontend/src/AppShell.tsx`'s `.editor-col`: the record tab is now inactive (still in the strip, unchanged otherwise), a new active console tab sits beside it, and a `.console-view` block follows the tabbar — `.console-toolbar` (breadcrumb "Northwind Blog › SQL Console", a static "Run" button with a play-triangle icon and a "Ctrl Enter to run" hint), `.console-tables` (three static table chips using the real lowercase table names — `posts` (3), `pages` (2), `authors` (2), matching Phase 16's record counts), a read-only `.console-input` textarea prefilled with `SELECT * FROM posts LIMIT 50;` (the prototype's real default query), and a `.console-results` area showing the prototype's own "no results yet" placeholder copy verbatim. Added the missing CSS to `AppShell.css` verbatim from the prototype's stylesheet: `.breadcrumb`/`.btn-save`/`.btn-save.enabled` (first used here) plus the full console block (`.console-view`, `.console-toolbar`, `.console-tables`, `.table-chip`, `.console-input`, `.console-results`, `.console-placeholder`, and the result-grid/error rules — `.console-error`, `.console-meta`, `.console-table-wrap`, `.console-table` — not exercised by this static snapshot but included for structural completeness, same precedent as Phase 16). No backend/database connection, click handling (table chips, Run button, tab switching), or the Phase 8 access/policy layer wired in. Verified via a clean `tsc --noEmit` and a real browser check against `design/master-admin-panel.html`'s own console view, plus confirming `/login` still renders unaffected.

**Resolved 2026-09-08 (overlap with Phase 8):** Phase 8 was narrowed to backend/policy-enforcement only (no UI) — this phase (18) is the static UI, and a later, not-yet-defined phase will wire this UI up to the Phase 5 backend and Phase 8's policy. See Phase 8's entry above for the narrowed scope.

## Phase 19 — Command Palette (Static, Matching the Prototype)

### Objective
Build the command palette UI element (per `PROJECT.md`'s VS Code element mapping — quick actions like "New Post," "Publish," "Switch Site," "Duplicate Record"), matching the design canvas prototype exactly, as a **static** piece: visual/structural only, not yet wired to the Phase 2 backend server or any real database, and not yet executing any real action.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name and the established "match the prototype exactly" standard (Phase 12a/13/15/16/17/18): the command palette's visual appearance and structure (open/close trigger, search/filter input, listed commands), built into the Phase 13 base global structure alongside the other static UI pieces, with static/placeholder commands rather than real, executable actions. **Explicitly not in scope for this phase**: any backend or database connection, or any command actually doing something. That remains deferred to wiring-up work.

### Completion Criteria
The real frontend renders a command palette visually and structurally matching the design canvas prototype exactly, using static placeholder commands — with no backend or database connection, and no command performing a real action.

**Status: Complete (2026-09-11).** The prototype's actual command-palette implementation (`design/Main.dc.html`'s `paletteOpen`/`filteredCommands` logic) turned out to differ from the conceptual example commands narratively listed in `PROJECT.md`/`DECISIONS.md` ("New Post," "Publish," "Duplicate Record") — the real command set is `File: Save Record`, `View: Close Editor`, `View: Close All Editors`, `Site: Connect New Site…`, `Site: Toggle Environment (currently <env>)`, one `Site: Switch to <site>` per connected site, and one `SQL: Open Query Console — <site>` per site. Per the established "match the prototype exactly" standard, this phase renders the prototype's real 7-command-template list, expanded against Phase 15's already-committed static site data (Northwind Blog, Acme Shop) and the prototype's default `env: 'Production'`, into 9 concrete static items — not the conceptual examples from `PROJECT.md`. **Owner resolved a second design fork directly (2026-09-11):** since the palette is a full-screen modal overlay only shown when `paletteOpen` is true in the prototype, and this phase has no real open/close logic, the owner chose to render it **statically open by default** (always visible on page load), reusing the prototype's own "just opened" state rather than building something invisible/unverifiable — same precedent as Phase 18's second, always-active console tab. Filled in `frontend/src/AppShell.tsx` with a new `.palette-backdrop`/`.palette` block (search row with the same magnifying-glass icon already used by the existing titlebar "Ctrl K" chip, a placeholder-only readOnly input, and the 9 static `.palette-item` rows) as a top-level sibling between `.main-row` and `.statusbar` — no `onClick` anywhere, matching Phases 15–18's inert-static-element precedent; `autofocus` deliberately omitted since the palette is permanently mounted rather than opened on a real keystroke. Added the full `/* Command palette */` CSS block to `AppShell.css` verbatim from the prototype's stylesheet (including the unreferenced `.palette-empty` rule, for structural completeness — same precedent as Phase 18's unexercised console-error/table CSS). The prototype's `.app { position: relative }` was deliberately not added to the real `AppShell.css`, since `.palette-backdrop` uses `position: fixed; inset: 0` (viewport-relative regardless of ancestor positioning) — a no-op change to an already-completed, verified phase's rule. The existing titlebar "Ctrl K" `.btn-chip` (built Phase 13) was left untouched. Verified via a clean `tsc --noEmit` and a real browser check against `design/master-admin-panel.html`'s palette view — backdrop dimming, palette position/size/styling, search icon and placeholder, all 9 command labels (including the real `…`/`—` characters), and the `:hover` state all confirmed correct, with the rest of the app shell (titlebar, activity bar, sidebar, tabs, SQL console view) still fully visible and unchanged underneath.

## Phase 20 — Status Bar (Static, Matching the Prototype)

### Objective
Build the status bar UI element (per `PROJECT.md`'s VS Code element mapping — connected site, environment, save/sync state), matching the design canvas prototype exactly, as a **static** piece: visual/structural only, not yet wired to the Phase 2 backend server, Phase 4 login, or any real database.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name and the established "match the prototype exactly" standard (Phase 12a/13/15/16/17/18/19): the status bar's visual appearance and structure, built into the Phase 13 base global structure alongside the other static UI pieces, with static/placeholder content rather than real connection/environment/save-state data. **Explicitly not in scope for this phase**: any backend or database connection — real connected-site info, real save/sync state, or any live state. That remains deferred to wiring-up work.

### Completion Criteria
The real frontend renders a status bar visually and structurally matching the design canvas prototype exactly, using static placeholder content — with no backend or database connection of any kind.

**Status: Complete (2026-09-11).** The prototype's actual status bar (`design/Main.dc.html`) has exactly four segments driven by real state: the active site name, an environment toggle pill, a static "Content Admin API v1" label, and a green-dot "Connected" indicator (unconditional in the prototype, not gated by any live-connection check) — plus a fifth, conditionally-rendered "N unsaved" segment that only appears when `hasUnsaved` is true. Since the prototype's own default state has the one seeded tab non-dirty (`hasUnsaved: false`), that segment is not rendered in the prototype's default view either — per the established "reuse the prototype's real default render state" standard (same precedent as Phase 17's non-dirty seeded tab), the "N unsaved" segment was omitted entirely rather than fabricating a count. Filled in `frontend/src/AppShell.tsx`'s `.statusbar` shell with `.status-left` ("Northwind Blog" + a "Production" environment pill, `status-env prod` class kept for markup fidelity even though no CSS rule currently targets `.prod`, only `.staging`) and `.status-right` ("Content Admin API v1" + the green-dot "Connected" indicator) — no click handler on the environment pill, matching Phases 15–19's inert-static-element precedent. **A real discrepancy was found and fixed while filling in the shell**: the existing `.statusbar` CSS rule (built Phase 13) was missing `color: #ffffff`, present in the prototype's own rule — added, since the new `.status-item` text has no color of its own and would otherwise inherit `.app`'s `#cccccc`. Added the five new `.status-*` rules to `AppShell.css` verbatim from the prototype's stylesheet, inserted into the existing `/* Status bar */` section rather than appended at the file's end. Verified via a clean `tsc --noEmit` and a real browser check against `design/master-admin-panel.html`'s status bar — bar color/height/text color, site name, environment pill (including its `:hover` lightening), "Content Admin API v1", and the green "Connected" dot all confirmed correct, with the rest of the app shell (titlebar, activity bar, sidebar, tabs, console view, command palette) still fully visible and unchanged.

## Phase 21 — Editor Pane (Static, Matching the Prototype)

### Objective
Build the editor pane UI element (per `PROJECT.md`'s VS Code element mapping — the content form for the open record; could reuse Monaco for raw JSON/HTML/code fields), matching the design canvas prototype exactly, as a **static** piece: visual/structural only, not yet wired to the Phase 6 data management engine, Phase 7 Content Admin API, or any real database, and no real Monaco integration yet.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name and the established "match the prototype exactly" standard (Phase 12a/13/15/16/17/18/19/20): the editor pane's visual appearance and structure, built into the Phase 13 base global structure alongside the other static UI pieces, with static/placeholder content rather than a real schema-driven form or live Monaco instance. **Includes the breadcrumb path shown above the form** (e.g. "Northwind Blog › Pages › Contact" — site/collection/record context), which is not separately named in `PROJECT.md`'s VS Code element mapping but is part of the editor pane's header, per the design canvas prototype — static/placeholder path text in this phase, real path data wired in Phase 24. **Explicitly not in scope for this phase**: any backend or database connection, real record data, real breadcrumb data, schema-driven field rendering, or Monaco wiring. That remains deferred to wiring-up work.

### Completion Criteria
The real frontend renders an editor pane — including its breadcrumb path — visually and structurally matching the design canvas prototype exactly, using static placeholder content — with no backend or database connection, real record/path data, or Monaco integration.

**Status: Complete (2026-09-11).** The prototype's editor pane (`design/Main.dc.html`) has no Monaco, no JSON/code field, and no image/video/relation/date field anywhere — its five field types are plain `text`, `textarea`, a custom boolean `toggle`, an `enum` `select`, and a `tags` chip-list. "Could reuse Monaco" in this phase's own objective text is `PROJECT.md`'s conceptual framing, not something the prototype implements; Monaco/Image/Video wiring stays deferred to Phases 24/25/27 as already scoped. This phase's own prose example breadcrumb ("Northwind Blog › Pages › Contact") was illustrative text, not the prototype's actual default render state — per the strong "reuse the prototype's real default state" precedent already established by Phases 15–20, this phase instead renders the prototype's real default record: the **Posts** collection's **"Launching Our New Storefront"** (`p1`), the same record already referenced in the sidebar tree, tab label, and command palette. Breadcrumb: "Northwind Blog › Posts › Launching Our New Storefront". Fields: Title/Slug (text), Status (select, "published"), Excerpt (textarea), Tags (chips: "product", "launch"), Featured (toggle, ON) — all copied verbatim from the prototype's real `POST_SCHEMA`/`p1` record values. Save button rendered disabled (no `enabled` class), matching the seeded record's non-dirty default, same precedent as Phases 17/18/20. **Owner resolved a third design fork directly (2026-09-11):** the editor pane and the SQL console view are mutually exclusive in the prototype (only one tab's content shows at a time), but the console tab was left active by Phase 18. The owner chose to swap the active tab — the record tab becomes active (showing the editor pane), the console tab becomes inactive (still present in the tab strip, no longer showing its content) — mirroring Phase 18's own precedent of making the tab under discussion the active one. This means Phase 18's `.console-view` JSX is no longer rendered (its CSS is untouched and stays available for reuse, same "keep unreferenced CSS" precedent as `.console-error`/`.palette-empty`) — a deliberate, called-out change, not a silent one. **Structural fix found and applied**: the prototype wraps its record-form/console/empty-state views in a shared, scrolling `.editor-area` div (needed for `.editor-toolbar`'s `position: sticky` header to work) — the real frontend never had this wrapper (`.console-view` sat directly in `.editor-col`). Retrofitted `.editor-area` around the new editor pane, matching the prototype's real structure. Added the full field/form CSS (`.editor-area`, `.editor-toolbar`, `.editor-form`, `.field-row`/`.field-label`, `.input`/`.textarea`/`.select`, `.toggle`/`.toggle-knob`, `.tags-field`/`.tag-chip`/`.tag-remove`/`.tag-input`) verbatim from the prototype's stylesheet; `.breadcrumb`/`.btn-save`/`.btn-save.enabled` were already present (Phase 18's shared tokens) and reused as-is. No `onClick`/`onChange`/`onInput` anywhere; text/textarea fields use `readOnly`+`defaultValue` (Phase 18's `.console-input` convention); the `<select>` has no native `readOnly` and was left enabled-but-unwired rather than `disabled` (which would break its visual match to the prototype) — the same harmless, non-wired-affordance category as Phases 19/20's inert clickable elements. Verified via a clean `tsc --noEmit` and a real browser check against `design/master-admin-panel.html`'s record view — breadcrumb, disabled Save button, all six fields, and the sticky toolbar behavior on scroll all confirmed correct, with the rest of the app shell (titlebar, activity bar, sidebar, command palette, status bar) unchanged.

## Phase 22 — Split Panes (Static, Matching the Prototype)

### Objective
Build the split panes UI element (per `PROJECT.md`'s VS Code element mapping — compare two records, or the same record across environments), matching the design canvas prototype exactly, as a **static** piece: visual/structural only, not yet wired to the Phase 6 data management engine, Phase 7 Content Admin API, or any real database. This is the last VS Code element from `PROJECT.md`'s mapping table — every element (activity bar, explorer sidebar, editor tabs, editor pane, command palette, status bar, SQL Console panel, split panes) now has a static, prototype-matching phase.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name and the established "match the prototype exactly" standard (Phase 12a/13/15/16/17/18/19/20/21): the split-pane layout's visual appearance and structure (dividing the editor area, resizing behavior if shown in the prototype), built into the Phase 13 base global structure alongside the other static UI pieces, with static/placeholder pane content rather than real, comparable records or environments. **Explicitly not in scope for this phase**: any backend or database connection, real record comparison, or real environment switching. That remains deferred to wiring-up work.

### Completion Criteria
The real frontend renders split panes visually and structurally matching the design canvas prototype exactly, using static placeholder content — with no backend or database connection of any kind.

**Status: Complete (2026-09-11), scope necessarily adapted — the prototype has no split-pane implementation to match.** Unlike every other Phase 12–21 static piece, investigation found `design/Main.dc.html` contains **zero split-pane markup, CSS, state, or methods anywhere** — not a dormant/off-by-default toggle, nothing (confirmed by a full-file grep for "split"/"compare"/"pane"/"divider" and a full read of `.editor-col` and the component's `state`). `PROJECT.md`'s "compare two records, or the same record across environments" was never actually built into the prototype — the same category of gap Phase 21 found for Monaco, but total (Phase 21 at least had five real field types to match against). Resizing behavior — `PHASES.md`'s own hedge, "if shown in the prototype" — is likewise confirmed absent: no resize-handle markup, CSS, or JS anywhere, so none was built here either. This phase is therefore original design work, styled to match the app's existing dark-theme visual language and reusing existing tokens rather than a verbatim prototype copy — three decisions made directly with the owner before implementation: **(1) content** — both panes show the *same* record ("Launching Our New Storefront") labeled **Production** and **Staging**, reusing Phase 20's `.status-env`/`.status-env.staging` pill styling (`.staging` gets its first real use anywhere in the app) and Phase 21's six editor-form fields duplicated verbatim with identical values in both panes (no fabricated per-environment differences); **(2) surfacing** — a third tab ("Compare · Launching Our New Storefront", a two-rectangles split-view icon) was added to the tab strip and made active (record and console tabs both become inactive but stay in the strip, mirroring Phase 18/21's precedent of activating the tab under demonstration), plus a second sidebar icon button ("Open Split View") was added next to the existing "Open SQL Console" button, per the owner's direct request — required wrapping both buttons in a new `.sidebar-header-actions` flex container since `.sidebar-header`'s `justify-content: space-between` only correctly handles two children; **(3) interactivity** — both new elements stay static/inert (no `onClick`), consistent with every element built in Phases 15–21; real open/close logic remains Phase 24's job. Filled in `frontend/src/AppShell.tsx`: the third tab, the second sidebar button, and a `.split-view` (two `.split-pane`s divided by a `.split-divider`) replacing the single editor-form that was Phase 21's active-tab content — Phase 21's `.editor-form`/`.field-row`/etc. CSS is fully reused by both panes, nothing invented beyond `.split-view`/`.split-pane`/`.split-pane-header`/`.split-divider`/`.sidebar-header-actions`. As with Phase 21's removal of Phase 18's `.console-view` from the render output, this phase's single-form render output is superseded by the two-pane view — called out here deliberately, not silently. Verified via a clean `tsc --noEmit` and a real browser check — three tabs with the new one active, two icon buttons in the sidebar header, Production/Staging panes with independent scrolling (confirmed by scrolling one pane and observing the other stay in place) divided by a visible 1px line, both showing identical field values, with the rest of the app shell unchanged.

## Phase 23 — Staging Layer (Backend + Frontend, Draft Edits, Compare via Split Panes, Deploy to Live)

### Objective
Build a staging layer for the schema-driven Data Management path (Phase 6/Content Admin API) so edits made through AdminPanel accumulate as drafts rather than writing directly to a target site's live database — with the owner explicitly reviewing and deploying ("shipping") staged changes to production, rather than every save hitting the live database immediately. The Phase 22 split-pane UI is repurposed as the real comparison view between a record's staged/draft state and its live state before deploying. **Owner's direct addition to the roadmap (2026-09-11)**, inserted between [Phase 22] and the wiring phase (renumbered from Phase 23 to [Phase 24] to make room) — see `DECISIONS.md`'s new "Staging layer for data-management edits, deployed to live on demand" entry for the full reasoning. **Explicit deviation from the standing "Phase 4 onward, backend only" convention (owner's direct instruction, 2026-09-11): this phase covers real backend *and* real frontend work**, not backend-plus-test-page like Phases 4–11, and not static-only like Phases 12–22 — the staged-vs-live comparison has no purpose without the Phase 22 split-pane UI actually wired to show it, so this phase builds both sides together rather than splitting backend/frontend across two separate phases the way Phase 6/[Phase 27] (Image/Video backend vs. frontend) or the minimal-vs-full Monaco split (now [Phase 24]/[Phase 25]) did.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name and the objective above:

**Backend:**
- A real staging/draft storage mechanism for pending record edits made via the Phase 6 Data Management System engine — distinct from that engine's existing direct-write CRUD, which this phase changes the meaning of (a save becomes "stage a draft," not "write live").
- New endpoint(s) to save/update a draft, read a draft alongside its live counterpart (for the frontend diff view), and a "deploy"/"ship" endpoint that applies a staged change (or set of changes) to the actual target database, replacing the live row(s) with the staged values.
- Explicitly scoped to the **schema-driven Data Management / Content Admin API path only** (Phase 6/7) — the **SQL Console (Phase 5) is a deliberate, separate exception that stays as-is**: it already has its own direct-write safety model (read-only-by-default, `confirm: true`, the Phase 8 protected-table policy) and is not being folded into a staging/draft model by this phase. Do not conflate the two paths.

**Frontend:**
- Wiring the Phase 22 split-pane UI to a real staged-vs-live diff for a record — sourced from the backend endpoints above — instead of its current static Production/Staging placeholder content (which showed the same record twice with identical values).
- Real save-to-draft and deploy-to-live actions in the editor pane/split-pane UI (e.g. the existing static Save button and a deploy control), replacing their current inert, unwired states.
- This is real frontend work against real backend endpoints — not the "minimal test page" pattern Phases 4–11 used, and a genuine exception to Phase 12–22's "static, no backend wiring" rule for this one piece.

**Both sides:**
- Not yet decided and left open for when this phase actually starts: how a draft is stored (e.g. a shadow/pending copy of the row, a JSON diff/patch, a separate drafts table), what happens if the live row changes after a draft was started (conflict handling), whether staging is per-record or supports batched multi-record deploys, and how this relates to the already-open "versioning/history in the editor pane" item (see `DECISIONS.md`) — they may end up being the same feature or two related-but-distinct ones; not assumed here.
- Given the size of combining real backend and real frontend work in one phase, it may need to be broken down further once work actually starts — same caveat [Phase 24] carries for the same reason; do not assume it stays a single undivided phase if the owner wants to split it.

### Completion Criteria
A real edit made through AdminPanel's Data Management path is held as a draft (backend) rather than immediately applied to the target database; the Phase 22 split-pane view (frontend, really wired) shows a genuine staged-vs-live comparison for that record, sourced from real backend endpoints; and a real "deploy" action, triggered from the real UI, applies the staged draft to the live database on demand — verified end-to-end against at least one of the Phase 1b local target databases, through the actual frontend, not just via `curl`/an API client.

**Status: Complete (2026-09-11).** The four items this phase's own scope left open were resolved directly with the owner: drafts are a full shadow copy per record (not a diff/patch), deploy always overwrites live with no conflict detection, staging is per-record only (batching deferred), and versioning/history stays a separate, still-unscheduled item (though an append-only `deploy_log` table was added as a cheap forward-compat hook, with no read endpoint/UI against it yet). Backend: new `drafts`/`deploy_log` tables in `adminpanel_app` Postgres; new `backend/src/dataManagement/staging.ts` (reuses Phase 6's `getTableSchema`/`validateValues`/`updateRecord` directly — no new per-engine SQL); new `backend/src/dataManagement/stagingRoutes.ts` mounted at `/data-api`, adding `POST/GET/DELETE .../records/:id/draft` and a confirm-gated `POST .../records/:id/deploy` that also reuses Phase 8's protected-table policy (a draft can be staged against any table, but deploying to a protected one is blocked). Frontend: the Phase 22 split-pane UI in `frontend/src/AppShell.tsx` is now genuinely wired — via plain `fetch`/`useState` (no state library yet, that's [Phase 26]'s job) — for one hardcoded demo record (`postgres`/`posts` id `1`); Production pane is real and read-only, Staging pane is editable, Save persists a real draft, Deploy applies it live behind a confirm step. A new one-off seed script (`npm run seed:demo-post`) creates the demo `posts` table/row, since none existed in any real database before this phase — only in prototype/JSX markup. Verified via a new `backend/e2e/staging.spec.ts` (26 e2e tests total now passing, up from 21, no regressions; 125 unit tests still passing), clean `tsc`/`build` on both `backend/` and `frontend/`, and a real end-to-end browser check against the actually-running backend/database (stage → reload → still staged → deploy → both panes converge, Save/Deploy correctly re-disable). See `DECISIONS.md`'s "Phase 23 implementation" entry and `ARCHITECTURE.md` for full detail.

## Phase 24 — Wiring the Full Static UI to the Backend and Database

### Objective
Connect every static UI piece built in Phases 12–22 (background, login page, base structure, activity bar, explorer sidebar, editor tabs, SQL Console panel, command palette, status bar, editor pane, split panes) to the real backend and databases, replacing static/placeholder content with real, live data and real, working actions. This is the integration phase the standing convention (Phase 4–11: backend-only; Phase 12–22: static UI matching the prototype) has been building toward.

**Split into six sub-phases, 24a–24f, on 2026-09-11 (Admin's direct command)** — the phase's own text already flagged it might need breaking down given its size (nine separate wiring jobs bundled into one). Each sub-phase is a real, ordered milestone (not an arbitrary slice): 24a lands the auth gate first since nothing else can be meaningfully demoed or verified without a real login; 24b–24f build outward from there. Only 24a's scope is written up in detail below — per the phase-lock rule, 24b–24f's detailed objective/scope/completion-criteria get filled in when each one actually starts, not invented in advance.

### Scope
Pulling together everything built in Phases 12–22, now split as follows:
- **24a — Auth Wiring & Login Gate**: the Phase 12b login page wired to real `POST /auth-api/login`, plus the whole backend API gated behind the Phase 4 session for the first time. See the full sub-entry below.
- **24b — Explorer & Editor Tabs (Real Data Browsing)**: the explorer sidebar (Phase 16) wired to real collections/records (Phase 6/7), and editor tabs (Phase 17) wired to real open-record state and unsaved-changes tracking.
- **24c — Editor Pane (Real Record Editing)**: the editor pane (Phase 21) wired to real schema-driven forms (Phase 6/7) and real breadcrumb path data, with only minimal/basic Monaco for code/JSON fields (full Monaco stays Phase 25's job).
- **24d — SQL Console Panel Wiring**: the SQL Console panel (Phase 18) wired to the real Phase 5 backend, gated by the Phase 8 access/modification policy.
- **24e — Activity Bar & Split Panes (Multi-Site/Comparison)**: the activity bar (Phase 15) wired to real site/workspace switching, and split panes (Phase 22) wired to real record/environment comparison.
- **24f — Command Palette, Status Bar & State Wrap-Up**: the command palette (Phase 19) wired to real actions, the status bar (Phase 20) wired to real connected-site/save-sync state, and the ad-hoc state management (open tabs, active site, connections) introduced across 24a–24e pulled together — full, proper Zustand-based state management across the app stays Phase 26's job, not this phase's.

### Completion Criteria
The full VS Code-metaphor UI is genuinely functional end-to-end: a logged-in user can browse real collections/records via the explorer, open and edit them via real editor tabs/editor pane forms backed by Phase 6/7, run a real, policy-gated query via the SQL Console panel, and trigger real actions via the command palette and status bar — with no static/placeholder content remaining in any of Phases 12–22's pieces. Met once all of 24a–24f are complete.

**Phase 24 overall status: In progress — 24a/24b complete, 24c–24f not started.**

### 24a — Auth Wiring & Login Gate

#### Objective
Make login real end-to-end and gate the rest of the backend API behind the session for the first time — the foundation the other five parts depend on, since nothing else in Phase 24 can be meaningfully demoed or verified without it.

#### Scope
Wire the Phase 12b login page to real `POST /auth-api/login` (real submission, server-error display, redirect-on-success, and self-redirect away from `/login` if already authenticated); reuse the frontend's Zod `loginSchema` server-side for real request validation (replacing the Phase 4 manual `typeof` checks); add rate limiting to the login route; and gate `/sql-console-api`, `/data-api`, and `/media` behind the Phase 4 session via a new `requireAuth` middleware — `/auth-api/login|logout|me` stay reachable without a session.

#### Completion Criteria
A user can log in through the real login page and reach the rest of the UI; a logged-out visit to `/` hard-redirects to `/login`; every route outside `/auth-api` returns `401` without a session and succeeds with one; repeated failed login attempts return a real `429`.

**Status: Complete (2026-09-11).** Two real, pre-existing bugs were found and fixed as part of this wiring, not separately: `/media`'s static mount sat before the session middleware in `backend/src/index.ts` (so a session check placed there wouldn't have seen `req.session`), and no `requireAuth` middleware existed anywhere — every non-auth route was unauthenticated by construction, not by a deferred decision. Backend: new `backend/src/auth/loginSchema.ts` (duplicated from `frontend/src/loginSchema.ts` — no shared package exists between the two isolated npm packages, so this is kept in sync by hand; see `DECISIONS.md`), `loginRateLimit.ts` (`express-rate-limit`, 5 attempts/15 min per IP on `/login` only, owner's direct choice, with a `DISABLE_RATE_LIMIT` env-gated bypass read live per-request for local dev/e2e), and `requireAuth.ts` (checks `req.session.userId`, mounted once in `index.ts` after the whole `/auth-api` router so login/logout/me stay reachable without a session). `authRoutes.ts`'s `/login` now validates via `loginSchema.safeParse` instead of manual `typeof` checks — full password-complexity reuse, owner's direct choice, with the accepted risk that the real owner's pre-existing password might not satisfy the regex (resolved via `npm run seed:owner` if so; see `DECISIONS.md`). `index.ts` reordered to `express.json()` → `session(...)` → `/health` (public) → `/auth-api` (public) → `requireAuth` → `/media`/`/sql-console-api`/`/data-api`×2 (all now gated). Frontend: new `frontend/src/AuthGate.tsx` (checks `GET /auth-api/me` on mount, hard-redirects to `/login` if unauthenticated, matching the project's no-client-router convention); `App.tsx` now wraps `AppShell` in it; `LoginPage.tsx` gained a real `fetch` to `/auth-api/login`, server-error display, a `submitting` state, and an already-authenticated self-redirect; `vite.config.ts`'s dev proxy gained `/auth-api` alongside the existing `/data-api` entry, so the session cookie is set same-origin with no CORS package needed. No logout UI exists yet (status bar/command palette are still static — that's 24f's job); logout is verified directly via `POST /auth-api/logout`. e2e infra: `backend/e2e/globalSetup.ts` now performs a real login after seeding the e2e account and saves a Playwright `storageState`; `sql-console.spec.ts`/`policy.spec.ts`/`remote-site.spec.ts`/`data-management.spec.ts`/`staging.spec.ts` all reuse it via `test.use()` (`auth.spec.ts` deliberately stays on a fresh, unauthenticated context, and gained three new tests for the format-validation-before-credential-check path, the malformed-body 400, and the blanket 401 on a gated route). Along the way, found and fixed a second real, pre-existing gap: Phase 8's protected-table policy correctly blocks `DROP TABLE` on any name containing "user", which meant `staging.spec.ts`'s own protected-table fixture (`e2e_user_accounts`) could never be torn down by its own teardown and was silently leaking on every run — fixed by giving that fixture a per-run-unique suffix rather than trying to force a drop the policy must legitimately refuse; also made `fixtures.ts`'s `create()` drop-before-create for the general (non-protected) case, so a crashed run's leftover self-heals next time. Verified: `tsc`/`build` clean on both packages; 125 unit tests and 28 e2e tests (up from 21) passing, confirmed stable across repeated runs; live checks against a real running server — unauthenticated `/data-api`, `/sql-console-api`, `/media` all `401`; a real wrong-password attempt in the actual browser UI shows the inline server error; 6 rapid login attempts against a rate-limit-forced-on server returned `401×5` then a real `429`. **The real owner logged in successfully** (verified directly by the owner, not Claude, via a temporary ngrok tunnel to the dev server since the owner was away from the machine) — the accepted lockout risk from the "Phase 24a implementation" decision did materialize (the pre-existing password didn't satisfy the new complexity schema) and was resolved exactly as planned, via `npm run seed:owner` with a new compliant password. See `DECISIONS.md` for the schema-duplication, rate-limit, policy-gap, and real-login-verification entries.

### 24b — Explorer & Editor Tabs (Real Data Browsing)

#### Objective
Wire the explorer sidebar (Phase 16) and editor tabs (Phase 17) to real backend data — real collections/records browsing and real open-tab lifecycle — without building the actual editable form UI, which stays [Phase 24c]'s separate job.

#### Scope
Explorer: real collections (`GET /data-api/postgres/collections`) and, lazily per expanded group, real records (`GET /data-api/postgres/:collection/records`) — hardcoded to the `postgres` engine for now, matching [Phase 23]'s `DEMO` precedent (real multi-site/engine switching is [Phase 24e]'s job). Editor tabs: clicking a real record opens (or focuses, if already open) a real tab; tabs can be closed; switching tabs swaps the content area, which previously always rendered the [Phase 23] Compare view unconditionally. New tabs opened from the explorer are deliberately **read-only** in this phase (a minimal, real, generic field dump — not a schema-driven editable form, which is [Phase 24c]'s job) — so they get no dirty-dot wiring, since there's nothing to be dirty about yet. The one place real "unsaved-changes tracking" already exists — [Phase 23]'s Compare tab — gets its already-styled-but-previously-unused `.dirty-dot` actually wired for real. The Compare tab and the static "SQL · Northwind Blog" tab stay untouched ([Phase 24d]'s job for the latter).

#### Completion Criteria
The explorer shows real collections/records from the target database (not the old static "Posts/Pages/Authors" demo tree); clicking a record opens a real, read-only tab with its real field values; tabs can be opened, focused (not duplicated), switched between, and closed for real; the pre-existing Compare tab and its real dirty-tracking/Save/Deploy behavior are unaffected by the rewiring.

**Status: Complete (2026-09-11).** One small additive backend change enabled this: `backend/src/dataManagement/dataRoutes.ts`'s `GET /:engine/:collection/records` handler was already computing a collection's schema internally (via `listRecords`) but silently dropping it before responding — fixed to include `schema` in the response, saving the frontend a second round-trip per collection expansion. Frontend, entirely in `frontend/src/AppShell.tsx`: new plain-`useState` state (`collections`, `expanded`, `collectionData` — lazy per-collection cache, `tabs`, `activeKey`) — no store, matching [Phase 26]'s deferral of real Zustand wiring. A shared `labelFor()` heuristic (prefers a `title` field, then `name`, else `` `${collection} #${pk}` ``) drives both tree-row and tab labels. The hardcoded three-group/three-record explorer tree and the single static tab were replaced with real `.map()`s over fetched data; the content area, which previously rendered the Compare split-view unconditionally regardless of which tab looked "active," now genuinely switches between the Compare view (`activeKey === 'compare'`) and a new real tab's read-only field list (`schema.fields.map(...)`, reusing the existing `.field-row`/`.field-label`/`.input` CSS as-is — no new CSS was needed anywhere in this phase). A known, accepted, and expected consequence: the explorer now shows whatever real tables exist in the target Postgres database, including leftover e2e test-fixture tables (`e2e_widgets`, orphaned `e2e_user_accounts_<timestamp>` tables, etc.) alongside the real `posts` table — not filtered, matching "real means real" elsewhere in this project. Verified: `tsc --noEmit`/`npm run build` clean on both packages; 125 unit tests and 28 e2e tests still passing (one new assertion added to `data-management.spec.ts` confirming the records endpoint now includes `schema`); a real, logged-in browser session (via the dedicated e2e test account, not the real owner's — Claude does not enter real passwords into any field) confirmed: real collections/debris tables listed, `posts` expands to show its one real record with the correct label, clicking it opens a real read-only tab showing real field values (id/title/slug/status/excerpt/...), re-clicking the same record focuses rather than duplicates, switching to/from the Compare tab correctly swaps content, closing the new tab works, and editing a Compare-tab staging field for real shows the dirty-dot and enables Save exactly as before (the edit was never saved, confirming the rewiring didn't disturb Phase 23's real save/deploy path). One already-documented, deliberate consequence of an earlier phase made manual verification harder, not a bug found: [Phase 19] chose to render the command palette (`.palette-backdrop`, `position:fixed; inset:0`) statically open by default with no close logic (see that phase's own completion note), which blocks all pointer interaction with the page underneath until [Phase 24f] wires it up for real. Manual verification worked around it via a temporary devtools style change (not a code change, not persisted).

## Phase 25 — Real Monaco Integration and Code Editing (Fully Functional)

### Objective
Take Monaco (per `ARCHITECTURE.md`'s stack — the actual VS Code editor component) from Phase 24's minimal/basic wiring to a fully functional code-editing experience: real syntax highlighting, and everywhere Monaco is meant to appear per the locked design — code/JSON/HTML fields in the editor pane, and the SQL Console's query input.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name and the "minimal in Phase 24, fully functional in Phase 25" split confirmed by the owner: real Monaco configuration (language modes, syntax highlighting, whatever editor features the design calls for) replacing Phase 24's basic/placeholder-level integration, across both the editor pane's code/JSON fields and the SQL Console panel's query input.

### Completion Criteria
Monaco is fully functional everywhere it's meant to appear (editor pane code/JSON fields, SQL Console query input) — real syntax highlighting and editing features working, not just a plain textarea or a bare, unconfigured Monaco instance.

**Status: Not started.**

## Phase 26 — Zustand State Management, Fully Wired

### Objective
Take frontend state management from Phase 24's minimal/ad-hoc wiring to a real, fully-wired Zustand implementation (per `ARCHITECTURE.md`'s stack decision) — open tabs, active site/workspace, and connection status all managed through proper Zustand stores rather than one-off local state.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name and the "minimal in Phase 24, fully functional in Phase 26" split, matching the same pattern used for Monaco (Phase 24 → 25): real Zustand store(s) covering open tabs (Phase 17), active site/workspace (Phase 15), and connection status (Phase 5/7/8), replacing whatever ad-hoc state Phase 24 used to make the initial wiring work.

### Completion Criteria
Open tabs, active site/workspace, and connection status are all managed through real Zustand stores across the app, with no leftover ad-hoc/one-off state from Phase 24 covering the same concerns.

**Status: Not started.**

## Phase 27 — Image and Video Field UI (Frontend)

### Objective
Build the frontend picker/upload/editing UI for the **Image** and **Video** field types added to the backend content-editing logic in Phase 6 — the real editor-pane experience for these two field types, going beyond the bare "image picker" concept mentioned narratively in `DECISIONS.md`'s schema-driven forms decision.

### Scope
To be defined in further detail as the owner directs, including how much real editing logic is in scope (e.g. just upload/preview/replace, vs. actual crop/trim-style editing) — not assumed here. Implied by the phase name: real frontend components for selecting/uploading an image or video for a record's field, previewing it, and replacing/clearing it, wired into the Phase 21/24 editor pane so these two field types render as more than a plain text input. Depends on Phase 6's backend field-type support and Phase 24's general wiring being in place.

### Completion Criteria
A record with an Image or Video field can have its value set, previewed, and changed through real frontend UI (not a placeholder or plain text input), with the change persisted through Phase 6's backend.

**Status: Not started.**

## Phase 28 — Full Frontend/Backend/Database End-to-End Testing

### Objective
Run a full end-to-end test of the entire system as it stands after Phases 1–26 — the real frontend (background, base structure, every VS Code-metaphor UI piece, Monaco, Zustand, Image/Video fields), the backend (server, login, SQL console, data management engine, Content Admin API, secrets storage), and the databases (local Phase 1 engines and any real connected sites) — all working together, not just each piece checked in isolation as before.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name: real, whole-system flows through the actual UI (not Phase 3's retired test page, which Phase 11 already removed) — log in, browse real collections/records via the explorer, open and edit records (including Image/Video fields) via the editor pane, run a real policy-gated SQL Console query, use the command palette, and confirm the status bar/tabs/split panes all reflect real state — across every local database engine and, if a real site's Content Admin API is connected, that site's database too.

### Completion Criteria
A real, whole-system end-to-end pass succeeds with no failures across the full frontend, backend, and database stack built in Phases 1–26 — confirming the system works as one integrated product, not just as individually-verified pieces.

**Status: Not started.**

## Phase 29 — Neon Free-Tier Postgres Setup

### Objective
Set up a production PostgreSQL database on Neon's free tier — resolving the previously-open "where does the panel's own data live in production" question (`TASKS.md`), since Render's free tier (`DECISIONS.md`'s hosting decision) has no persistent disk. Same pattern as the Portfolio project's own production database.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name: create a Neon project/database for this panel's own production data (the login account from Phase 4, secrets from Phase 10, and whatever else the backend needs to persist) — the production counterpart to Phase 1a's local Postgres for the app itself, and distinct from Phase 1b's local target engines and the local/remote databases the SQL Console (Phase 5) or Content Admin API (Phase 7) connect to as managed targets. This is the panel's *own* database.

### Completion Criteria
A real Neon Postgres database exists and is reachable from the Phase 2 backend server, ready to hold the panel's own production data (login account, secrets, etc.) — no data needs to be migrated into it yet for this phase to be complete, just the database itself set up and connectable.

**Status: Not started.**

## Phase 30 — Actual Render Deployment, with UptimeRobot

### Objective
Stand up the real Render web service and get the panel actually live, **including setting up the UptimeRobot keep-alive monitor** — the hosting target was decided early (`DECISIONS.md`'s "Hosting — Render free tier + UptimeRobot" decision), but until now no phase has actually built the deployment or the monitor itself.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name: create the real Render web service for this project, connect it to the Phase 29 Neon database, configure whatever environment variables/secrets the backend needs (per Phase 10's storage mechanism), and get a real deploy live and reachable — plus the UptimeRobot keep-alive monitor `DECISIONS.md` already committed to, following the same pattern the Portfolio project used.

### Completion Criteria
The panel is deployed and reachable at a real Render URL, connected to the Phase 29 Neon database, with an UptimeRobot monitor actively pinging it to prevent free-tier idle spin-down.

**Status: Not started.**

## Phase 31 — Full End-to-End Testing of the Deployed Live App

### Objective
Run a full end-to-end test against the **real, live, deployed** app on Render (Phase 30) — the same scope Phase 28 already verified locally, now re-verified against production: the Neon database (Phase 29), real environment/secrets configuration, and the live URL itself, rather than a local dev environment.

### Scope
To be defined in further detail as the owner directs. Implied by the phase name: repeat Phase 28's whole-system flows (login, explorer/collections/records, editor pane including Image/Video fields, the policy-gated SQL Console, command palette, status bar) against the live Render URL and the real Neon database, confirming production configuration (env vars, secrets, database connection) actually works — not just that the code works locally.

### Completion Criteria
A real, whole-system end-to-end pass succeeds with no failures against the live, deployed app at its real Render URL, connected to the real Neon database — confirming the production deployment works, not just the local dev setup.

**Status: Not started.**

## End of Roadmap

Phase 31 is the last defined phase. The roadmap (Phase 0–31) is complete and locked — see the notice at the top of this file. No phase beyond Phase 31 exists; do not add one without an explicit, direct command from Admin.
