# Current Tasks

## Active

[Phase 24e] "Activity Bar & Split Panes" is in progress — only the split-pane/comparison half (done ahead of order, owner's direct request, 2026-09-11): a real per-tab split/compare toggle, the editor area starting empty by default, and a new global bulk-deploy action. The activity-bar half (real multi-site switching) has not been started. See Completed below for full detail.

## Next

The full roadmap (`PHASES.md`, Phase 0–31) is locked and gives the complete build order. [Phase 24] "Wiring the Full Static UI to the Backend and Database" is split into six sub-phases, 24a–24f (owner's direct command, 2026-09-11 — see `DECISIONS.md`'s "Phase 24 split" entry): 24a Auth Wiring & Login Gate (**complete and verified**), 24b Explorer & Editor Tabs (**complete**), 24c Editor Pane (**complete**), 24d SQL Console Panel Wiring (**not started**), 24e Activity Bar & Split Panes (**in progress, split-pane half only** — see Active above), 24f Command Palette/Status Bar/State Wrap-Up (**not started**, though its command-palette open/close mechanics were fixed as a standalone bugfix — see Completed below, not counted as phase progress since no command actually executes yet). **24d, and 24e's remaining activity-bar half, are both next up** — order between them not yet decided. A few items are still genuinely undecided even though most now have an assigned phase (the phase is *when* it gets decided, not that it already has been):
- Whether records get git-like diff/version history in the editor pane — **related to [Phase 23]'s new "Staging Layer" scope** but not necessarily the same feature; see `DECISIONS.md`'s Versioning/history open-decision entry.
- A real, remotely-implementable version of the Content Admin API (per-site auth/API keys, search, media listing, and an actual HTTP call to a separate site's own backend) — **not assigned to any phase**; [Phase 9]'s spec documents AdminPanel's own internal `/data-api` contract only, not this. See `DECISIONS.md`'s Phase 9 entry.
- Selective/per-record deploy — **not assigned to any phase**; [Phase 24e]'s bulk deploy-all is explicitly a "for now" simplification, with per-record/selected deploy noted as a real future step. See `DECISIONS.md`'s "Phase 24e (split-pane half) implementation" entry.

Resolved 2026-09-11: [Phase 24e] "Activity Bar & Split Panes" — split-pane/comparison half complete (done ahead of order, owner's direct request), activity-bar half not started. Fixed three real problems: the editor area no longer opens a Compare tab by default (starts empty); the sidebar's split-view button now toggles the *currently active* tab's own record into a live-vs-staged comparison in place (`viewMode: 'edit' | 'compare'` on `OpenTab`), instead of always jumping to one hardcoded demo record — a real bidirectional toggle, with the button's icon reflecting current state, and a "No Tab Selected" tooltip if clicked with nothing open; and Deploy was removed from every per-record pane entirely, replaced by one global sidebar button that deploys every pending draft across the whole site in one action. The old standalone Compare/`DEMO` tab and its entire dedicated state were deleted outright from `frontend/src/AppShell.tsx`. New backend: `listAllDrafts(engine)` (`backend/src/dataManagement/staging.ts`) and `POST /data-api/:engine/drafts/deploy-all` (`stagingRoutes.ts`), reporting partial success (`deployed`/`skipped`/`failed`) rather than all-or-nothing, reusing `deployDraft`/`isProtectedTable` verbatim. New `backend/e2e/deployAllDrafts.spec.ts` (30 e2e tests total, up from 29). Along the way, found and removed two purely-decorative, never-wired mock tabs (a static "Launching Our New Storefront" doc tab and a static "SQL · Northwind Blog" tab) that looked like real tabs but never responded to clicks — reported by the owner as "I can't change tabs." Also fixed, as a standalone bugfix not counted toward any phase: the command palette ([Phase 19]'s static piece) now has real open/close state (toggled via Ctrl/Cmd+K or the titlebar chip, closed via Escape or clicking outside it) instead of being permanently visible and blocking all pointer interaction with the page underneath. See `PHASES.md`'s 24e entry and `DECISIONS.md` for full detail.

Resolved 2026-09-11: [Phase 24c] "Editor Pane (Real Record Editing)" is complete — [Phase 24b]'s read-only record tabs are now genuine, schema-driven editable forms. Four design forks resolved directly with the owner before implementation (matching this project's established pattern): image/video fields are read-only path display only (real picker UI is [Phase 27]'s job); relation fields get a real `<select>` populated from the target collection's actual records; Monaco is scoped to `baseType === 'other'` (JSON-ish) fields only, not richtext; new tabs get Save-to-draft only, no Deploy (generalizing deploy/comparison to arbitrary records stays [Phase 24e]'s job). Frontend-only — new `frontend/src/schemaTypes.ts` and `frontend/src/editorFields.tsx` (the per-field-type dispatch), `frontend/src/AppShell.tsx`'s `OpenTab` restructured to a full per-tab draft/live/baseline lifecycle reusing [Phase 23]'s staging endpoints, and a new `@monaco-editor/react` dependency. New seed script (`npm run seed:field-types-demo`) and a new `backend/e2e/fieldTypes.spec.ts` (29 e2e tests total now passing, up from 28) since no prior seeded record exercised a relation/richtext/JSON/image/video field. See `PHASES.md`'s 24c entry for full detail.

Resolved 2026-09-11: [Phase 24b] "Explorer & Editor Tabs (Real Data Browsing)" is complete — the explorer sidebar and editor tabs are wired to real `/data-api` data for the first time, replacing the old static "Posts/Pages/Authors" demo tree and single static tab. New tabs opened from the explorer are deliberately read-only (schema-driven editing was [Phase 24c]'s job); the pre-existing [Phase 23] Compare tab is untouched and its real dirty-tracking now actually renders the dirty-dot. One small additive backend change (`GET /:engine/:collection/records` now includes `schema` in its response) made this possible without a second round trip. See `PHASES.md`'s 24b entry for full detail.

Resolved 2026-09-11: [Phase 24a] "Auth Wiring & Login Gate" is complete and verified — real login/logout/session-gate wiring across frontend and backend, server-side reuse of the Phase 12b Zod schema (full complexity, owner's direct choice), a strict 5-attempts/15-min rate limit on `POST /auth-api/login`, and every non-`/auth-api` route (`/sql-console-api`, `/data-api`, `/media`) now requires a session. Also fixed two real, pre-existing bugs found along the way (a `/media`-before-`session()` middleware ordering bug, and a protected-table-policy-caused test-fixture leak in `staging.spec.ts`). The real owner's password needed resetting (`npm run seed:owner`) to satisfy the new complexity schema, then logged in successfully — verified directly by the owner via a temporary ngrok tunnel (owner was away from the machine; torn down immediately after). See `PHASES.md`'s 24a entry and `DECISIONS.md`'s four Phase 24a/verification entries for full detail.

Resolved 2026-09-09: [Phase 10] "Secrets Storage Mechanism" is complete — an encrypted `secrets` table in `adminpanel_app` Postgres (AES-256-GCM via `node:crypto`, a `category` column separating `db-credential`/`api-key`), with the real SQL Console credentials (`TARGET_*`, `REMOTE_SITE_PORTFOLIO_*`) migrated in via `npm run migrate-secrets` and retired from `.env.local` for real (owner's direct choice — proven, not just built, by re-running Phase 5/7/8's verification with those env vars actually gone). See `PHASES.md`, `DECISIONS.md`.

Resolved 2026-09-09: [Phase 9] "Content Admin API Spec Documentation" is complete, with a scope correction — the spec documents AdminPanel's real internal `/data-api`/`/auth-api` contract (its own locally-reachable databases only), not a protocol any remote site implements, since that relationship was never actually built. New file `Project Docs/CONTENT-ADMIN-API-SPEC.md` (a deliberate exception to the six-file limit — see `CLAUDE.md`). See `PHASES.md`, `DECISIONS.md`.

Resolved 2026-09-09: the SQL console's granular, per-database/per-connection access policy (beyond the basic global read-only-by-default posture already resolved in Phase 5) is settled — a protected-table-name policy (tables containing "user"/"credential"/"session"/"password"/"secret" are read-only, never writable, across every connection) enforced in `backend/src/sqlConsole/policy.ts`, owner's direct choice, Phase 8. See `PHASES.md`, `DECISIONS.md`.

Resolved 2026-09-09: the SQL console's basic write-safety default is settled — **read-only by default, `confirm: true` required for non-`SELECT` statements** (owner's direct choice, Phase 5). A more granular per-connection policy remains Phase 8's job — see `PHASES.md`, `DECISIONS.md`.

Resolved 2026-09-09: "login mechanics" is settled — server-side session cookie, `bcryptjs` hashing, credentials in a new `users` table in Phase 1a's `adminpanel_app` Postgres, sessions persisted there too via `connect-pg-simple` (owner's direct choices, Phase 4). See `PHASES.md`, `DECISIONS.md`.

Resolved 2026-09-08: "backend server framework/runtime" is settled — **Express + TypeScript** (owner's direct choice, Phase 2). See `PHASES.md`.

Resolved 2026-09-08: "spec-first vs. site-first" is settled by roadmap ordering — Phase 6/7 (build the Content Admin API) come before Phase 9 (document its spec), so the project proceeds **site-first**. See `DECISIONS.md`.

Resolved 2026-09-08: "where the panel's own data gets persisted" (Render's free tier has no disk) is settled in direction — [Phase 29] "Neon Free-Tier Postgres Setup" — though the actual setup and migration of Phase 4/10's data into it hasn't happened yet.

Explicitly **not** current scope, deferred to **Version 2** (a future direction beyond the locked Phase 0–31 roadmap, not assigned to any phase, not to be built until asked for — see `PROJECT.md`, `DECISIONS.md`):
- Multi-account creation, an Admin role, or any policy system for additional "agent" accounts. Only the single-user login flow is being built for now.
- A "Generate Connection" API/UI to register a new site's database connection at runtime, replacing [Phase 7]'s current hardcoded-per-site approach.
- Connecting to a site's genuinely **remote/production** database (e.g. Portfolio's Neon Postgres), plus the SSL/TLS support the connection layer would need for that. The current roadmap (Phase 0–31) restricts the SQL Console and Phase 7's remote-site extension to databases reachable locally, on the same machine/disk as AdminPanel itself, only.

Also not scheduled (no Version 2 tag, just genuinely open): a dedicated media library/browser UI beyond per-record Image/Video fields (Phase 6/27) — see `DECISIONS.md`.

## Blocked

None.

## Completed

- [x] [Phase 24e] "Activity Bar & Split Panes" — split-pane/comparison half done — 2026-09-11. Done ahead of order (owner's direct request) while [Phase 24d] was still not started; the activity-bar half (real multi-site switching) remains untouched/static. Fixed three real, reported problems in the editor pane, all stemming from how [Phase 23] originally built the split view as a single hardcoded, always-open "Compare" tab tied to one demo record (`postgres`/`posts` id `1`): **(1)** the editor area opened that Compare tab by default on every app load instead of starting empty — fixed by removing the standalone tab and its entire dedicated state (`live`/`staged`/`baseline`/`hasDraft`/`loading`/`saving`/`deploying`/`error`, `refresh`/`handleSave`/`handleDeploy`/`updateStaged`) outright, and changing `activeKey`'s initial state from `'compare'` to `''` (the editor area now shows [Phase 24c]'s already-built "No editor open" placeholder by default); **(2)** the sidebar's "Open Split View" button always jumped to that one hardcoded record regardless of what was open — fixed by giving `OpenTab` a `viewMode: 'edit' | 'compare'` field and a new `toggleSplitForActiveTab()` that flips the *currently active* tab's own viewMode in place (no new pane is ever created); if no tab is active, a small auto-dismissing "No Tab Selected" tooltip appears above the button instead of doing nothing silently. The toggle is bidirectional (click again to un-split) and the button's icon swaps between a two-pane glyph and a single-page glyph to reflect current state. **(3)** Per the owner's direct follow-up: Deploy was removed from every per-record pane entirely (both single-edit and split view) and replaced by one global sidebar icon button (left of "Open SQL Console") that deploys **every** pending draft across the whole site in one action — explicitly bulk-only for now, selective/per-record deploy noted as a real future step. New backend capability since only single-record deploy existed before: `listAllDrafts(engine)` (`backend/src/dataManagement/staging.ts`, spans every collection for an engine unlike the existing per-collection `listDrafts`) and `POST /data-api/:engine/drafts/deploy-all` (`stagingRoutes.ts`), which iterates every pending draft, applies the [Phase 8] protected-table policy per draft, and reports partial success (`deployed`/`skipped`/`failed` arrays) rather than aborting the whole batch on one bad/protected draft — reuses `deployDraft`/`isProtectedTable` verbatim. Documented in `Project Docs/CONTENT-ADMIN-API-SPEC.md` §4.2. The split/compare view itself is fully schema-driven (unlike the old hardcoded Compare tab, which only ever knew about `posts`'s six specific fields): a shared `renderFields(source, editable)` helper in `AppShell.tsx` reuses [Phase 24c]'s `renderFieldInput()` dispatch for both the normal single-pane form and the two-pane split view, with a new `forceDisabled` parameter (`editorFields.tsx`) driving the read-only Production pane. **Also fixed along the way, as standalone bugfixes not counted toward this or any phase**: two purely-decorative, never-wired Phase 17/18 mock tabs (a static "Launching Our New Storefront" doc tab and a static "SQL · Northwind Blog" tab, the latter's own content pane already removed by [Phase 21]) were removed from the tab strip — they looked identical to real tabs but never responded to clicks, reported by the owner as "I can't change tabs." The command palette ([Phase 19]'s static piece) gained real open/close state (Ctrl/Cmd+K or the titlebar chip to open; Escape or clicking outside to close) instead of being permanently visible and blocking all pointer interaction with the page underneath. **Verified**: `tsc -b && vite build`/`oxlint` clean on `frontend/`; `tsc -p tsconfig.test.json` clean on `backend/` (no backend code changed beyond the new bulk-deploy addition); 125 unit tests and 30 e2e tests (up from 29, new `backend/e2e/deployAllDrafts.spec.ts`) all passing; a real, logged-in browser session confirmed every behavior above, including running the bulk deploy via the same authenticated `fetch` call the button itself makes (to avoid triggering a real blocking `window.confirm()` dialog through browser automation, same precaution [Phase 23]'s own manual verification used) and confirming two different records' live values were genuinely updated after a full page reload. See `PHASES.md`'s 24e entry and `DECISIONS.md`'s "Phase 24e (split-pane half) implementation" entry for full detail.

- [x] [Phase 24c] "Editor Pane (Real Record Editing)" Completed — 2026-09-11. Third of the six-part [Phase 24] split. Four design forks resolved directly with the owner before implementation (matching this project's established pattern of confirming ambiguous design decisions — Phases 18/19/21/22): **(1)** image/video fields render read-only path display only, no edit affordance (the real picker/upload UI is [Phase 27]'s separate job, and today's upload endpoint bypasses staging entirely — writes straight to live — so a staged text-edit of a path would imply a capability that doesn't work end-to-end yet); **(2)** relation fields (`specialType === 'relation'`) get a real `<select>` populated from the target collection's actual records, via the existing `GET /:engine/:collection/records` endpoint and the existing `labelFor()` helper, no search/pagination; **(3)** Monaco is scoped to `baseType === 'other'` fields only (unrecognized/untyped columns, e.g. a Postgres `jsonb` column) — `specialType === 'richtext'` fields stay a plain textarea; **(4)** new tabs get Save-to-draft only, no Deploy button — generalizing deploy/comparison to arbitrary records stays [Phase 24e]'s job, not this one. **Frontend-only, no backend changes needed** — `/data-api`'s CRUD, schema, and staging/draft endpoints ([Phase 6]/[Phase 7]/[Phase 23]) already supported everything this phase required. New `frontend/src/schemaTypes.ts` (shared `Values`/`FieldSchema`/`CollectionSchema` types plus `labelFor()`, pulled out of `AppShell.tsx` to avoid a runtime circular import with the new dispatch module) and `frontend/src/editorFields.tsx` (`renderFieldInput()`, the real per-field-type dispatch driven by `baseType`/`specialType` — relation → real select, image/video → read-only text, richtext → textarea, boolean → the existing `.toggle` pattern, date → native date input, integer/float → native number input, `other` → a minimal Monaco JSON editor via a new `@monaco-editor/react` dependency, everything else → plain text). `frontend/src/AppShell.tsx`'s `OpenTab` restructured from [Phase 24b]'s frozen `record` snapshot to a full per-tab draft/live/baseline/dirty/saving/error lifecycle, populated via the existing `GET .../draft` endpoint on open — relies on a confirmed backend behavior that the draft endpoint always returns a populated `draft` (real draft or normalized live values), so there's no "no draft yet" null-case to special-case client-side. `saveTabDraft()` defensively strips the primary key before posting, since `validateValues` (backend) rejects a request body containing the PK column at all. The Compare/`DEMO` tab's hardcoded `draftUrl`/`deployUrl` string constants became parameterized functions shared by both it and the new per-tab logic (a pure substitution, zero behavior change to the Compare tab). `toggleExpand`'s inline collection-fetch was extracted into a standalone `ensureCollectionLoaded()` so a record tab with relation fields can trigger it independently of explorer-tree state, reusing the same `collectionData` cache the tree already populates (zero extra fetch if that collection was already expanded). New seed script `backend/src/scripts/seedFieldTypesDemo.ts` (`npm run seed:field-types-demo`) creates a real `authors`/`field_types_demo` table pair exercising every new field type, since the only prior seeded record (`posts`/id 1) had no relation/richtext/`other`/image/video-tagged columns. New `backend/e2e/fieldTypes.spec.ts` (postgres-only, proving the CRUD/staging contract accepts these field shapes end-to-end) covers schema introspection, staging a partial draft touching JSON/image fields, a correct live-vs-draft diff, and deploy applying it live. **Verified**: `tsc -b && vite build`/`oxlint` clean on `frontend/`; `tsc -p tsconfig.test.json` clean on `backend/` (no backend code changed); 125 unit tests and 29 e2e tests (up from 28) all passing; a real, logged-in browser session against the seeded `field_types_demo` record confirmed every field type renders and behaves correctly (including a working Monaco JSON editor with no console errors, and a real relation dropdown populated with "Jane Doe"), Save/dirty-dot tracking worked exactly as designed, and the edit reloaded correctly from the draft after closing/reopening the tab and after a full page reload; the `posts` collection and the pre-existing Compare/`DEMO` tab were both re-confirmed working unchanged after the refactors. See `PHASES.md`'s 24c entry for full detail.

- [x] [Phase 24b] "Explorer & Editor Tabs (Real Data Browsing)" Completed — 2026-09-11. Second of the six-part [Phase 24] split. **Backend**: one small additive change — `backend/src/dataManagement/dataRoutes.ts`'s `GET /:engine/:collection/records` handler now includes the `schema` its own `listRecords()` call already computed internally but was previously dropping before responding, so the frontend gets a collection's primary key and field list in the same request as its rows. **Frontend**, entirely in `frontend/src/AppShell.tsx`: new plain-`useState` state (`collections`, `expanded`, a lazy per-collection `collectionData` cache, `tabs`, `activeKey`) replaces the old hardcoded three-group/three-record explorer tree and single static tab with real `.map()`s over fetched `/data-api` data — a group's records are only fetched the first time it's expanded, then cached. A shared `labelFor()` heuristic (prefers `title`, then `name`, else `` `${collection} #${pk}` ``) drives both tree-row and tab labels. Clicking a record opens a real tab (or focuses it if already open — no duplicates); closing falls back to the previous remaining tab, else the [Phase 23] Compare tab. The content area — which previously rendered the Compare split-view unconditionally regardless of which tab visually looked active — now genuinely switches between it and a new tab's content. New tabs are deliberately **read-only** (a generic field dump reusing the existing `.field-row`/`.field-label`/`.input` CSS, no new CSS anywhere this phase) since schema-driven editable forms are [Phase 24c]'s separate job — so they get no dirty-dot wiring at all (nothing to be dirty about yet). The Compare tab itself is untouched except its already-styled-but-previously-unused `.dirty-dot` now actually renders based on its real, pre-existing `dirty` check. A known, accepted, expected consequence: the explorer shows whatever real tables exist in the target Postgres database, including leftover e2e test-fixture debris (`e2e_widgets`, orphaned `e2e_user_accounts_<timestamp>` tables) alongside the real `posts` table — not filtered. **Verified**: `tsc --noEmit`/`npm run build` clean on both packages; 125 unit tests and 28 e2e tests still passing (one new assertion in `data-management.spec.ts` confirming the records endpoint now returns `schema`); a real, logged-in browser session — via the dedicated e2e test account, never the real owner's, since Claude does not enter real passwords into any field — confirmed every real behavior above, including that editing the Compare tab's staging field still shows the dirty-dot and enables Save exactly as before (the test edit itself was never saved). One already-documented, deliberate consequence of [Phase 19]'s own decision (render the command palette statically open by default, no close logic yet) made manual browser testing harder — worked around with a temporary, non-persisted devtools style change, not a code change. See `PHASES.md`'s 24b entry for full detail.
- [x] [Phase 24a] "Auth Wiring & Login Gate" Completed — 2026-09-11. First of the six-part [Phase 24] split (see `DECISIONS.md`'s "Phase 24 split" entry). **Backend**: new `backend/src/auth/loginSchema.ts` (duplicated from `frontend/src/loginSchema.ts` — no shared package exists between the two isolated npm packages, so kept in sync by hand), `loginRateLimit.ts` (`express-rate-limit`, 5 attempts/15 min per IP on `/login` only, a `DISABLE_RATE_LIMIT` env var read live per-request for local/e2e bypass), and `requireAuth.ts` (checks `req.session.userId`, `401` otherwise). `authRoutes.ts`'s `/login` now validates via the full Zod schema (owner's direct choice — accepted risk that the real owner's pre-existing password might not satisfy the complexity regex, resolved via re-seeding if so) instead of manual `typeof` checks. `index.ts` reordered — `session(...)` now runs before any route, `requireAuth` mounted once after the whole `/auth-api` router, gating `/media`/`/sql-console-api`/`/data-api`×2 for the first time; fixed a real pre-existing bug along the way (`/media`'s static mount previously sat before `session(...)`, so `req.session` wouldn't have existed there). **Frontend**: new `frontend/src/AuthGate.tsx` (checks `GET /auth-api/me` on mount, hard-redirects to `/login` if unauthenticated — no client router exists, matching the project's plain-pathname convention); `App.tsx` wraps `AppShell` in it; `LoginPage.tsx` now does a real `fetch` to `/auth-api/login` with server-error display, a submitting state, and a self-redirect if already authenticated; `vite.config.ts`'s dev proxy gained `/auth-api` (no CORS package needed — same-origin via the proxy, same pattern as the existing `/data-api` entry). No logout UI exists yet (status bar/command palette are still static, that's [Phase 24f]'s job) — logout verified directly via `POST /auth-api/logout`. **e2e infra**: `globalSetup.ts` now performs a real login and saves a Playwright `storageState`, reused via `test.use()` in five of six spec files (`auth.spec.ts` deliberately stays unauthenticated, gained three new tests). Found and fixed a second real, pre-existing gap along the way: [Phase 8]'s protected-table policy correctly blocks `DROP TABLE` on "user"-containing names, which meant `staging.spec.ts`'s own protected-table fixture could never be torn down by its own teardown and was silently leaking on every run — fixed with a per-run-unique fixture name (the policy itself was correctly left alone) plus a general drop-before-create self-heal in `fixtures.ts` for the non-protected case. **Verified**: `tsc`/`build` clean on both packages; 125 unit tests and 28 e2e tests (up from 21) passing, confirmed stable across repeated runs; live checks against a real running server (unauthenticated `/data-api`/`/sql-console-api`/`/media` all `401`; a real wrong-password attempt in the actual browser UI shows the inline server error; 6 rapid login attempts against a rate-limit-forced-on server returned `401×5` then a real `429`). **The remaining real-credential check is also done**: the real owner's pre-existing password didn't satisfy the new complexity schema (the anticipated risk materialized), resolved via `npm run seed:owner`, then logged in successfully — verified directly by the owner (Claude does not have and was not given the real password), reached from the owner's phone via a temporary ngrok tunnel to the local dev server (rate limiting forced on for the duration, torn down immediately after — see `DECISIONS.md`'s "Remote real-login verification via a temporary ngrok tunnel" entry). See `PHASES.md`'s 24a entry and `DECISIONS.md`'s four Phase 24a/verification entries for full detail.
- [x] [Phase 23] Completed — 2026-09-11. Built the staging/draft layer for the schema-driven Data Management path (Phase 6/7) — the SQL Console (Phase 5/8) is untouched, per the phase's own explicit scope. **Backend**: two new tables in `adminpanel_app` Postgres (`backend/src/db/schema.ts`'s `ensureSchema()`) — `drafts` (at most one row per `(engine, collection, record_id)`, a **full shadow copy** of the record's would-be values as `draft_values JSONB`, upserted) and an append-only `deploy_log` (populated from the exact row `updateRecord` returns at deploy time, zero extra queries) — a deliberate, lightweight forward-compat hook for a future deploy-history feature, kept separate from `drafts` since the two model different lifecycles ("what I'm editing" vs. "what has shipped"); no read endpoint/UI exists against `deploy_log` yet. New module `backend/src/dataManagement/staging.ts` (`saveDraft`/`getDraft`/`listDrafts`/`discardDraft`/`getDraftWithLiveDiff`/`deployDraft`) reuses Phase 6's `getTableSchema`/`validateValues`/`getRecord`/`updateRecord` directly rather than duplicating any per-engine SQL — `deployDraft` calls `updateRecord` verbatim for the actual write, so schema drift between staging and deploy surfaces for free as a normal `ValidationError`/400. New routes in `backend/src/dataManagement/stagingRoutes.ts`, mounted alongside `dataRoutes.ts` at `/data-api`: `POST/GET/DELETE .../records/:id/draft` and `POST .../records/:id/deploy` (body `{confirm:true}`, gated 404 no-draft → 403 Phase 8 protected-table policy, reusing `isProtectedTable` verbatim → 409 missing confirm, mirroring the SQL Console's `gatedQuery` ordering). A small `routeHelpers.ts` was extracted from `dataRoutes.ts` (`isValidEngine`/`assertKnownCollection`/`handleError`) so both routers share one copy. **Frontend**: `frontend/src/AppShell.tsx`'s Compare tab split-view is now genuinely wired (`useState`/`useEffect`, plain `fetch` — no state/data library yet, both are still Phase 26/24's job) for exactly one hardcoded demo record (`postgres`/`posts`/id `1`, kept as a single `DEMO` constant for Phase 24 to later swap for real routed params) — Production pane is real, controlled, and read-only (including fixing a small pre-existing gap: the Status `&lt;select&gt;` now has real `disabled`, not just a missing `readOnly`); the Staging pane is the only genuinely editable one; Save (existing-but-previously-unused `.btn-save.enabled` CSS) now POSTs a real draft; a new Deploy button (new `.btn-deploy` CSS, reusing `.btn-save`'s box model with the app's existing amber "staging" accent) POSTs a real deploy behind a `window.confirm(...)` (no modal component exists in this codebase yet). Tags simplified to a single comma-separated text field this phase (no array/tags base type exists in `fieldTypes.ts`; chip add/remove stays exactly as non-functional as it already was) — a deliberate, called-out scope trim, not a regression. A new Vite dev-proxy (`vite.config.ts`) routes `/data-api` to the backend. **New seed script** `backend/src/scripts/seedDemoPost.ts` (`npm run seed:demo-post`, following `seedOwner.ts`'s precedent) creates a real `posts` table + row in the postgres target database with the exact values every prior static phase already hardcoded in the JSX ("Launching Our New Storefront") — a real, previously-undocumented gap found while wiring: no such table/row existed in any real database before this phase, only in prototype/JSX markup. **Verified**: a new `backend/e2e/staging.spec.ts` (26 total e2e tests now passing, up from 21, no regressions) covers stage → live-unchanged → diff → deploy-without-confirm(409) → deploy(200, live now matches, draft consumed) → deploy-with-no-draft(404) → discard(200 then 404) across all three engines, plus a dedicated protected-table-policy-at-deploy(403) case and a schema-drift-at-deploy(400) case on postgres; all 125 unit tests still pass; `tsc --noEmit`/`npm run build` clean in both `backend/` and `frontend/`. Manually verified end-to-end in a real browser against the real running backend/database: real seeded data loaded into both panes, editing the Staging pane enabled Save, saving persisted the draft (confirmed surviving a full page reload), deploying (via direct API call, to avoid triggering a real `window.confirm()` dialog through browser automation) applied it live and the UI correctly showed both panes converged with Save/Deploy back to their disabled state. See `ARCHITECTURE.md`, `DECISIONS.md` for full detail.
- [x] Amended [Phase 23] "Staging Layer" to explicitly cover real backend **and** real frontend work together — 2026-09-11, owner's direct follow-up command ("In Phase 23 Add Backend and Frontend both for Staging Layer"), same day the phase was injected. A deliberate exception to the standing "Phase 4 onward is backend-only" convention — the staged-vs-live comparison has no purpose without the Phase 22 split-pane UI actually wired to real draft/live data, so this phase doesn't split backend and frontend across two separate phases the way Phase 6/Phase 27 (Image/Video) or the old Phase 23/25 (Monaco) pairing did. Updated `PHASES.md`'s Phase 23 title/objective/scope (now split into explicit Backend/Frontend/Both-sides sub-scopes) and completion criteria, plus `DECISIONS.md`'s "Staging layer for data-management edits, deployed to live on demand" entry. See `PHASES.md`'s Phase 23 entry for full detail.
- [x] Injected [Phase 23] "Staging Layer" into the locked roadmap, between [Phase 22] and the wiring phase — 2026-09-11, owner's direct command, given while discussing how [Phase 22]'s new split-pane UI could be reused for something real: comparing a staged draft edit against a record's live database state before deploying it, rather than every AdminPanel edit writing straight to a target site's live database. Renumbered every phase from the old Phase 23 onward up by one (old Phase 23–30 → new [Phase 24]–[Phase 31]) — updated every cross-reference across `PHASES.md`, this file, `DECISIONS.md`, `ARCHITECTURE.md`, `CONTENT-ADMIN-API-SPEC.md`, `PROJECT.md`, and `CLAUDE.md`. Scoped to the schema-driven Data Management/Content Admin API path only — the SQL Console keeps its own existing, separate write-safety model ([Phase 5]/[Phase 8]) unchanged. See `PHASES.md`'s new Phase 23 entry and `DECISIONS.md`'s "Staging layer for data-management edits, deployed to live on demand" entry for full detail.
- [x] [Phase 22] Completed — 2026-09-11. Unlike every prior static phase, the prototype had zero split-pane markup/CSS/state to match — confirmed via full-file grep and a full read of `.editor-col`/component state. Necessarily original design work, three decisions made directly with the owner: same record ("Launching Our New Storefront") shown in both panes labeled Production/Staging (reusing Phase 20's until-now-unused `.status-env.staging` styling and Phase 21's six form fields verbatim, identical values in both panes); surfaced via a third, active tab ("Compare · Launching Our New Storefront") plus a second sidebar icon button next to "Open SQL Console" (required a new `.sidebar-header-actions` wrapper); both stay static/inert, no `onClick`, consistent with Phases 15–21. Phase 21's single-form render output is superseded by the two-pane `.split-view` (its CSS fully reused, nothing wasted) — the same kind of active-tab-swap consequence Phase 21 itself applied to Phase 18. Verified via a clean `tsc --noEmit` and a real browser check, including independent per-pane scrolling. This completes every Phase 12–22 static UI piece. See `PHASES.md`'s Phase 22 entry for full detail.
- [x] [Phase 21] Completed — 2026-09-11. Built the editor pane using the prototype's real default record (Posts/"Launching Our New Storefront"), not `PHASES.md`'s illustrative "Pages/Contact" breadcrumb example — six fields (Title/Slug text, Status select, Excerpt textarea, Tags chips, Featured toggle) copied verbatim from the prototype's `POST_SCHEMA`/`p1` values, Save button disabled (non-dirty default). Owner resolved a third design fork directly: swapped the active tab (record active, console inactive) since the editor pane and console view are mutually exclusive in the prototype — Phase 18's `.console-view` JSX is no longer rendered (CSS untouched, still reusable). Retrofitted a real structural gap: the prototype's shared scrolling `.editor-area` wrapper (needed for the toolbar's sticky-header CSS), missing from the real frontend until now. Confirmed the prototype has no Monaco/JSON/image/video/relation/date field anywhere — Phase 21 only needed to match its actual five field types. Verified via a clean `tsc --noEmit` and a real browser check, including the sticky-toolbar-on-scroll behavior. See `PHASES.md`'s Phase 21 entry for full detail.
- [x] [Phase 20] Completed — 2026-09-11. The prototype's status bar has four segments driven by real state (site name, environment pill, a static "Content Admin API v1" label, a green-dot "Connected" indicator) plus a fifth, conditional "N unsaved" segment that only renders when `hasUnsaved` is true — since the prototype's own default state has `hasUnsaved: false` (the seeded tab is non-dirty), that segment was omitted, matching Phase 17's precedent of rendering its seeded tab non-dirty. Filled in `frontend/src/AppShell.tsx`'s `.statusbar` shell with `.status-left` ("Northwind Blog" + a "Production" pill, no click handler) and `.status-right` ("Content Admin API v1" + the green "Connected" dot). **Found and fixed a real gap**: the existing `.statusbar` CSS rule (Phase 13) was missing `color: #ffffff` present in the prototype's rule — added, since the new item text would otherwise inherit `.app`'s dimmer default text color. Added the five new `.status-*` CSS rules verbatim from the prototype's stylesheet. Verified via a clean `tsc --noEmit` and a real browser check against `design/master-admin-panel.html`, including the environment pill's `:hover` state. See `PHASES.md`'s Phase 20 entry for full detail.
- [x] [Phase 19] Completed — 2026-09-11. The prototype's actual command list (`design/Main.dc.html`) differs from the conceptual "New Post/Publish/Duplicate Record" examples in `PROJECT.md`/`DECISIONS.md` — the real 7-command-template set (`File: Save Record`, `View: Close Editor`, `View: Close All Editors`, `Site: Connect New Site…`, `Site: Toggle Environment (currently <env>)`, per-site `Site: Switch to <site>`, per-site `SQL: Open Query Console — <site>`) was used verbatim, expanded to 9 concrete items against Phase 15's already-committed site data (Northwind Blog, Acme Shop). Owner resolved a second design fork directly: since the palette is a modal overlay only shown when opened in the prototype and this phase has no real open/close logic, it's rendered **statically open by default**, same precedent as Phase 18's always-active second tab. Filled in `frontend/src/AppShell.tsx` with a new `.palette-backdrop`/`.palette` block (search row, placeholder-only readOnly input, 9 static `.palette-item` rows) between `.main-row` and `.statusbar`, no click handlers, `autofocus` omitted. Added the full `/* Command palette */` CSS block to `AppShell.css` verbatim, including the unreferenced `.palette-empty` rule. `.app`'s missing `position: relative` (vs. the prototype) was deliberately left unchanged — a no-op given `.palette-backdrop` uses `position: fixed`. Verified via a clean `tsc --noEmit` and a real browser check against `design/master-admin-panel.html`'s palette view, including the `:hover` state. See `PHASES.md`'s Phase 19 entry for full detail.
- [x] [Phase 18] Completed — 2026-09-09. Owner resolved a design fork mid-phase: since the prototype's console panel only shows when a console-type tab is active but Phase 17 had already made the record tab active, the owner chose to add a second, active "SQL · Northwind Blog" tab (console icon) beside the now-inactive record tab, rather than leaving the tabbar untouched and stacking the panel underneath regardless of tab state. Filled in `frontend/src/AppShell.tsx`'s `.editor-col` with the full `.console-view` reused verbatim from the prototype's own real "just opened the console" state: breadcrumb "Northwind Blog › SQL Console", a static Run button, three static table chips (`posts` 3, `pages` 2, `authors` 2 — real lowercase table names, matching Phase 16's counts), a read-only query textarea prefilled with `SELECT * FROM posts LIMIT 50;`, and the prototype's own "no results yet" placeholder text. Added the missing `.breadcrumb`/`.btn-save`/console-block CSS to `AppShell.css` verbatim from the prototype's stylesheet. No backend/database connection, click handling, or Phase 8 policy wiring of any kind. Verified via a clean `tsc --noEmit` and a real browser check against `design/master-admin-panel.html`'s console view, plus confirming `/login` still renders unaffected. See `PHASES.md`'s Phase 18 entry.
- [x] [Phase 17] Completed — 2026-09-09. Filled in `frontend/src/AppShell.tsx`'s `.editor-col` shell with a `.tabbar` containing a single static tab, reused verbatim from the prototype's own default render state — the doc icon, the label "Launching Our New Storefront" (the same record Phase 16 shows active in the explorer tree), no dirty dot, and a static, non-functional close (×) button, rendered active (`.tab.active`, the `#3794ff` bottom-border indicator). Added the missing tab-strip CSS to `AppShell.css` verbatim from the prototype's stylesheet; `.doc-icon-sm` (added in Phase 16) is used here for the first time. No backend/database connection, click-to-switch, working close button, or state of any kind. Verified via a clean `tsc --noEmit` and a real browser check against `design/master-admin-panel.html`, plus confirming `/login` still renders unaffected. See `PHASES.md`'s Phase 17 entry.
- [x] [Phase 16] Completed — 2026-09-09. Filled in `frontend/src/AppShell.tsx`'s `.sidebar` shell with a header ("Northwind Blog" + static "Open SQL Console" icon button), monospace endpoint line, and a tree — all reused verbatim from the prototype's own default demo/render state: **Posts** (count 3, expanded) with 3 record rows, the first highlighted/active; **Pages** (count 2) and **Authors** (count 2), both collapsed with no child rows. Added the missing sidebar CSS to `AppShell.css` verbatim from the prototype's stylesheet, plus a new global `.mono` utility class. No backend/database connection or state of any kind. Verified via a clean `tsc --noEmit` and a real browser check against `design/master-admin-panel.html`. See `PHASES.md`'s Phase 16 entry.
- [x] [Phase 15] Completed — 2026-09-09. Filled in `frontend/src/AppShell.tsx`'s `.activitybar` shell with two static site-avatar items reused verbatim from the prototype's own default demo data (`design/Main.dc.html`'s `SITES_INITIAL`) — "Northwind Blog" (`NB`, `#3fa7ff`, active) and "Acme Shop" (`AS`, `#c77dff`) — plus the flex spacer and a static, non-functional "Connect new site" `+` button. Added the missing `.activity-item`/`.avatar`/`.avatar-add`/`.activity-spacer` CSS to `AppShell.css` verbatim from the prototype's stylesheet. No backend/database connection or state of any kind. Verified via a clean `tsc --noEmit` and a real browser check against `design/master-admin-panel.html`. See `PHASES.md`'s Phase 15 entry.
- [x] [Phase 14] Completed — 2026-09-09. Owner's direct clarification: the favicon is the same icon shown in the titlebar's top-left `.app-icon` (two overlapping 16×16 rounded squares, `#3794ff`), already present verbatim in both `frontend/src/AppShell.tsx` and the prototype's `design/Main.dc.html`. Replaced `frontend/public/favicon.svg` (an unrelated, never-customized Vite template placeholder) with that exact icon — `currentColor` hardcoded to `#3794ff`, transparent background, no added tile (owner's direct choice: a literal match, nothing extra). `frontend/index.html` needed no changes. Verified in a real browser against the running app's titlebar icon. See `PHASES.md`'s Phase 14 entry.
- [x] [Phase 0a] Defined the core product concept: one reusable, personal admin panel driven by a shared Content Admin API protocol, instead of a bespoke UI per site — see `PROJECT.md`, `DECISIONS.md`.
- [x] [Phase 0a] Mapped the VS Code-metaphor UI (activity bar, explorer tree, editor tabs, command palette, status bar, split panes) onto the panel's intended interaction model — see `PROJECT.md`, `DECISIONS.md`.
- [x] [Phase 0a] Identified schema-driven forms as the load-bearing design decision, and scoped the SQL console as a deliberate, separate exception to the schema-driven CRUD path — see `DECISIONS.md`.
- [x] [Phase 0b] Locked the production stack: Electron + React/TypeScript (Vite) + Monaco + Zustand + native per-engine DB drivers + `safeStorage` + `electron-store` + `electron-builder`; evaluated and rejected Tauri — see `DECISIONS.md`.
- [x] [Phase 0c] Built and published a design canvas prototype (`design/master-admin-panel.html`) validating the VS Code-metaphor UI as a mock frontend — 2026-09-07, commit `90a90b4` — see `ARCHITECTURE.md`.
- [x] Committed the initial design/stack/prototype work to `main` — 2026-09-07 (commit `90a90b4`, "Add Master Admin Panel design, tech stack decision, and working prototype").
- [x] Migrated `docs/DESIGN.md` and `docs/tech.md` into this 6-file documentation system (`PROJECT.md`, `ARCHITECTURE.md`, `DECISIONS.md`) and removed the original two files — 2026-09-08, matching the Portfolio project's documentation structure.
- [x] Revised [Phase 0b]'s stack decision: pivoted from an Electron desktop app to a browser-based web app (frontend + backend server), hosted on Render's free tier + UptimeRobot, gated by a new single-user login (a desktop app didn't need one) — 2026-09-08, owner's explicit direction ("just use it as a browser based site"). A future Admin/policy-controlled multi-account system was noted as a direction but explicitly scoped out of current work. Updated `PHASES.md`, `PROJECT.md`, `ARCHITECTURE.md`, `DECISIONS.md` accordingly — see `DECISIONS.md` for the full before/after and newly-open implementation decisions.
- [x] Defined and locked the full build roadmap, Phase 1 through Phase 31 — 2026-09-08, phase by phase per Admin's direct commands: local multi-engine DB setup (1), backend server (2), a temporary test page (3), login (4), SQL console backend (5), the data management/CRUD engine and Content Admin API implementation (6–7, reordered mid-session once their dependency became clear), the SQL console's access/modification policy narrowed to backend-only (8), the Content Admin API spec document (9), secrets storage (10), a backend end-to-end test pass retiring the Phase 3 test page (11), every static UI piece matched to the design canvas prototype (12–22, including a flagged and resolved overlap between the old "Phase 8" and the new static SQL Console panel phase), wiring the full UI to the backend (23), Monaco and Zustand taken from minimal to fully functional (24–25), Image/Video field types split across backend (folded into Phase 6) and frontend (26), full local end-to-end testing (27), the panel's own production database on Neon (28), actual Render deployment with UptimeRobot (29), and full live end-to-end testing (30). `PHASES.md` was then explicitly locked against further unprompted edits — see `PHASES.md`'s lock notice and `CLAUDE.md` rule 3. `PROJECT.md`, `ARCHITECTURE.md`, `DECISIONS.md`, and this file were all updated to stay consistent with the completed roadmap (stale "not yet defined"/"open" framing removed, phase cross-references added throughout, a new decision record added for the Image/Video field types).
- [x] Split [Phase 1] into [Phase 1a] "Local Postgres for the App Itself" and [Phase 1b] "All Database Engine Types Setup" — 2026-09-08, owner's direct command. 1a is new: a dedicated local Postgres instance for the panel's own data (login account, secrets), giving Phase 4/10 a concrete local home instead of the previously open "database from Phase 1, or another mechanism" framing. 1b carries over the original Phase 1 scope unchanged (local Postgres/MySQL/SQLite as target/managed databases). Updated all downstream cross-references in `PHASES.md` (Phases 3, 4, 5, 7, 11, 28) and this file's Active/Next sections accordingly.
- [x] [Phase 1a] Completed — 2026-09-08. Used the owner's already-installed local PostgreSQL 18 (native Windows service, shared across their other local projects) — no new install needed. Created a dedicated, isolated role/database (`adminpanel_app`/`adminpanel_app`), verified reachable via `psql` connecting as that role (not the `postgres` superuser), and recorded connection details in a local, gitignored `.env.local` at the repo root (interim placeholder — Phase 10 still owns the real secrets-storage decision). See `PHASES.md`'s Phase 1a entry for full verification detail.
- [x] [Phase 1b] Completed — 2026-09-08. Postgres 18 and MySQL 8.0 were already installed and running natively — no new installs needed. Provisioned isolated target-engine credentials distinct from Phase 1a's app-own database: Postgres `adminpanel_target_pg`, MySQL `adminpanel_target_mysql`, and a real SQLite file at `local-data/adminpanel_target.sqlite` (new gitignored folder). Each engine verified with a full `CREATE TABLE`/`INSERT`/`SELECT`/`DROP TABLE` round-trip. Connection details appended to `.env.local` under a `TARGET_*` prefix. **Phase 1 (1a + 1b) is now fully complete.** See `PHASES.md`'s Phase 1b entry for full verification detail.
- [x] [Phase 2] Completed — 2026-09-08. Resolved the framework choice: **Express + TypeScript** (owner's direct decision). Scaffolded a real `backend/` Node.js project with a single `GET /health` route (no DB/login/SQL-console logic yet, per this phase's own scope). Verified via a real HTTP request (`curl` → `200 {"ok":true}`) and a clean `tsc --noEmit` type-check. This is the project's first real application code — `ARCHITECTURE.md` updated accordingly.
- [x] [Phase 3] Completed — 2026-09-08. Added `pg`/`mysql2`/`better-sqlite3`/`dotenv`, three throwaway endpoints (`backend/src/testRoutes.ts`, mounted at `/test-api/*`) each hitting one Phase 1b target database, and a plain static page (`backend/public/test.html`) with one button per engine. Verified by actually clicking all three buttons in a real browser — all returned real data. **Caught and fixed a real bug along the way:** `.env.local`'s `TARGET_MYSQL_PASSWORD` had an unquoted `#`, which `dotenv` silently truncates as a comment — broke MySQL auth until the password values were quoted. See `PHASES.md`'s Phase 3 entry for detail.
- [x] [Phase 4] Completed — 2026-09-09. Resolved login mechanics: server-side session (httpOnly cookie, `express-session`), `bcryptjs` password hashing, credentials in a new `users` table in Phase 1a's `adminpanel_app` Postgres (auto-created at startup, no migration framework needed for one table). Sessions are persisted in the same database via `connect-pg-simple` rather than in-memory, so logins survive dev-server restarts and Render's eventual free-tier cold starts. Added `backend/src/db/pool.ts` (shared `pg.Pool`), `backend/src/db/schema.ts` (`ensureSchema()`), `backend/src/auth/authRoutes.ts` (`/auth-api/login`, `/auth-api/logout`, `/auth-api/me`), and `backend/src/scripts/seedOwner.ts` (one-off `npm run seed:owner -- <username> <password>` CLI — the only account-creation/password-reset path; no signup flow). Did **not** gate any existing route behind auth — that's Phase 24's job. **Caught and fixed a real bug along the way:** `pool.ts` originally constructed its `pg.Pool` from `process.env` at module-import time, but ES module imports are hoisted and evaluate before `index.ts`'s own `dotenv.config()` call runs, so the pool was built before `.env.local` loaded — fixed by having `pool.ts` load its own env before constructing the pool. Verified end-to-end via `curl` and direct `psql` checks (see `PHASES.md`'s Phase 4 entry for full detail).
- [x] Split [Phase 12] into [Phase 12a] "Global Background Matching the Prototype" and [Phase 12b] "Login Page (Static, Theme-Matched)" — 2026-09-09, owner's direct command. 12a carries over the original Phase 12 scope unchanged (background only, pixel-matching the prototype). 12b is new: a standalone static login page, kept explicitly separate from Phase 13's base structure rather than folded into it. Since the design canvas prototype predates the browser-based pivot that introduced the login requirement, it has no login screen to match — the owner directed that 12b's visual design be created fresh within that phase, using the prototype's VS Code-metaphor theme rather than a pre-existing mockup. Updated downstream cross-references in `PHASES.md` (Phase 13's title/objective/scope/completion criteria, the "Phase 12/13/14..." standard-setting language across Phases 14–22, and Phase 24's wiring-up scope, which now explicitly includes wiring the login page to Phase 4's `/auth-api/login`).
- [x] [Phase 5] Completed — 2026-09-09. Resolved the SQL console's basic write-safety default: **read-only by default, `confirm: true` required for any non-`SELECT` statement** (owner's direct choice) — a granular, per-connection policy remains Phase 8's separate job. Added `backend/src/sqlConsole/` (`connections.ts` — persistent per-engine connections against the Phase 1b `TARGET_*` databases; `classify.ts` — a pragmatic, honest-effort write-statement classifier plus a single-statement-only guard; `adapters.ts` — normalizes `pg`/`mysql2`/`better-sqlite3`'s very different result shapes into one `{columns, rows, rowCount, elapsedMs, command?}` shape; `sqlConsoleRoutes.ts` — `GET/POST /sql-console-api/:engine/{tables,query}`). Uses meaningful HTTP status codes (400/409/500), following `authRoutes.ts`'s precedent rather than `testRoutes.ts`'s "always 200" style, since this is real, permanent code. Not gated behind the Phase 4 login session — that's Phase 24's job. Verified end-to-end across all three engines via `curl`: table listing, a real `SELECT` result, a 409 on an unconfirmed write, a 400 on a multi-statement request, and a full confirmed `CREATE`/`INSERT`/`SELECT`/`DROP` round trip that actually persisted. `/test-api/*` and `/auth-api/*` reconfirmed undisturbed. See `PHASES.md`'s Phase 5 entry for full detail.
- [x] [Phase 6] Completed — 2026-09-09. Built the schema-driven CRUD engine (owner's direct decisions: same Phase 1b databases as Phase 5; schema auto-introspected from real columns; special types tagged via a column-name naming convention — `_richtext`/`_image`/`_video`/`_id_relation`; real backend file-upload handling for Image/Video, not deferred to Phase 27). Added `backend/src/dataManagement/` (`introspection.ts`, `fieldTypes.ts`, `validation.ts`, `crud.ts`, `upload.ts`, `dataRoutes.ts`), mounted at `/data-api`: collections/schema/records CRUD plus `POST .../records/:id/upload/:field` (multer, new dependency) which saves a file to a new gitignored `backend/media/` and writes its served path into the record in one request. Table/column identifiers used in queries only ever come from introspection's own output — the injection guard for this path (distinct from Phase 5's raw-SQL path, which has no such guard need since it's explicitly unrestricted-with-confirmation instead). No write-safety gate needed here, unlike Phase 5 — this path is validated by construction. Not gated behind the Phase 4 login session — that's Phase 24's job. **Caught and fixed a real bug along the way:** `better-sqlite3` rejects binding a JS `boolean` directly — fixed by converting booleans to `0`/`1` for SQLite parameter binding only. Verified end-to-end across all three engines: schema introspection tagging, full CRUD round trips, a real file upload verified byte-for-byte via the new `/media` route, and the full negative-case matrix. `/test-api/*`, `/auth-api/*`, `/sql-console-api/*` reconfirmed undisturbed. See `PHASES.md`'s Phase 6 entry for full detail.
- [x] [Phase 7] Completed — 2026-09-09, narrowed to the SQL-bridging capability only (not the full Content Admin API surface). **Owner gave a hard mid-session constraint: no file in the target site's own repo (`C:\Portfolio`) may be touched at all** — ruling out this phase's original "site's own API server bridges to its DB" framing, since that would require adding an endpoint to Portfolio's own codebase. Built instead: Phase 5's direct-connection SQL Console model extended to a real, external site's real database (Portfolio's local dev Postgres, read from Portfolio's own untouched `.env`), not an HTTP API implemented by that site. New `remoteSites` registry in `backend/src/sqlConsole/connections.ts` (Postgres-only, id-keyed, env-driven — currently just `portfolio`); `adapters.ts`'s Postgres functions generalized to take a `Pool` parameter; new `GET/POST /sql-console-api/sites/:siteId/{tables,query}` routes reusing Phase 5's read-only/`confirm:true` safety gate verbatim. Verified against Portfolio's real dev database, deliberately read-only only (table listing, a real `SELECT`, and the full negative-case matrix — no confirmed write was ever sent against Portfolio's real data). `git status` inside `C:\Portfolio` confirmed clean throughout. See `PHASES.md`'s Phase 7 entry and `DECISIONS.md`'s new "Phase 7 implemented as a direct remote-DB connection, not an API bridge" entry for full detail.
- [x] [Phase 9] Completed — 2026-09-09. Documented AdminPanel's real, internal `/data-api`/`/auth-api` contract in a new `Project Docs/CONTENT-ADMIN-API-SPEC.md` (a deliberate exception to the six-file limit, see `CLAUDE.md`) — scoped down from the original "protocol a remote site implements" framing, since that relationship was never actually built. See `PHASES.md`, `DECISIONS.md`.
- [x] [Phase 10] Completed — 2026-09-09. Built a real secrets storage mechanism: an encrypted `secrets` table in `adminpanel_app` Postgres (AES-256-GCM via `node:crypto`, `backend/src/secrets/`), with the real `TARGET_*`/`REMOTE_SITE_PORTFOLIO_*` credentials migrated in via `npm run migrate-secrets` and retired from `.env.local` for real. See `PHASES.md`, `ARCHITECTURE.md`.
- [x] [Phase 11] Completed — 2026-09-09. Built the project's first permanent test suite (125 Vitest unit tests + 21 Playwright API-mode e2e tests against the real running server and Phase 1b local databases), then deleted Phase 3's throwaway `testRoutes.ts`/`public/test.html` for real once both suites passed clean. See `PHASES.md`, `ARCHITECTURE.md`.
- [x] [Phase 13] Completed — 2026-09-09. Built `frontend/src/AppShell.tsx`/`AppShell.css` — the owner's requested "base.html"-equivalent layout scaffold, read verbatim off the prototype's real markup/CSS, for every later static UI piece (Phases 15–22) to mount into. `App.tsx` now renders `<AppShell />` instead of a bare background div, folding Phase 12a's background in as the base layer (`App.css` deleted). Per the owner's direct clarification, the titlebar was built for real in this phase (app icon/name, empty breadcrumb slot, static "Ctrl K" button) rather than left as another empty shell, since no Phase 14–22 entry claims it; `.activitybar`/`.sidebar`/`.editor-col`/`.statusbar` stay empty shells at the prototype's exact dimensions/colors, each commented with which phase fills it. `LoginPage.tsx` and `main.tsx`'s pathname router untouched. Verified via a clean `tsc --noEmit` and a real browser check against `design/master-admin-panel.html`. See `PHASES.md`, `ARCHITECTURE.md`, `DECISIONS.md`.
- [x] [Phase 12b] Completed — 2026-09-09. Built `frontend/src/LoginPage.tsx`/`LoginPage.css`, a standalone static login page kept deliberately separate from `App.tsx` (Phase 12a's own untouched deliverable) — verified by temporarily mounting it in `main.tsx`, checking it in a real browser, then reverting `main.tsx` back to `App`. Designed fresh (no prior mockup existed) but reusing the prototype's exact existing tokens: the `.modal` card pattern, `.input` field styling, `.btn-primary.enabled` button styling, and the `.console-error` red token for validation messages, all on the same Phase 12a app-shell background. Added `zod` (owner's direct request) for client-side validation — username required, password requires 8+ characters with uppercase/lowercase/digit/special-character rules — checked on submit, no real submission/session/redirect logic. Also hardened against browser autofill/autocomplete interference during testing (owner's direct follow-up requests): `autoComplete` tuned per field, password-manager ignore attributes, and the readonly-until-focus trick. Recorded a forward note (see Next above, `DECISIONS.md`) to reuse the same Zod schema server-side plus add rate limiting when [Phase 24] wires this page to the real backend. **[Phase 12] (12a + 12b) is now fully complete.** See `PHASES.md`, `ARCHITECTURE.md`, `DECISIONS.md`.
- [x] [Phase 12a] Completed — 2026-09-09. Scaffolded the project's first real frontend, `frontend/` (Vite + React 19 + TypeScript), sibling to `backend/`. Implemented only the global background matched to the design canvas prototype's actual content (`design/Main.dc.html`'s `.app` container — not `master-admin-panel.html`'s own Claude Design Canvas editor-tool chrome, whose `--om-*`/Tailwind tokens belong to the canvas tool, not this project): a flat `#1e1e1e` background, `#cccccc` text, `"Segoe UI", system-ui, -apple-system, sans-serif` font stack, full viewport height. Vite's template boilerplate (counter demo, logos) stripped; `App.tsx` renders a single empty `.app-shell` div via a plain `App.css` (no Tailwind/CSS framework introduced). No other prototype UI chrome built, per this phase's narrow scope. Verified via a clean `tsc --noEmit` and by fetching the running dev server's served `index.html`/compiled `App.css` directly to confirm the exact values, then viewing it in a real browser. See `PHASES.md`, `ARCHITECTURE.md`.
- [x] [Phase 8] Completed — 2026-09-09. Owner's direct choice for the concrete rule: tables whose name contains "user"/"credential"/"session"/"password"/"secret" (case-insensitive) are read-only, never writable — not even with `confirm:true` — enforced uniformly across every connection (local Phase 1b engines and Phase 7 remote sites). New `backend/src/sqlConsole/policy.ts` (`isProtectedTable`, `extractWriteTargetTable` — a pragmatic regex-based extraction of a write statement's target table, same "not a full parser" style as `classify.ts`, `null` rather than "protected" for anything unrecognized). `sqlConsoleRoutes.ts`'s two query routes refactored to share one `gatedQuery` helper (multi-statement check → policy check, new, 403 → confirm check, existing, 409 → run) rather than duplicating the new check. Verified without ever sending a confirmed write against Portfolio's real data: a `DELETE FROM auth_user` against `sites/portfolio` with no `confirm` at all returned `403` (proving the policy fires independently of the confirm gate); a confirmed write naming a protected-pattern table on a local engine still returned `403`; a normal confirmed write on a non-protected table still succeeded (real `CREATE`/`DROP` round trip on a throwaway table); a real `SELECT` against Portfolio's actual `auth_user` still succeeded (read intentionally untouched). All other routes reconfirmed undisturbed; `tsc`/`build` clean; `git status` inside `C:\Portfolio` clean throughout. See `PHASES.md`'s Phase 8 entry for full detail.
